# 十二生肖棋盘 · 迭代优化过程文档

> **用途**：记录从创意到成品的全过程迭代。直接服务比赛评审第 4 项「迭代打磨」——展示玩法如何演进、为何演进、每次改了什么。
>
> **维护契约**：后续每次对话若发生**重大迭代**（玩法 pivot / 规则变更 / demo 重做 / 表现层或视角大改 / 新系统接入 / 数值大调 / 关键决策反转），我（AI 策划）会**自动**追加一条带日期的记录到本文档，并同步更新「当前状态快照」与「已锁定决策登记表」——无需用户提醒（8.9 用户确认固化为常态）。小修小补不单列。
>
> **评审一句话指引**：每条迭代记录都回答三问——改了什么、为什么改、改完后果如何。

---

## 一、当前状态快照

> 每次大迭代后同步刷新本节。

- **形态**：H5 平面 45° iso 棋盘，9×9（非 81 全走，外圈环 + 中心十字，46 格可走）
- **玩法骨架**：自由掷骰攒行动点（可一口气掷完 5 骰，也可边走边掷、行动点不够了再掷）+「点击地图方向/WASD 逐格移动，每步耗 1 行动点」+「行动点全局累积」+「数值对撞吸收滚雪球」（8.13 由大富翁强制走 N 格 pivot 而来，初稿误加回合制，8.13 晚用户反馈后撤销回合制；弃实体方向键改点地图按方位移动；WASD 屏幕相对方向，8.17 单平面镜头随转向动态 rotateZ(R)（R 恒为 90° 倍数，静止帧=四角度之一无变形，transform+--nrz 锁步过渡丝滑），W 始终=视角远方）
- **目标**：骰子预算内到达中心 Boss 并击败它
- **差异化**：骰子-大富翁母题 + 十二生肖符咒收集系统（build 搭配）
- **美术基调**：低多边形色块风（8.10 定调：纯色块 + 切面渐变高光 + 描边几何体，纯 CSS 无图；8.9 像素风已废弃——贴纸悬浮违和）
- **核心决策点**：手头骰子/行动点的资源分配——一口气全掷攒点、还是保留骰子以备不时之需；行动点如何分配移动路线，落点选哪格最优（途经不触发，只有落点触发）
- **自洽设计**：Boss 在中心、十字臂触碰即战 → 上臂=承诺决战，外圈游荡 vs 内圈决战分层
- **Boss 与符咒**：12 生肖 Boss 全部接入（狗3/鼠7/牛11/虎15/兔19/龙23/蛇27/马31/羊35/猴39/鸡43/猪47，终局年兽50）——特殊胜利条件包括虎=数值>Boss且≤2倍、兔=跳跃登顶、马=行动≥30格、羊=先击败3替罪羊、猴=先与分身融合、鸡=唤醒6沉睡格、猪=消耗500金币解锁；**龙Boss与蛇Boss现为普通数值判定，云雾/陷阱仅作主题地形**。对应 12 符咒效果已定稿（羊=替罪羊承担失败吸收20%、猴=分身同步80%、鸡=沉睡格集满3苏醒收益×2、猪=每格概率+骰/金币等，见文档）
- **特殊地形**：云雾格（☁ 踩一次消失/离开才消失）、沉睡格（💤 盲盒不显示收益、集满3个苏醒，数值/骰子/金币三类）、分身格（💠 到达融合）、陷阱格（-数值/-骰子/回起点，回起点可反复触发）
- **demo**：`demo/index.html`（单文件，手机自适应），第 1 张棋盘已可玩；第 1 关走廊验证版 `demo/level1.html`（用户指定参数，数值已验证）；3D+2D 双视角原型 `demo/level1-3d.html`（8.9 实验）；**游戏主入口 `index.html`**（主界面 + 50 关选关 + L1/L2 手工 + L3~L50 生成器草稿可玩，8.9 建壳 / 8.10 换低多边形皮肤 / 8.13 行动点自由移动 + 界面放大 + 音效）
- **编辑器**：`地图编辑器.html` 支持全部 12 符咒/武器模拟、传送门连通地图、跳跃孤立格、沉睡格/分身格/替罪羊分身摆放、**多层地图（层1/2/3 分层编辑 + 跨层传送门）**，写回游戏本体前重读磁盘防覆盖；主菜单「地图编辑器」已直接接入，旧关卡无 `z` 与 Boss 字符串 `z` 均兼容编辑
- **存档系统**：主界面"开始游戏"→存档管理界面，**6 个固定槽位（两行三列）**（新建=全新进度/读取/删除），`sx_save_<n>` 各存完整进度（含成就 `ach`），`sx_active_save` 记录当前激活槽；旧单存档进度自动兼容迁移
- **成就系统**：数据驱动成就表 `ACHIEVEMENTS` 共 21 项，含通关/金币/符咒/武器里程碑及全部 13 个生肖Boss/年兽专属成就；解锁时使用从顶部下落的独立成就横幅和专属四段上行音效，多成就按队列逐个播放，每存档独立持久化
- **音乐/设置系统**：`audio/menu.*` 主界面乐 + `audio/theme1~5.*` 5种地图主题（按关卡分段循环），`Music` 管理器播放，缺文件静音；设置面板可独立调节背景音乐/音效音量并持久化，顶栏静音按钮继续作为总静音
- **手工关卡库**：`levels/` 已存 31 张手工关卡（L1~L23 全部由编辑器手工制作，含兔系 16-19 与龙系 20-23；L24~L31 8.22 制作）——游戏优先加载手工关卡，缺失时回退生成器
- **待接（8.10 设计稿规划）**：DAG 分叉路径（四旋钮：密度/宽度/步数张力/嵌套）+ 难度引擎分段改造、挑战模式（独立存档肉鸽）、低多边形色板/几何形态细化（宝箱/武器系统已部分落地）
- **关键风险**：分叉密度=玩法生死线（8.10 已定为四旋钮之一，前期 1 个→后期 4-6 个；难度引擎需从单段改分段计算）

---

## 二、已锁定决策登记表

| 项 | 决策 | 谁定 / 何时 | 备注 |
|---|---|---|---|
| 棋盘形态 | 9×9 iso 平面棋盘（非爬塔） | 用户 / 8.3 | 从塔式 pivot 而来 |
| 移动规则 | 自由掷骰：可一次掷完 5 骰攒行动点，也可边走边掷；点击地图方向/WASD 逐格移动每步耗 1 行动点，行动点全局累积 | 用户 / 8.13 | 推翻 8.3「强制走 N 格」；分叉由自由移动天然解决，不再弹窗选路；8.13 弃实体方向键改点地图（点主角前方=向前）；8.13 晚用户反馈撤销回合制（初稿误加"每回合掷 1 骰"限制） |
| 50 关映射 | 50 张不同 9×9 棋盘，AI 生成 | 用户 / 8.3 | 关卡体验变化 + AI 驾驭力 |
| Boss 位置 | 棋盘中心 | 策划 / 8.3 | 触碰即战，臂=承诺 |
| Boss 触碰 | 触碰即开战（不等走完） | 策划 / 8.3 | Boss 分割十字为 4 臂 |
| 每层/每格选位 | 强制走 N，落点唯一触发 | 规则 | 途经不触发 |
| 平局 | 玩家胜 | 策划 / 8.3 | 休闲友好向，可调 |
| 吸收 | 全额（胜方 += 敌方数值） | 框架 / 8.2 | 滚雪球 |
| 狗符咒 | 战斗有效数值 ×1.2 | 框架 / 8.2 | 吸收仍按敌方原值 |
| 步数张力杠杆 | 骰子预算 D（每关可掷次数） | 策划 / 8.3 | D 越小越紧 |
| 第1关验证参数 | 起始4 / 5骰 / 15步走廊 / 格+2途经即收 / 门怪6·16·16 / Boss24 | 用户 / 8.3 | 无岔路无吸收，验证骰子张力手感 |
| 表现层视角 | 伪 3D 倾斜棋盘跟随主视角 + 2D 俯视地图切换（实验） | 用户 / 8.9 | 等轴静态视角被否（"左上往右下看着怪"）；8.17 先试四层同时渲染后被否（贴图/DOM 4 倍负担→每步卡顿），**还原为单平面 + 动态 rotateZ(R)**（R 恒为 90° 倍数，静止帧=四角度之一无变形；transform+--nrz 同缓动锁步过渡保持丝滑） |
| 游戏外壳 | index.html 单文件三屏：主界面 / 50 关选关 / 关卡 | 用户+策划 / 8.9 | 选关格金框=生肖 Boss 关 |
| 进度解锁 | localStorage 持久化，通 N 解锁 N+1 | 策划 / 8.9 | 评审模式（全解锁）待做 |
| 第1关教程参数 | 起始4 / 4骰 / 直线9步 / 门怪6·8 / Boss16（恰好平局收尾） | 用户+策划 / 8.9 | 只教两件事：丢骰子前进 + 比大小 |
| 关卡 UI 布局 | 三卡统计（行动点/骰子/距Boss）+ 点地图移动 + 掷骰居中带动画 + 控制行[地图][掷骰][重开] + 顶栏音效开关 | 用户 / 8.9→8.13 | 8.13 加行动点卡、弃方向键、加音效开关；8.13 晚取消回合制→移除「回合」卡与「结束行动」按钮；玩家数值由棋子 token 显示 |
| 美术基调 | 低多边形色块风（纯色块 + 切面渐变高光 + 描边几何体，纯 CSS 无图） | 用户 / 8.10 | 8.9 像素风废弃（贴纸悬浮违和）；色板/几何细化待补 |
| AI 关卡流水线 | genLevel：种子随机走 + 骰子分布反解步数 + PLAN 三单调约束 | 策划 / 8.9 | v1 骨架，岔路/符咒待接 |
| 难度曲线机制 | 目标胜率 0.90→0.52 + 胜率/步数/骰子三单调 | 策划 / 8.9 | 粒度受骰子分布限制 ±7pt/步 |
| 路径结构 | DAG 图结构（主干 + 分叉收敛，非树状） | 8.10 稿 | 分叉四旋钮：密度/宽度/步数张力/嵌套 |
| 分叉点视野 | 站分叉点可见每分支第一格，深处不可见 | 8.10 稿 | 有依据非瞎选；嵌套分叉走入才显现 |
| 宝箱系统 | 数值/骰子双池，玩家主动选是否开 | 8.10 稿 | 前期好结果 70%+ → 后期 debuff 反超 |
| 失败机制 | 不引入安全垫，失败即结束重试 | 8.10 稿 | 保留代价让符咒选择有分量（肉鸽逻辑） |
| 武器系统 | 每局限 1 件，跨维度效果，永久收集 + 局内限量 | 8.10 稿 | 与符咒区分：武器=资源转化/量变，符咒=规则质变 |
| 羊Boss(35) | 先击败 3 个替罪羊分身才能挑战 Boss；羊符咒=首次战败可让替罪羊承担失败吸收20%（每局一次，Boss无效） | 用户+策划 / 8.22 | 完成 12 生肖接入 |
| 猴Boss(39) | 先到达分身格融合才能挑战 Boss；猴符咒=地图某格生成分身，主角获得数值时分身同步+80%，到达融合吸收 | 用户+策划 / 8.22 | 分身格新增地形 |
| 鸡Boss(43) | 沉睡格集满3个同时苏醒，唤醒6个(2组)才能挑战 Boss；鸡符咒=每经一个沉睡格立即苏醒获120% | 用户+策划 / 8.22 | 沉睡格新增地形 |
| 猪Boss(47) | 消耗500金币解锁 Boss 格才能挑战；猪符咒=每经一格概率获得额外骰子/金币 | 用户+策划 / 8.22 | 金币格辅助凑500 |
| 马符咒 | 本关第一个与最后一个骰子点数×2行动点（非仅首骰） | 用户 / 8.22 | 修复原首骰×2从未生效的bug |
| 回起点陷阱 | 反复触碰反复回起点（不入 doneSet，入口拦截也豁免），数值/骰子陷阱仍一次性 | 用户 / 8.22 | 强迫玩家思考其它道路 |
| 关卡数据隔离 | startLevel 深拷贝 lv，铺云/连杀只作用副本，重进地图干净 | 策划 / 8.22 | 修复铺云污染原始关卡 |
| 多层地图(50关) | 同关3层(z)，各层坐标独立靠跨层传送门连通；游戏端只渲染当前层 | 用户+策划 / 8.22 | 编辑器层1/2/3选择器 + 传送门目标层 |
| 沉睡格 | 盲盒显示💤?；支持数值/骰子/金币三类收益；集满3个苏醒 | 用户 / 8.22 | 鸡符咒=集满3苏醒收益×2 |
| 分身格 | ①39猴Boss关自带无符咒也生效 ②带符咒其它关自动生成 ③无符咒非39不生成 | 用户+策划 / 8.22 | 获得数值实时+80%同步 |
| 兔跃落点 | 轴向/对角2格 + 斜前方1格共12个 | 用户 / 8.22 | 支持"正前方一格往左一格" |
| 主界面UI | 主界面UI.png(1024×1536竖版)6按钮热区 | 用户 / 8.22→8.29 | 开始游戏/玩法说明/成就/地图编辑器/符咒图鉴/设置均已接入；地图编辑器跳转同目录 `地图编辑器.html` |
| 存档系统 | 3固定槽位,可新建/读取/删除;开始游戏→存档界面 | 用户+策划 / 8.22 | sx_save_<n> 各存完整进度 |
| pad贴图 | `.ground` 支持顶面贴图 ground.png,缺图回退CSS渐变 | 用户 / 8.22 | 与沙地主题协调 |
| 成就系统 | 21项数据驱动成就；顶部下落横幅+专属音效+顺序队列 | 用户+策划 / 8.22→8.29 | 13个Boss/年兽各有专属通关成就，旧存档静默补齐历史Boss成就 |
| 音乐系统 | main菜单+5主题音乐,按关卡分段循环；背景音乐/音效独立音量 | 用户+策划 / 8.22→8.29 | 设置持久化，0 音量刷新不回默认值，缺文件静音 |
| 存档槽位 | 6槽两行三列,新建=全新进度从第1关开始 | 用户 / 8.22 | SAVE_SLOTS=6 |
| 武器系统 | 全金币解锁(无关卡门槛);刃/镰刀/匕首可升1~3星(100/150/250金);骰子类不升星 | 用户+策划 / 8.22 | 幸运骰不限次,诅咒骰+3掷6损1 |
| 武器升星 | 刃:临时+50/80/120每格-5/-4/-3;镰刀:每杀+5/8/12;匕首:精英+3/5/8行动 | 用户+策划 / 8.22 | 星级影响基础数值,展示当前+升星预览 |
| common贴图 | 主角hero.svg/分身avatar.svg/武器weapons/,portal·chest·clone=主题normal+竖立贴图 | 用户 / 8.22 | images/common/,缺图回退 |
| 武器图标 | iconbox 48×48,贴图盖emoji兜底(后删兜底) | 用户 / 8.22 | 放大+层叠修复 |
| 符咒图鉴 | 三列四行,贴图/效果/解锁关/Boss特殊效果 | 用户+策划 / 8.22→8.29 | 按 charm-cell 上下版心重排；龙Boss无特殊效果，不显示旧踏云条件 |
| 年兽贴图 | 50关年兽立绘=boss_nian.svg | 用户 / 8.22 | ART.bosses.nian 路径对应 |
| 清空进度 | 成就界面一键重置为全新版本 | 用户 / 8.22 | 清成就+存档+进度 |
| 通关符咒图标 | 获得符咒弹窗用 charms 贴图 | 用户 / 8.22 | 贴图优先,emoji兜底 |
| 兔系关卡(16-19) | 手工制作：兔1(传送门×2)/兔2/兔3/兔Boss(跳跃登顶) | 用户 / 8.19-8.20 | levels/level_16~19 |
| 龙系关卡(20-23) | 手工制作：龙1(云×4)/龙2(云×12)/龙3(云×4)/龙Boss | 用户 / 8.20 | levels/level_20~23 |
| 挑战模式 | 独立存档肉鸽，一命爬 50 关，全符咒可选 | 8.10 稿 | 失败清进度不清纪录 |
| 难度引擎分段 | 分叉后改"多段拼接"计算胜率 | 8.10 稿 | 不推翻引擎，扩为分段（前段+各支线+汇合后段） |

> 待锁：11 符咒效果已定稿接入（剩余：12 生肖全完成）；武器效果库与数值、金币产出/商店曲线、宝箱结果池随 50 关衰减曲线、低多边形色板与几何形态细化。

---

## 三、迭代记录（按时间倒序，最新在上）

### 【8.29·提交前定向修复 + 评审存档6】（生肖英雄_游戏本体.html）
- **改了什么**：①Boss虎连杀判断移到羊/猴/鸡/猪前置条件之后，不能再绕过3替罪羊、分身融合、6沉睡格或猪Boss500金币门槛；前置不满足时连杀状态保留不消费。②修复50关第一组传送门反向链接：层2 `(4,8)` 的目标由错误 `z:2` 改为层1 `(0,1,z:1)`，四个传送门目标全部有效。③猪Boss出战说明删除“本关有金币格”错误承诺，改为显示当前金币、500金币挑战门槛，并明确失败不扣、胜利才扣；不足500时在准备弹窗直接警告。④清空全部进度同步删除 `sx_tileintro_v2` 并清空内存 `introedTiles`，新流程会重新看到特殊格教学。⑤制作名单增加来源状态：设置打开时返回设置，年兽终局打开时关闭后直接返回终局祝贺弹窗，不再误跳设置。⑥新增 `ensureReviewSave6()`：游戏启动时若槽6为空，自动创建评审存档（全50关解锁 `unlocked:51`、12符咒、6武器、可升星武器满星、金币1000），不覆盖已有槽6。
- **为什么**：用户在提交前按风险清单选择修复机制绕过、终局传送门、猪Boss说明、教学重置、制作名单返回，并要求准备全解锁评审存档6。
- **后果（已验证）**：羊/猪前置条件下虎连杀不能触发；50关四个传送门目标均存在；猪说明显示当前金币且无金币格误导；终局制作名单关闭不打开设置、设置制作名单关闭恢复设置；清空进度后教学键/内存均为空；槽6数据为50/50关、1000金币、6武器、12符咒，且已有槽6不覆盖。无头Chrome回归、脚本语法和格式检查通过。
- **文件**：`生肖英雄_游戏本体.html`

### 【8.29·选关界面金框Boss说明】（生肖英雄_游戏本体.html）
- **改了什么**：关卡选择界面网格下方新增一行说明「🔲 金框关卡为 Boss 关」——含金色小方块示例（`.boss-dot` 与 Boss 关金框同款样式），小屏适配字号。方便玩家识别 13 个生肖 Boss 关（BOSS_LV）。
- **为什么**：用户希望玩家理解金框关卡的特殊含义。
- **后果（已验证）**：说明文字位于网格正下方（`hintTop ≥ gridBottom`），窄屏不横向溢出，Boss 关计数13与说明一致。脚本语法检查通过。
- **文件**：`生肖英雄_游戏本体.html`（`.lv-bosshint`/`.boss-dot`/`screenLevels` 说明行）

### 【8.29·非Boss关Boss贴图按关差异化 + Boss立绘加大】（生肖英雄_游戏本体.html）
- **改了什么**：①非Boss关的Boss贴图不再统一取第一张未用精英图，也不再使用会聚集的相邻种子随机——新增 `themeNonBossRank()`，按每个主题内非生肖Boss关的顺序**均衡轮换** elite 池，并配置各主题起始偏移；若目标 elite 已被本关精英使用则顺延到下一张，仍保证不撞脸。这样同主题各贴图次数最多只相差约1（关卡自身精英避让时少量顺延）。②Boss立绘明显加大：`.boss-img` 由 76×76 → **100×100px**，`.boss-entity` 上移 -16px → -26px 让大立绘不压数值；普通怪/精英仍为 56×56，凸显Boss体型。
- **为什么**：用户反馈同主题下所有非Boss关的Boss都是同一张贴图（根因是 `free[0]` 恒取第一张）；并希望Boss贴图明显大一点以区分于普通怪。
- **后果（已验证）**：主题分布——set1各elite出现1~2次；set2出现1~2次；set3为12关/7张图，出现1~3次；set4出现1~2次；set5的L48/L49分别 elite6/elite1，不再相同。所有非Boss关Boss均避开本关已用精英；Boss立绘实测100×100、普通怪56×56；L35生肖Boss仍用 `boss_goat.svg`。脚本语法检查通过。
- **文件**：`生肖英雄_游戏本体.html`（`unusedEliteVariant`/`.boss-img`/`.boss-entity`）

### 【8.29·实体贴图按主题随机抽取 + 非Boss关Boss用未用精英贴图】（生肖英雄_游戏本体.html）
- **改了什么**：①普通怪/精英怪立绘不再固定 `mob1~3`/`elite1~2`，改为按当前主题 `entities` 目录的 `mob*/elite*` 数量动态生成名称池（新增 `THEME_MOB_COUNTS`/`THEME_ELITE_COUNTS` 配置各套图数量，`themeMobVariants`/`themeEliteVariants` 按 `THEME_DIR` 解析当前套图），对应主题的普通怪从 `mob*` 池稳定随机、精英怪从 `elite*` 池稳定随机（关卡id+格序号种子，重开不变，每格一种）。②非生肖Boss关（含教程1/2与普通关）的Boss格改用「本关精英格未使用过的一张 `elite*` 贴图」(`unusedEliteVariant()`)，避免Boss与场上精英撞脸；生肖Boss关与终局年兽(50)仍用各自生肖立绘。③删除旧 `MOB_VARIANTS`/`ELITE_VARIANTS`/`levelEliteVariant` 常量与函数。
- **为什么**：用户在各主题 `entities/` 补充了大量普通怪/精英怪贴图，希望对应主题随机抽取使用，并让非Boss关的Boss也复用主题内未用过的精英贴图，保持每关观感多样。
- **后果（已验证）**：L5(set1)普通怪从6张 mob* 随机、精英从7张 elite* 随机，Boss格使用本关未用过的elite；L30(set3)非Boss关Boss用未用精英贴图；L35(set3)生肖Boss关仍用羊Boss立绘。脚本语法检查通过。
- **文件**：`生肖英雄_游戏本体.html`（`themeMobVariants`/`themeEliteVariants`/`unusedEliteVariant`/`tileIconHTML` Boss分支）

### 【8.29·猪Boss失败不扣费 + 年兽贴图修复 + 终局通关祝贺弹窗】（生肖英雄_游戏本体.html）
- **改了什么**：①猪Boss(47)的500金币解锁费改为**挑战成功后扣除**——原先踏上Boss格即扣500，挑战失败也会扣；现改为踏上Boss格仅解锁（`pigUnlocked=true`，不扣金币），失败不扣，**仅 `endGame` 胜利分支扣除一次**（新增 `pigFeePaid` 防重复扣，开局/通关均重置）。②修复第50关年兽贴图不显示——L50 Boss格 `z:3`（数字图层号），原渲染代码把数字 `z` 当成生肖标识导致 `ART.bosses[3]` 为 undefined；现终局(50)强制解析为 `"nian"`，字符串 `z`（如 `"dog"`）仍优先，数字 `z` 或缺省按关卡生肖推导，年兽立绘解析到 `images/bosses/boss_nian.svg`。③通关年兽后显示专属祝贺弹窗：`🏮 祝贺你，你已成功通关！`，含感谢游玩与希望喜欢的文案，并提供「🎬 制作名单」按钮（复用设置页制作名单）与「返回选关」按钮。
- **为什么**：用户反馈挑战猪Boss失败不应损失金币；年兽贴图已存在但未显示（根因是数字图层号被误当生肖标识）；通关终局需要一个有仪式感的祝贺收尾。
- **后果（已验证）**：猪Boss解锁时金币保持500、失败后仍500、胜利后扣除至0且只扣一次；L50年兽立绘最终加载 `images/bosses/boss_nian.svg`（complete，无柴犬兜底）；通关年兽后弹窗显示祝贺文案+制作名单按钮。脚本语法检查通过。
- **文件**：`生肖英雄_游戏本体.html`（`tileIconHTML` Boss标识解析/`endGame` 猪Boss扣费/`sync` 终局祝贺弹窗）

### 【8.29·替罪羊贴图 + 沉睡格首现说明】（生肖英雄_游戏本体.html）
- **改了什么**：①第35关羊Boss的三个替罪羊分身敌人改用专属立绘贴图——`tileIconHTML` 在 `p.t==="mob" && p.scapegoat` 时优先走 `entityCands("scapegoat", art)`，经套图加载链解析到 `images/themes/set3/entities/scapegoat.svg`（缺图逐级回退基础 mob 贴图），三个替罪羊均生效。②确认第36关为首次出现沉睡格，关卡开始时弹出「💤 特殊格 · 沉睡格」说明；特殊格首次说明沿用 `sx_tileintro_v2` 记录，沉睡格说明只在首次遇到时显示一次。
- **为什么**：用户要求羊Boss关的替罪羊用专门贴图区分于普通小怪，并希望沉睡格在首次出现的关卡（36）开头就有说明。
- **后果（已验证）**：无头Chrome验证第35关三个替罪羊格的立绘 src 均为 `images/themes/set3/entities/scapegoat.svg`；第36关在陷阱/传送门/云雾已介绍的真实进度下，关卡开始弹「沉睡格」说明并记录 `sleep:true`。脚本语法检查通过。
- **文件**：`生肖英雄_游戏本体.html`（`tileIconHTML` 替罪羊分支）

### 【8.29·成就系统强化】顶部下落横幅 + 专属音效 + 全Boss专属成就（生肖英雄_游戏本体.html）
- **改了什么**：①成就解锁不再复用底部普通 `#toast`，新增独立 `#achievementToast`：固定在屏幕顶部，以 `achievement-drop` 从上方落下并轻微回弹，离场向上收回，层级110保证醒目；普通提示仍留在底部。②新增 `sfx.achievement()` 四段上行解锁音（523→659→784→1047/1319Hz + 轻微高频闪光噪声），首条成就延迟600ms播放，避免与通关音效重叠。③把原800ms定时覆盖改为真正的 `achQueue` 顺序队列：当前横幅完整显示2600ms并离场后才播下一条。④成就从6项扩展到21项：新增25关、1000金币、6符咒、全6武器里程碑，并为狗3/鼠7/牛11/虎15/兔19/龙23/蛇27/马31/羊35/猴39/鸡43/猪47/年兽50各添加专属通关成就；蛇与年兽沿用旧ID兼容已解锁数据。⑤Boss成就仅在本次通关对应关卡时弹出；读取旧存档时根据顺序进度静默补齐已通关Boss成就，避免更新后一次弹出大量历史横幅。⑥修复新建存档未清空成就数组及未结束成就队列的遗留问题，成就列表未解锁项恢复 `.locked` 灰显。
- **为什么**：用户反馈成就提示与普通提示同样从下方出现、不醒目，要求改为从上往下跳并配置独立音效；原队列间隔过短会互相覆盖，且成就数量不足，Boss通关缺少专属纪念。
- **后果（已验证）**：成就横幅位于 `top:14px`、动画为 `achievement-drop`，普通Toast仍在 `bottom:40px`；两项连续解锁时音效计数1→2且第二条在第一条完整离场后出现；13个Boss关卡映射为3/7/11/15/19/23/27/31/35/39/43/47/50，旧进度迁移不弹历史提示。无头Chrome回归及脚本语法检查通过。
- **文件**：`生肖英雄_游戏本体.html`

### 【8.29·设置系统 + 制作名单 + 文档入口 + 地图编辑器主菜单接入】（生肖英雄_游戏本体.html）
- **改了什么**：①主菜单「设置」热区正式接入设置弹窗，新增背景音乐与音效两个 0~100 音量滑块；`Music.vol` 与 `sfx.vol` 分别实时生效并写入 `ruth_musicvol`/`ruth_sfxvol`，修复音量为 0 时刷新被 `|| 默认值` 覆盖的问题，顶栏静音继续同时控制两类声音。②新增「制作名单」弹窗：玩法设计策划 mlw + Claude Sonnet 5（交流讨论）；代码 DeepSeek-V4-Flash / Flash Vision Exp（主）、GLM-5.2/GPT-5.6-SOL（辅）；美术 GPT-Image-2 / Text-to-Image；音乐 Lyria（Gemini）；名单改为职责标题与人员信息分行，并设置独立窄版安全区，防止英文长名称越出贴图。③设置页新增「设计思路文档」「迭代优化过程」按钮，打开同目录 `.docx`。④主菜单「地图编辑器」热区从“即将上线”改为直接跳转同目录 `地图编辑器.html`。
- **为什么**：用户需要可调音量和正式署名/设计材料入口，并要求独立编辑器可从游戏主入口直接访问，不再依赖手工找文件。
- **后果（已验证）**：背景音乐/音效独立调节和持久化正常；制作名单切换正常；两个 Word 文件存在且链接可触发；地图编辑器链接解析到同目录文件。无头 Chrome 功能测试与脚本语法检查通过。
- **文件**：`生肖英雄_游戏本体.html`、`地图编辑器.html`、`设计思路文档.docx`、`迭代优化过程.docx`

### 【8.29·特殊格/Boss说明补齐 + 弹窗贴图安全区与紧凑高度】（生肖英雄_游戏本体.html）
- **改了什么**：①特殊格首次说明存储键升级为 `sx_tileintro_v2`，修复旧版本“先写已读、后被 sync 关掉”造成陷阱/传送门/云雾永不再提示的脏状态；第13关陷阱、第16关传送门、第20关云雾会各显示一次说明。②补齐虎15（数值>Boss且≤2倍，杀过头失败）、兔19（孤立跳跃点+一次自由跳跃）、马31（行动≥30格+数值达标）Boss开关说明；龙23与蛇27按用户确认删除特殊效果说明，图鉴同步清除旧“踏云/全陷阱为特殊条件”文案，云雾和陷阱仅作主题地形。③弹窗文字统一深棕适配羊皮纸；先修为贴图安全区，后按反馈取消强制 400×640 高框，改为最大宽360px、内容自适应高度、长内容在78vh内滚动，标题/正文/按钮统一紧凑排版。
- **为什么**：旧弹窗状态污染导致玩家实际从未看到说明；部分 Boss 关缺机制预告；长文超出贴图后，强制按贴图全高又显得过高，需要兼顾版心与信息密度。
- **后果（已验证）**：13/16/20 特殊格标题分别正确显示；15/19/31 Boss说明显示，第23关与第27关不再弹龙/蛇Boss特殊说明；虎/兔/终局/制作名单等长文无子元素越界，短文弹窗不再被拉满至640px。
- **文件**：`生肖英雄_游戏本体.html`

### 【8.29·地图编辑器旧关卡灰显/不可点击兼容修复】（地图编辑器.html）
- **改了什么**：旧关卡通常省略 `z`，Boss 又可能用字符串 `z:"dog"/"rat"` 表示美术身份；编辑器却直接比较原始 `p.z`，把它们误判成其他图层并套用 `.other-layer{opacity:.35;pointer-events:none}`。现统一通过已有 `pz(p)` 将非数字 `z` 解释为逻辑第1层，覆盖 `cellMap` 索引、网格灰显、选择边框、邻接判断、删除与重复格校验，同时保留原始字符串 `z` 供导出和Boss美术使用；关卡库格数读取增加空数组保护。
- **为什么**：用户反馈以前关卡整体变灰且格子无法点击编辑；根因不是关卡锁定，而是多层地图升级后的旧数据兼容回归。
- **后果（已验证）**：无 `z` 旧格不再灰显，`pointer-events=auto`，可点击、选中、改属性；`z:"dog"` Boss可正常显示、查找和删除；真实数字层1/2/3逻辑保持不变。
- **文件**：`地图编辑器.html`

### 【8.29·游戏UI与规则修复合集】（生肖英雄_游戏本体.html）
- **改了什么**：①完全移除 F2/UI Tuner 功能及残留。②存档管理改两列，六卡统一标题/正文/按钮位置并去掉冗余“当前/进行中”字样；准备/商店卡改两列并提高描述/价格可读性；符咒图鉴按 `charm-cell.png` 上半图标名称解锁关、下半效果/Boss效果重排，移动端同步适配；弹窗浅色贴图文字改深色。③特殊格说明调用移到首轮 `sync()` 之后，避免一闪即关。④修复开局无骰也能免费走一步：死路免费掉头不再把起点格当成死路，起点移动必须先有行动点。⑤第19关兔Boss自由跳跃提示与各类提示流程回归。
- **为什么**：UI贴图替换后文字位置/颜色与美术版心不匹配；旧调试工具不应出现在发行版；起点被“死路免费返回”误判造成隐藏免费行动；特殊说明弹窗此前被同步流程关闭。
- **后果（已验证）**：发行界面更干净；存档/准备/符咒图鉴桌面与移动端布局稳定；开局行动点为0时移动被正确拦截；特殊说明弹窗可保持显示。游戏脚本 `node --check` 与相关浏览器回归通过。
- **文件**：`生肖英雄_游戏本体.html`


### 【8.22·card/modal贴图文字适配】物品卡与弹窗按米黄贴图版心重排字号/颜色/位置（生肖英雄_游戏本体.html）
- **改了什么**：针对 `images/UI/panel/card.png`（340×400）与 `images/UI/modal/modal-bg.png`（400×640）的米黄暖色版面，逐条精确调整文字排版（未再使用批量替换脚本）：①**modal-bg弹窗**——`.mbox` 改 400:640 比例、宽度 `min(88vw,400px)`、`padding:62px 42px 44px` 对齐贴图上/中/下版心，长内容 `max-height:86vh;overflow-y:auto`；标题 25px 深棕、正文 15px 深棕、强调字深棕，按钮 16px 暖白/金棕（主按钮保留金色）。②**panel/card物品卡**——`.pw/.pc` 改 340:400 比例、卡片内部 flex 纵向布局；图标区 54px，名称 16px 深棕、描述 13px 中棕、价格 13px 深棕，下一星效果深绿；底部价格用 `margin-top:auto` 固定在贴图底部文字区；选中态用内描边而非额外边框避免盖住贴图。③**小屏适配**——≤390px 时 modal padding/标题/正文与卡片名称/描述/价格同步缩小，保证手机可读、不溢出。
- **为什么**：用户完成 UI 全量换图后，`card.png` 与 `modal-bg.png` 的内容区为浅米黄，但原白字/浅灰字太小且对比不足、位置未落在贴图留白版心，导致武器/符咒卡与弹窗文字看不清。
- **后果**：物品卡名称/效果/价格按贴图上中下区域清晰排布；弹窗标题/正文/按钮按 400×640 版心落位，文字深棕高对比；长弹窗可滚动；手机端字号仍保持可读。验证：CSS 花括号 **478/478** 平衡，游戏脚本 `node --check` 通过；关键样式（modal 400:640、card 340:400、名称16px/描述13px/价格13px）确认存在。
- **文件**：`生肖英雄_游戏本体.html`（`.mbox`/`.mbox h2`/`.mbox p`/`.mbox button`/`.pw,.pc`/`.pw-icon,.pc-icon`/`.pw-name,.pc-name`/`.pw-desc,.pc-desc`/`.pw-price,.pc-price`/小屏 media）

### 【8.22·UI贴图替换全面修复】补绑定不生效贴图 + 新增界面卡文字协调（生肖英雄_游戏本体.html）
- **改了什么**：用户按 AI生成Prompt.md 完成 images/UI 全部贴图替换后，修复"很多没生效或文字不和谐"——①**补绑定**（有贴图但游戏未绑定→不显示）：`gameTitle` 补 `data-ui="topbar/titlebar"`（titlebar.png 生效）；`#announce` 补 `data-ui="modal/announce"` 且 `.abox` 背景改**透明**（露出红绸 announce-bg 贴图）、`#announce` 加**兜底深色背景**（无贴图时可读白字）；动态生成的存档/成就/符咒卡补绑定——`renderSaves` 末尾 `.save-card` 调 `uiBg(el,'panel','save-card')`、`renderAchievementList` 末尾 `.ach-item` 调 `uiBg('panel','ach-item')`、`renderCharmDex` 末尾 `.charm-cell` 调 `uiBg('panel','charm-cell')`。②**新增界面卡文字协调**（存档/成就/符咒卡贴图为米黄暖色，原米黄/金色文字看不清）：统一改**深棕系 + 白色微描边**（10处：sc-no/sc-prog/sc-empty/ach-n/ach-d/ach-st/chm-name/chm-lv/chm-eff/chm-boss/chm-state），标题深棕、正文中棕、状态深绿。③保留此前已调协调（状态卡文字靠右白金、控制按钮深棕、金币袋深棕）。
- **为什么**：用户完成所有 UI 贴图替换后，很多没生效（贴图有但元素未绑定，尤其顶栏标题/战斗横幅/动态新增的三类卡），或生效后文字颜色/大小与贴图不和谐（新增界面卡米黄贴图配米黄字看不清）。
- **后果**：顶栏标题/战斗横幅/存档/成就/符咒卡贴图全部生效；新增界面卡文字深棕协调；stat/controls/coinbag 文字保持协调。验证：游戏脚本 `node --check` OK；全面确认（save-card/ach-item/charm-cell uiBg、titlebar/announce data-ui、三类卡深棕文字、abox透明+announce兜底）全部通过。
- **文件**：`生肖英雄_游戏本体.html`（gameTitle data-ui/#announce data-ui+兜底/.abox透明/`renderSaves`/`renderAchievementList`/`renderCharmDex` uiBg/`.save-card`/`.ach-*`/`.chm-*` 文字深棕）

### 【8.22·玩法说明界面 + 特殊格首次弹窗】主菜单玩法说明 + 首次遇特殊格弹窗介绍（生肖英雄_游戏本体.html）
- **改了什么**：①**玩法说明界面**——新增 `screenHowto` + `renderHowto`，主菜单 b2「玩法说明」→`goHowto()`；内容分板块：目标·通关关卡 / 掷骰子·获得行动点（Space投掷、WASD/方向键/点地图移动、每格1行动点）/ 遇怪·比数值（普通怪显示数值、精英怪走近1格内才揭晓、Boss终点）/ 宝箱·未知奖励（数值/骰子/金币/诅咒）/ 符咒&武器（商店购武器可升星、符咒击败Boss解锁、出站前配3个、终局年兽关可带12）/ 按键操作（Space掷骰、WASD方向键移动、弹窗按1/2/3选选项、Space确认）/ 特殊格子说明提示。②**特殊格首次弹窗**——新增 `maybeShowTileIntro()`：`startLevel` 载入关卡后检查是否首次出现 trap/portal/sleep/cloud/clone 特殊格（localStorage `sx_tileintro` 记录已介绍过的类型，跨存档持久）；首次出现则该类格弹窗介绍其效果（陷阱格/传送门/沉睡格/云雾格/分身格各一段文案），之后不再重复弹；特殊格效果**不写入主菜单玩法说明**（只在玩法说明里提示"特殊格会在首次遇到时弹窗介绍"）。
- **为什么**：用户要求主菜单玩法说明补充基础玩法（投掷骰子获得行动点、WASD/鼠标走格、遇普通怪比数值获胜得数值、精英怪数值走近才显示、宝箱开数值/骰子/金币、符咒武器特殊效果、按键操作如Space投掷/WASD移动/1 2数字选弹窗选项）；而陷阱/传送门/沉睡/云雾等特殊格效果要求在**第一次出现该格的关卡开始时**弹窗介绍，**不写入主菜单玩法说明**。
- **后果**：主菜单「玩法说明」可查看完整基础玩法（含按键操作说明）；首次踏入含某特殊格的关卡时自动弹窗介绍该格效果（本地记录避免重复）。验证：游戏脚本 `node --check` OK；玩法说明+特殊格测试 22 项全过（界面/内容板块/按键说明/特殊格首次检测去重/本地记录）。
- **文件**：`生肖英雄_游戏本体.html`（`screenHowto`/`howtoList`/`renderHowto`/`goHowto`/`.howto-*` CSS/`TILE_INTRO`/`maybeShowTileIntro`/`startLevel` 调用/主菜单 b2 按钮）

### 【8.22·状态卡拆独立贴图】四张状态卡各自 data-ui + 强调色占位模板（生肖英雄_游戏本体.html + images/UI/stat）
- **改了什么**：状态卡从共用 `stat/card` 一张底板改为**每张独立贴图**——①HTML 四张卡 `data-ui` 改为 `stat/ap-card`（行动点绿）/`stat/dice-card`（剩余骰子黄）/`stat/dist-card`（距Boss红）/`stat/moves-card`（已行动蓝）；②`UI_ART.stat` 映射改为 `{ap:'ap-card',dice:'dice-card',dist:'dist-card',moves:'moves-card'}`（kebab-case 文件名）；③用 PIL 生成 4 张**强调色占位模板**（360×180，透明底+圆角描边+左侧强调条+名称尺寸）放 `images/UI/stat/`；④删除旧的共用 `stat/stat-card.png`。
- **为什么**：用户反馈三块状态板块（行动点/骰子/距Boss，含已行动共4张）需要各自的 UI 贴图，而非共用一张底板（各自强调色不同：绿/黄/红/蓝）。
- **后果**：每张状态卡可独立换皮（覆盖 `images/UI/stat/<名称>.png`）；缺图回退 CSS 卡外观。验证：游戏脚本 `node --check` OK；状态卡测试 13 项全过（四张 data-ui/UI_ART 映射/4张png/路径正确）。**注**：旧 `images/UI/stat/stat-card.svg`（228×93，用户前期放的共用版）保留未删，如需可改名复用或分别做。
- **文件**：`生肖英雄_游戏本体.html`（4张卡 `data-ui`/`UI_ART.stat`），`images/UI/stat/ap-card|dice-card|dist-card|moves-card.png`（新增4张）

### 【8.22·UI贴图生成Prompt重写】整体风格参考主界面UI，覆盖全部组件可喂AI出图（images/UI/AI生成Prompt.md）
- **改了什么**：重写 `images/UI/AI生成Prompt.md`——①**全局风格改为参考主界面UI**（低多边形3D卡通 + 暖金/米黄木质 + 鎏金描边 + 朱红点缀 + 微立体浮雕）；②覆盖**十类组件**逐段 Prompt：顶栏(back/mute/coinbag/titlebar)、4张独立状态卡(ap/dice/dist/moves)、控制按钮(map/roll/reset)、弹窗(modal-bg/toast/announce)、物品卡(card)、选关(lv-cell/lv-banner)、商店(shop-btn)、出战(slot/bigbtn)、小图标(coin/dice/flag/move)、**新增界面卡**(save-card/ach-item/charm-cell)；③每段含尺寸+画面内容+注意事项（透明底/不留边距/不写字/图形铺满）；④附「放置与生效」对照表 + 关于新增界面卡建议加 data-ui 的说明。
- **为什么**：用户要求生成所有需要 UI 组件贴图的 Prompt 提示词，整体美术风格参考主界面UI（低多边形卡通木牌鎏金暖色调）。
- **后果**：每类 UI 组件有独立可喂 AI 的提示词（含尺寸与画面细节）；用户逐张出图覆盖 `images/UI/` 即换皮；新增的存档/成就/符咒卡一并纳入。验证：文件完整（十类组件/含参考主界面风格/含 ap-card 与 charm-cell Prompt）。
- **文件**：`images/UI/AI生成Prompt.md`（重写）

### 【8.22·UI贴图占位模板生成】21张透明底描边模板，方便直接改图（images/UI/*）
- **改了什么**：为所有可替换 UI 组件逐张生成**占位模板图**（透明底 + 半透明淡填充 + 圆角描边 + 对角参考线 + 中央文字：部件名 + 建议尺寸），放到 `images/UI/<子目录>/` 对应位置共 21 张——topbar（back/mute/coinbag/titlebar）、stat（stat-card）、controls（btn-map/btn-roll/btn-reset）、modal（modal-bg/toast-bg/announce-bg）、panel（card）、level（lv-cell/lv-banner）、shop（shop-btn）、prep（slot/bigbtn）、icons（coin/dice/flag/move 64×64）。底图类=淡黄填充+名称尺寸，图标类=淡蓝填充+居中尺寸。
- **为什么**：用户想替换 UI 贴图，需要逐张占位模板（透明+描边+文字提示）放在对应目录，方便直接在上面改。
- **后果**：每张模板标注了与组件显示尺寸对应的**建议分辨率**（如 back 128×128、btn-roll 440×112、stat-card 360×180）；用户直接改图导出覆盖即可（改后需去掉中央提示文字）；缺图/占位图会自动回退或显示提示。用 PIL（透明 RGBA + rounded_rectangle 描边 + 文字）生成；验证 21/21 尺寸与透明符合。
- **文件**：`images/UI/topbar|stat|controls|modal|panel|level|shop|prep|icons/*.png`（新增21张）

### 【8.22·游戏界面单屏布局】状态/装备/棋盘/控制一屏显示，无需滚动（生肖英雄_游戏本体.html）
- **改了什么**：①`#screenGame.active` 改为 **`100vh; display:flex; flex-direction:column; overflow:hidden`**——整屏不滚动。②**HUD 紧凑化**：顶栏（选关/标题/音效/金币）48→40px、状态卡（行动点/骰子/距Boss/步数）padding/字号缩小、装备栏（武器+符咒）单行。③**棋盘 `#scene` 改 `flex:1` 自适应**（不再固定640px），`min-height:0` 防溢出。④**底部控制区固定单行**：地图/掷骰/重开 56px + 跳跃/龙雾紧凑。⑤**相机自适应缩放**：新增 `camZoom(sc)`——棋盘随 scene 高度缩放（`max(0.62,min(1.35,(h/640)*ZOOM))`，基线640→1.16），scene 缩小后棋盘仍完整居中；`camUpdate` 与 `screenToGrid`（点击棋盘反算坐标）共用 `camZoom`，保证棋盘缩放后点击格子仍准确。
- **为什么**：用户反馈原游戏界面各区块纵向堆叠（棋盘固定640px），手机竖屏需滚动才能看到状态/控制按钮；要求玩家在同一网页屏幕内看到所有必要信息（行动点/骰子/距Boss/装备栏/符咒栏/地图/掷骰/重开/跳跃/龙雾）而不需滑动。
- **后果**：`#screenGame` 一屏自适应；棋盘占剩余高度、相机自动缩放适配；点击棋盘坐标与渲染缩放一致不偏移。验证：游戏脚本 `node --check` OK；布局测试 16 项全过（100vh布局/HUD紧凑/scene自适应/camZoom随高度/两处缩放一致）。**注**：主文件实为 `生肖英雄_游戏本体.html`（此前开发中由 index.html 改名而来，非 index.html）。
- **文件**：`生肖英雄_游戏本体.html`（`#screenGame` CSS/`.stats`/`.loadout-bar`/`.controls`/`.special-controls`/`#scene`/`camZoom`/`camUpdate`/`screenToGrid`）

### 【8.22·sky.svg 尺寸与弹窗热键】天空尺寸建议 + 弹窗数字热键（生肖英雄_游戏本体.html）
- **改了什么**：①**sky.svg 尺寸确认**——`#skyImg` 用 `object-fit:fill` 拉伸铺满 `#scene`，宽高由 `fitSky()` 设为 scene 的 clientWidth/Height；**任意尺寸都会被拉伸铺满**，建议**正方形或 ≥1024×1024**（scene 最大宽 ~540px、高自适应）保证高清屏清晰。②**弹窗数字热键**——`keydown` 弹窗分支新增：按 **数字 1~9** 选 `.mbox` 里第 N 个按钮（`[...mbox.querySelectorAll('button')][idx].click()`），空格/回车仍点主按钮（`.pri`）；跳跃/龙雾不设按键。
- **为什么**：用户确认 sky.svg 所需大小；并要求给可点击的弹窗设热键（如牛符咒弹窗按1=第一行选项、按2=第二行），跳跃/龙雾除外。
- **后果**：弹窗可键盘选择（牛符咒/双生骰/幸运骰/牛Boss豪赌等均受益）；圆点棋盘缩放后点击坐标一致。验证：游戏脚本 `node --check` OK；热键测试 13 项全过（数字1/2选按钮/越界不触发/非数字不触发/空格回车保留/双生骰选择）。
- **文件**：`生肖英雄_游戏本体.html`（`#skyImg`/`fitSky`/keydown 弹窗分支）

### 【8.22·符咒图鉴功能】三列四行 + 贴图/效果/解锁关/Boss特殊效果（index.html）
- **改了什么**：①新增 `screenCharm` 符咒图鉴界面（主菜单 b5「符咒图鉴」→`goCharms()`），`charmdex` 用 `grid-template-columns:repeat(3,1fr)` **三列四行**（12符咒正好3×4）。②新增 `CHARM_DEX` 信息表：每符咒 `{unlockLv,bossFx}`——解锁关卡（狗3/鼠7/牛11/虎15/兔19/龙23/蛇27/马31/羊35/猴39/鸡43/猪47）与 Boss 关特殊效果（参考 docx 汇总）。③每格 `renderCharmDex` 展示：符咒贴图（`ART.charms[id]` + emoji 兜底）、符咒名、解锁关卡、「📜效果」「👹 Boss特殊效果」「✓已解锁/🔒未获得」；已解锁彩色、未解锁灰显。④图鉴 CSS `.charm-cell/.chm-*`。
- **为什么**：用户要求做符咒图鉴，三列四行，每格放符咒贴图+效果介绍+解锁关卡+Boss关特殊效果（参考 docx 的符咒/Boss系统设定）。
- **后果**：主菜单可查看12符咒图鉴，含贴图/效果/解锁关/Boss特殊效果/解锁状态；贴图放 `images/charms/<id>.svg` 即显示（缺图emoji兜底）。验证：`node --check` OK；图鉴测试 17 项全过（三列四行/12项/解锁关与BOSS_LV逐一对齐/Boss效果全覆盖/解锁状态）；walk/ach/saves 回归通过。
- **文件**：`index.html`（`screenCharm`/`charmDex`/`CHARM_DEX`/`renderCharmDex`/`goCharms`/`.charmdex`/`.charm-cell`/`.chm-*`）

### 【8.22·年兽Boss贴图接入】50关年兽立绘用 boss_nian.svg（images/bosses/boss_nian.svg）
- **改了什么**：确认年兽Boss贴图对应——`ART.bosses.nian='images/bosses/boss_nian.svg'`（用户放入 `images/bosses/boss_nian.svg`，911KB）；50关 boss 格在 `tileIconHTML` 走 `LEVEL.id===50?"nian"` 推导 z，候选链 `entities/boss_nian.svg → tiles/boss_nian.svg → ART.bosses.nian(svg) → boss_nian.png`，onerror 逐级回退命中 svg。
- **为什么**：用户把最终boss年兽贴图放入 `images/bosses/boss_nian.svg`，要求对应上。
- **后果**：50关年兽Boss显示自定义立绘；路径已配好无需改代码。验证：`node --check` OK；年兽+清空测试 18 项全过（ART.bosses.nian/文件存在/50关推导nian/候选链回退）。
- **文件**：`index.html`（`ART.bosses.nian`/`tileIconHTML` boss 分支），`images/bosses/boss_nian.svg`（用户新增）

### 【8.22·清空成就进度功能】成就界面一键重置为全新版本（index.html）
- **改了什么**：成就界面 `screenAch` 底部新增 **「🗑 清空成就与进度（全新版本）」** 按钮（`.sc-btn danger`）→ `resetAllProgress()`：清空所有进度键（`sx_unlocked/sx_coins/sx_weapons/sx_wstars/sx_charms/sx_loadout/sx_active_save/sx_bonusDice`）+ 全部存档槽 `sx_save_1~6`；重置内存进度为初始值（`unlocked=1,coins=0,ownedWeapons=[],weaponStars={},ownedCharms=[],achievements=[],loadout={w:null,c:[null,null,null]},activeSave=0`）；确认弹窗提示，清空后 toast+刷新成就列表。
- **为什么**：用户想增加清空成就进度的功能，以便发给别人时是全新版本（无成就/无进度/无存档）。
- **后果**：一键重置为全新初始状态（保留音效设置 `ruth_muted`）；发版前点一下即干净。验证：`node --check` OK；年兽+清空测试 18 项全过（清空按钮/清空所有存档槽/重置进度/成就空）。
- **文件**：`index.html`（`resetAllProgress`/成就界面清空按钮）

### 【8.22·通关弹窗符咒贴图】获得符咒弹窗用 charms 贴图替换 emoji（index.html）
- **改了什么**：通关弹窗（`status==="won"` 的「获得符咒」）里 `<div class="charm-gain-ico">${c.icon}</div>`（纯emoji）改为 `${iconHTML(ART.charms[newCharm],c.icon,'cico')}`——贴图优先（`images/charms/<id>.svg`），缺图才回退 emoji。
- **为什么**：用户反馈通关弹窗的符咒图标显示 emoji 而非 `images/charms/` 里的贴图；要求所有符咒 emoji 统一换成对应贴图。
- **后果**：通关获得符咒弹窗显示 charms 贴图；顺带排查确认装备栏/图鉴/顶栏符咒图标均已用 `iconHTML(ART.charms[id],icon)` 贴图优先（共4处）。验证：`node --check` OK；符咒图标测试 8 项全过（通关弹窗贴图/ART.charms路径/12贴图存在/无裸emoji渲染）。
- **文件**：`index.html`（`sync` 通关弹窗 charm-gain-ico）

### 【8.22·武器系统重做】全金币解锁 + 3把升星武器 + 幸运骰不限次 + 诅咒骰+3（index.html + editor.html）
- **改了什么**：①**取消关卡解锁**——移除所有 `unlockLv`，武器全金币购买，商店标题改"金币购买，无需解锁关卡"。②**3把升星武器**（腐朽之刃/贪婪镰刀/掠夺者匕首，1→3星）：购买=1星（100金）、升2星（150金）、升3星（250金），数值随星上升——刃：开局+50/80/120临时、每走1格-5/-4/-3；镰刀：每杀+5/8/12；匕首：击败精英+3/5/8行动；新增 `maxStars/starVals/stepCost/upPrices/starsDesc` 字段。③**不升星武器**：幸运骰/双生骰/诅咒骰 `maxStars:1`。④**幸运骰不限次**——去掉每关2次限制（`luckyUsesLeft` 弃用）。⑤**诅咒骰+3**——基础从+2改为+3，掷出6损失1骰不变。⑥**兼容旧存档**——`ownedWeapons`（已拥有id数组）+ `weaponStars`（{id:星级}）分开，存档 `sx_wstars` + 槽位 `wstars` 字段；`weaponStar()` 取当前星，`newSave` 重置 `weaponStars={}`。⑦编辑器 `EDITOR_WEAPONS` 文案同步。
- **为什么**：用户重做武器系统：不用等关卡通过解锁、全金币；3把近战武器可金币升星、数值随星上升；骰子类不升星；幸运骰不限次、诅咒骰基础+3。
- **后果**：武器购买/升星纯金币驱动；升星武器数值阶梯成长（1~3星）；幸运骰可无限重掷1~2；诅咒骰均值更高但掷6损骰风险；新存档武器不正确残留星级。验证：`node --check` OK（index+editor）；武器测试 34 项全过（定义/星级效果/诅咒+3/幸运不限次/存档）；walk/saves/ach 回归通过。
- **文件**：`index.html`（`WEAPONS`/`weaponStar`/`shopWeaponAction`/`renderShop`/`startLevel` blade·scythe·dagger 按星/`doRoll` 诅咒+3·幸运不限次/`prepCharm`/`saveToSlot`/`loadFromSlot`/`newSave`/`.pw-star`），`editor.html`（`EDITOR_WEAPONS`）

### 【8.22·common贴图接入】主角/分身/武器贴图 + portal/chest/clone 改为"主题normal+竖立贴图"（index.html + images/common/）
- **改了什么**：接入 `images/common/` 通用贴图——①**主角** `common/hero.svg`：`heroTokenHTML` 改用候选链 `HERO_CANDS=['images/common/hero.svg','images/chars/hero_front.png']`（chainImg 加载，缺则回退CSS角色）。②**分身** `common/avatar.svg`：分身格从"平面💠"改为**立绘**——`tile-entity` 分支 clone 用 `artChain(['entities/avatar.svg','tiles/avatar.svg'],['images/common/avatar.svg',ART.tiles.clone])`。③**武器** `common/weapons/*`：`ART.weapons` 基础图改 `images/common/weapons/<id>.svg`。④**portal/chest/clone 立绘化**：从 `FLAT_TYPES` 移除 portal/clone，`STANDING_TYPES` 加入 portal/clone——三者用**当前主题 normal 纹理打底 + 竖立贴图**（与怪物格一致），chest 原本已是竖立。
- **为什么**：用户提供 `images/common/` 通用贴图（hero.svg/avatar.svg/weapons/），并说明分身格/传送门/宝箱格=当前主题 normal 格 + 竖着立起的贴图（像怪物格）。
- **后果**：主角/分身/武器换成 common 贴图；portal/chest/clone 格呈"主题normal底 + 竖立立绘"，与怪物格视觉统一；缺图自动回退。验证：`node --check` OK；贴图接入测试 17 项全过（文件存在/候选链/FLAT_TYPES移除/STANDING_TYPES/纹理打底/avatar立绘）；walk/weapon 回归通过。
- **文件**：`index.html`（`HERO_CANDS`/`heroTokenHTML`/`ART.weapons`/`FLAT_TYPES`/`STANDING_TYPES`/`tile-icon` avatar/`artChain`），`images/common/`（hero.svg/avatar.svg/weapons/）

### 【8.22·武器图标放大+层叠修复】iconbox 26→48 + art-img盖emoji兜底 + 删emoji兜底（index.html）
- **改了什么**：①**图标放大**——`.iconbox.wico,.iconbox.cico` 从 26×26 放大到 **48×48**（武器大SVG不再被压小）。②**层叠修复**——`iconHTML` 的武器贴图 `art-img`（绝对定位）原本被 flex流中的 emoji 兜底 `fb` 居中叠住（小图标盖大图）；改 `ico-fb` 为绝对定位 `z-index:0` 底层、`art-img` `z-index:1` 顶层，贴图盖住 emoji。③**删 emoji 兜底**——用户要求去掉，`iconHTML` 不再渲染 `fb`（移除 `ico-fb` 元素与 CSS），无图时 iconbox 空白。
- **为什么**：用户反馈武器图标太小、且"原小图标占位"（emoji兜底叠在贴图上）。
- **后果**：武器/符咒图标 48×48 清晰缩放；贴图不再被 emoji 小图标遮挡；无 emoji 兜底，缺图空白。验证：`node --check` OK；图标测试 8 项全过（层叠层级/有/无src行为/48px）。
- **文件**：`index.html`（`iconHTML`/`.iconbox .art-img`/删`.ico-fb`/`.iconbox.wico,.iconbox.cico`）

### 【8.22·三个bug修复】50关符咒跨关 + 50关通关进度 + 新存档武器星级残留（index.html）
- **改了什么**：①**50关符咒跨关**——玩家在50关装满12符咒后 `loadout.c` 残留12元素，切其它关装备界面显示全部；`renderPrep` 开头对非50关将 `loadout.c` 截断到前3并保存，杜绝12符咒带入其它关；`startLevel` 维持"50关取全部/其它关取前3"。②**50关通关进度**——`endGame` 里 `if(LEVEL.id===unlocked&&unlocked<50)` 在通关50关（unlocked=50）时不增长，导致永久停在49/50；改 `<=50`，通关50后 `unlocked=51`，`Math.min(unlocked-1,50)>=50` 显示50/50，`beatNian` 成就随 `unlocked>50` 正确触发。③**新存档武器星级残留**——`newSave` 未重置全局 `weaponStars`，新存档武器显示旧三星；`newSave` 加 `weaponStars={}`，且 `renderShop` 未拥有武器按1星显示（`const star=own?weaponStar(id):1`）、不显示升星预览与星标、描述用"购买后 ⭐1"。
- **为什么**：用户反馈第50关符咒可带去其它关使用、50关通关后进度仍49/50、新存档武器变三星。
- **后果**：非50关固定3符咒槽（50关专用满符咒不进其它关）；50关通关后进度 50/50 正确；新存档武器一律1星，购买前不显示旧星级。验证：`node --check` OK；bug修复测试 12 项全过（非50关截断12→3、50关保留12、charms取数、通关50→unlocked=51、显示50/50）；walk/weapon2/saves 回归通过。
- **文件**：`index.html`（`renderPrep` 截断/`startLevel`/`endGame` `<=50`/`newSave` 重置/`renderShop` 未拥有按1星）

### 【8.22·成就系统框架】数据驱动成就表 + 事件触发检查 + 解锁弹窗 + 成就界面（index.html）
- **改了什么**：搭建成就系统框架——①`ACHIEVEMENTS` 数据驱动成就表（示例：初出茅庐/小有所成/小富翁/符咒大师/降伏灵蛇/终结年兽，用户后续可增删改，每个 `{id,name,icon,desc,test(state)}`）；②`checkAchievements(ctx)` 检查所有未解锁成就、达成即解锁（去重、每存档独立）；③`showAchievementToast` 居中横幅弹窗（🏆 成就达成）+ `unlockAndToast` 逐个排队播放；④解锁列表并入存档（`saveToSlot` 存 `ach`、`loadFromSlot` 读 `ach`，每槽独立）；⑤新增 `screenAch` 成就界面 + `renderAchievementList`（图标/名称/描述/已解锁未解锁徽章），主菜单「成就」按钮从"即将上线"改为 `goAchievements()`；⑥`endGame` 胜利后调用 `checkAchievements` 并弹窗。`state` 上下文提供 `wins/coins/charms/beatSnake/beatNian/lastLevel`。
- **为什么**：用户要求搭建成就系统框架，后续再补具体成就；要求"做到某件事时触发成就弹窗并解锁"。
- **后果**：成就框架就位——往 `ACHIEVEMENTS` 加条目即自动生效（test 返回 true 达成）；通关/金币/符咒/击败Boss等触发弹窗解锁并随存档保存；成就界面展示已/未解锁状态。验证：`node --check` OK；成就测试 22 项全过（框架定义/存档持久化/解锁去重/各示例成就/无ctx不崩）；walk/saves/music 回归通过。
- **文件**：`index.html`（`ACHIEVEMENTS`/`achievements`/`checkAchievements`/`achievementUnlocked`/`showAchievementToast`/`unlockAndToast`/`renderAchievementList`/`goAchievements`/`screenAch`/`endGame` 检查/`saveToSlot`/`loadFromSlot`/`.ach-*`）

### 【8.22·音乐系统】main菜单/关卡分主题循环背景音乐（index.html + audio/）
- **改了什么**：新增背景音乐系统——①建 `audio/` 目录 + `README.md` 规范：`menu.*` 主界面音乐、`theme1~5.*` 5种地图主题（与 THEME_SETS 分段一致：theme1=1~11关、theme2=12~23、theme3=24~39、theme4=40~47、theme5=48~50）；②`Music` 管理器：`play(key)` 循环播放 `audio/<key>.mp3/.ogg/.wav`、缺文件静默、音量0.5、`stop()`；③`show(id)` 非游戏界面播 `menu`、`setArtTheme(id)` 按主题分段播 `theme<idx>`；④与静音按钮联动（`sfx.toggle` 同控音乐音量）；⑤首次指针/键盘交互解锁自动播放限制（浏览器要求）。支持 `.mp3/.ogg/.wav`，缺文件自动静音不破坏。
- **为什么**：用户想加入音乐系统，已准备 5 种地图主题音乐 + 1 个主界面音乐，要求准备好文件夹并接入。
- **后果**：主界面/选关/存档播主界面乐，进入关卡按主题分段播对应主题乐，循环、可静音、缺文件静音；用户只需把 6 个音乐文件按 `audio/README.md` 命名放进 `audio/`。验证：`node --check` OK；存档+音乐测试 28 项全过（6槽/全新存档/音乐播放/主题分段/静音联动/首次交互解锁）。
- **文件**：`index.html`（`Music`/`show`/`setArtTheme`/`sfx.toggle` 静音联动/自动播放解锁），`audio/`（新目录 + README.md）

### 【8.22·存档系统调整】6槽位两行三列 + 全新存档（index.html）
- **改了什么**：①槽位数 `SAVE_SLOTS` 从 3 → **6**，存档界面改为**两行三列**网格（`grid-template-columns:repeat(3,1fr)`），卡片/按钮字号微调适配3列布局；②`newSave(slot)` 改为**创建全新存档**——进度重置为初始值（`unlocked=1,coins=0,ownedWeapons=[],ownedCharms=[],loadout={w:null,c:[null,null,null]},bonusDice=0`），不再复制当前在玩进度，新建后从第1关重新开始。
- **为什么**：用户反馈存档系统无法增加一个全新存档（原新建会复制当前进度），且希望两行三列布
局。
- **后果**：6 槽位两行三列展示；新建=干净新进度从第1关开始；读取/删除/槽位隔离不变。验证：`node --check` OK；存档+音乐测试 28 项全过（6槽、全新存档重置、两行三列、音乐）。
- **文件**：`index.html`（`SAVE_SLOTS`/`newSave`/`.saves-grid`/`.save-card`/`.sc-btn`）

### 【8.22·pad贴图美化】棋盘底 pad 支持顶面贴图，缺图回退 CSS 渐变（index.html + images/ground.png）
- **改了什么**：①新增 `ground-img` 元素铺在 `.ground`（棋盘底 pad）上，`build3` 渲染，负责 pad 顶面观感；②新增 `ART.ground` 槽位（`images/ground.png`，套图 `ground.png/svg` 优先）；③`applyArt` 用 `artChain(['ground.png','ground.svg'],ART.ground)` 加载，成功 `display:block`，失败自动隐藏（保留原 CSS 渐变兜底）；④CSS `ground-img` 带 `border-radius:36px`（匹配 ground 圆角）+ inset 阴影（保留边缘立体感），`z-index:0` 在 ground 之上、tiles 之下。⑤生成默认贴图 `images/ground.png`（520×520 暖沙色渐变 + 细砂颗粒 + 少量浅石）。
- **为什么**：用户反馈棋盘下的 pad 与主题（沙地背景）格格不入，希望用贴图美化、与主题协调。
- **后果**：棋盘底 pad 从深蓝灰纯 CSS 渐变改为贴图的沙地质感，与背景沙地场景协调；浮岛厚度（box-shadow 堆叠）保留仍立体；换皮只需在套图目录放 `ground.png/svg`。验证：`node --check` OK；ground 美化测试 9 项全过（槽位/渲染/CSS/加载/回退/贴图存在）；walk 回归通过。
- **文件**：`index.html`（`ART.ground`/`build3`/`applyArt`/`.ground-img`），`images/ground.png`（新增）

### 【8.22·多存档系统】主界面"开始游戏"进入存档管理界面，可新建/读取/删除存档（index.html）
- **改了什么**：①**主界面换新图**——主菜单图由旧 `UI_cut.png` 换成 `images/UI/主界面UI.png`（1024×1536 竖版标题+6按钮），热区重测为 6 个按钮（开始游戏/玩法说明/成就/地图编辑器/符咒图鉴/设置），`width:98vw;max-width:65vh;aspect-ratio:1024/1536`，`left:16%;width:69%`，b1~b6 按测量百分比定位（top 38.4%~88.9%）；目前只开放"开始游戏"→`goSaves()`，其余功能 toast"即将上线"。②**3 个固定存档槽位**——`SAVE_SLOTS=3`，每槽 `sx_save_<n>` 存 `{unlocked,coins,weapons,charms,loadout,bonusDice}`，`sx_active_save` 记录当前激活槽；新增 `saveToSlot/loadFromSlot/deleteSlot/slotSummary`；`saveProg` 有激活槽时写该槽，无槽时写旧键（兼容旧进度）。③**存档界面**——新增 `screenSaves`（高亮当前存档、进度摘要：通关 X/50·💰金币·符咒·武器；空槽显示"＋新建存档"；非空槽显示"读取/删除"按钮），`renderSaves/newSave/readSave/removeSave`；主菜单开始游戏→`goSaves()`。
- **为什么**：用户要求开始游戏后进入存档界面，可在界面新建/读取/删除存档；且想更新主界面 UI 素材为新的 `主界面UI.png`，点击区域跳转功能、暂只开开始游戏。
- **后果**：主界面换新竖版 UI（6 个按钮热区精确对准），开始游戏→存档管理；3 槽位互不干扰，新建存当前进度、读取载入进度、删除确认后清除（删当前激活槽回无存档态）；旧进度自动兼容。验证：`node --check` OK；存档系统测试 18 项全过（新建/读取/删除、槽位隔离、saveProg 写激活槽、无槽写旧键、空槽读取失败）；walk 回归通过。
- **文件**：`index.html`（`screenSaves`/`.saves-*`/`.save-card`/主菜单热区 b1~b6/`SAVE_SLOTS`/`saveToSlot`/`loadFromSlot`/`deleteSlot`/`slotSummary`/`saveProg`/`goSaves`/`renderSaves`/`newSave`/`readSave`/`removeSave`）

### 【8.22·马Boss行动进度计数器】马Boss关(31)"已行动"改为「X/30」目标进度，达30格变绿（index.html）
- **改了什么**：`sync` 里马Boss关(`bossZodiac==="马"`)的 `#statSteps` 从纯数字改为 `totalSteps + " / 30"`，卡片标题由「已行动」改为「行动进度」；`totalSteps>=30` 时数字加 `.done` 类（绿色高亮，新增 `.s-moves .v.done` 与 `.s-moves .v` 蓝色配色）。非马Boss关保持原「已行动 N」。
- **为什么**：用户确认马Boss胜利条件=总行动≥30格（此前 genLevel `id===31 P≥30` 与 Boss 分支 `totalSteps>=30` 早已写入），但原计数器只显示累计步数、不显示 30 格目标，玩家无法直观看到还差多少。
- **后果**：马Boss关开局即可看到「行动进度 X/30」，达到 30 格变绿提示满足条件，胜利条件可视化。验证：`node --check` OK；马Boss分支 `cv>=need&&totalSteps>=30` 在、`id===31 P≥30` 在。
- **文件**：`index.html`（`sync` 马Boss分支/`.s-moves` 样式）



### 【8.22·马符咒修正】马符咒改"本关第一个与最后一个骰子点数×2"，并修复首骰从未生效的bug（index.html + editor.html + docx）
- **改了什么**：①**规则修正**——马符咒从「仅第一个骰子 ×2」改为「**本关第一个骰子 与 最后一个骰子** 的点数转化为 2 倍行动点」：新增 `horseLast` 状态，`roll()` 在掷骰前若 `diceLeft===1`（最后一颗）则置 `horseLast=true`；`animateRoll` 用 `horseDouble=hasCharm("horse")&&(horseFirst||horseLast)` 判定 ×2，结算后同时清 `horseFirst/horseLast`。②**顺带修复既有 bug**——原代码 `if(horseFirst)horseFirst=false;` 在读取 flag **之前**就清掉它，导致马符咒首骰 ×2 从未真正生效；改为先判定后清理。③文档/编辑器同步：`CHARMS.horse`、`EDITOR_CHARMS.horse`、编辑器装备提示、docx 全部改为「第一个与最后一个骰子×2」；editor `simulatePath` 马符咒估算从 +1 骰改为 +2 骰（首末两骰加倍）。
- **为什么**：用户指出马符咒效果是"每关第一个骰子和最后一个骰子所投掷点数转化为 2 倍行动点，而不是只有第一个骰子"。
- **后果**：带马符咒时本关首掷与末掷（掷前仅剩1颗骰）均 ×2 行动点，中间骰正常；首骰 bug 一并修复。验证：`node --check` OK（index+editor）；逻辑测试 11 项全过（首骰×2/中间不×2/末骰×2/flag 清理/不再先清后读）。
- **文件**：`index.html`（`roll`/`animateRoll`/`horseLast`/`CHARMS.horse`），`editor.html`（`EDITOR_CHARMS.horse`/`simulatePath`/装备提示），`docx/宝箱boss武器和符咒系统.md`



### 【8.22·新增4个Boss关与符咒】羊/猴/鸡/猪 Boss 胜利条件 + 4 符咒 + 沉睡格/分身格/替罪羊地形（index.html + editor.html + docx）
- **改了什么**：补齐最后 4 个生肖 Boss 与符咒，完成 12 生肖全接入——①**羊(35)**：genLevel 在 Boss 前放 3 个「替罪羊分身」（mob+`scapegoat:true`），全部击败后（`scapegoatsDefeated>=3`）才能挑战羊Boss；**羊符咒**=本局首次对普通/精英战斗失败时弹窗，可用替罪羊承担失败、吸收敌人 20% 数值继续前进（每局一次，Boss 无效）。②**猴(39)**：路径中途放「分身格」，到达融合（`cloneMerged`）后才能挑战猴Boss；**猴符咒**=任意关自动在路径格生成分身（`cloneIdx`），主角每次获得数值时分身同步 +80%（`cloneSync` 挂在增益/击败/宝箱/牛符咒/豪赌/沉睡全部数值获得点），到达分身格融合吸收。③**鸡(43)**：放 6 个「沉睡格」，经过不立即获得收益、集满 3 个同时苏醒（`sleepPending`/`sleepWoken`），唤醒 6 个（2组）才能挑战鸡Boss；**鸡符咒**=每经过一个沉睡格立即苏醒并获得 120% 收益。④**猪(47)**：放金币格供凑 500 金币，消耗 500 金币解锁 Boss 格（`pigUnlocked`）后才能挑战；**猪符咒**=每经过一格概率获得额外奖励（20%+1骰/10%+2骰/5%+3骰；30%+10金/20%+20金/10%+30金，`pigFarmRoll`）。⑤渲染：新增 `clone` 格子类型（3D 💠+积累值、2D 💠、mcell 配色）、`sleep` 3D 样式补全（`.t-sleep`/`.sleep-wrap`/苏醒 `.awake`）、FLAT_TYPES 补 `clone`、ART.tiles 补 clone。⑥编辑器：TYPES/工具栏按钮/属性面板（沉睡格收益、分身格提示、普通怪勾「替罪羊分身」）全部补齐，`EDITOR_CHARMS` 4 符咒描述更新，`simulatePath` 支持沉睡/分身/羊/鸡/猪/猴结算（马估算同步改），导出含 `scapegoat/sleep/clone`。⑦docx：Boss 与符咒章节补 9-12 号、特殊地形补沉睡格/分身格/替罪羊。
- **为什么**：用户要求把剩下 4 个 Boss 关与符咒效果更新到 docx，并在游戏本体与编辑器实现。
- **后果**：12 生肖 Boss 全部可玩（35/39/43/47 生成关即玩，genLevel 分别放替罪羊/分身格/沉睡格/金币格）；4 个新符咒的规则、UI 弹窗、Boss 前置条件弹窗、3D/2D 渲染、编辑器编辑能力全部就位。验证：`node --check` OK（index+editor）；新机制逻辑测试 39 项全过（4 符咒效果/4 Boss 前置/渲染/状态变量）；虎/兔/龙/云/牛/持久化回归全过（3 个 FAIL 均为过时旧测试的过期断言，非回归）。**注**：用户口述"鸡=第四十四关"，但代码 BOSS_LV 固定 鸡@43（35/39/43/47 每4关一个），按 43 实现并写入文档。
- **文件**：`index.html`（CHARMS 4 项/状态变量/startLevel 重置+猴分身生成/genLevel 4 关适配/landOn 沉睡·分身·替罪羊·猪符咒/羊符咒弹窗/Boss 前置 4 项/`cloneSync`/`pigFarmRoll`/`offerGoatCharm`/渲染/4 个 Boss 弹窗），`editor.html`（TYPES/按钮/属性/EDITOR_CHARMS/simulatePath/导出），`docx/宝箱boss武器和符咒系统.md`



### 【8.22·云雾格持久化修复】startLevel 深拷贝 lv，铺云不再污染原始关卡（重进地图干净）（index.html）
- **改了什么**：`startLevel` 开头把传入的关卡对象**深拷贝**（`lv=JSON.parse(JSON.stringify(lv))`）再 `normalizeLevel`。根因：`prepStart` 从 `LEVELS.find(...)` 拿到的是**关卡库同一引用**，而 `normalizeLevel` 原地修改并返回同一对象，龙符咒铺云的 `addCloudCell` 的 `LEVEL.path.push` 直接污染了 `LEVELS` 里的原始关卡——重进同一关时之前铺的云雾格还在。
- **为什么**：用户反馈"27关使用云雾格后通关失败，下次再进入同一地图云雾格仍然存在"。
- **后果**：铺云/连杀等一切对 `LEVEL.path` 的改动只作用于副本，重进地图恢复原样。验证：`node --check` OK；8 项测试全过（铺云后当前关卡 5 格、原关卡仍 4 格无云、二次进入干净、normalizeLevel 不污染原 cells）。
- **文件**：`index.html`（`startLevel`）



### 【8.22·回起点陷阱永不失效加固】landOn 开头 doneSet 拦截也豁免回起点陷阱，双保险反复触发（index.html）
- **改了什么**：在 8.22 上一轮「回起点陷阱不加入 doneSet」基础上再加一道保险——`landOn` 开头的"已消耗格拦截"条件改为 `if(p.t!=="boss"&&doneSet.has(idx)&&!(p.t==="trap"&&p.eff==="start"))return;`：**即使回起点陷阱意外进了 doneSet 也绝不拦截**，反复踩、反复回起点。
- **为什么**：用户反馈修复后 24 关回起点陷阱仍"踩一次就失效"（上一轮排查确认 trap 分支代码正确、逻辑测试通过，为彻底排除任何意外路径，故在入口拦截层加同款豁免）。
- **后果**：回起点陷阱（`eff==="start"`）在任何情况下都反复触发；数值/骰子陷阱仍一次性。验证：`node --check` OK；9 项测试全过（不入 doneSet、入口不拦截即使 doneSet 有、反复触发；数值陷阱仍一次性）。**提示**：若仍感觉失效，请强刷 Ctrl+F5 并确认非 editor 旧缓存写回。
- **文件**：`index.html`（`landOn`）



### 【8.22·回起点陷阱反复触发】回起点类陷阱改为可反复触发（index.html）
- **改了什么**：`landOn` 的 trap 分支由统一 `doneSet.add(idx)` 改为 `if(p.eff!=="start")doneSet.add(idx)`：**回起点类陷阱（`eff==="start"`，如 24 关）不加入 doneSet，反复触碰反复回起点**；数值/骰子陷阱保持一次性（入 doneSet）。蛇符咒免疫逻辑保留（回起点陷阱被蛇免疫一次后下次仍会触发）。
- **为什么**：用户要求"类似 24 关的回起点类陷阱不是一次性的，反复触碰就反复回到起点，强迫玩家思考其它道路"。
- **后果**：24 关回起点陷阱踩一次回起点、再踩仍触发；数值/骰子陷阱不变。验证：`node --check` OK；7 项测试全过（回起点不入 doneSet/反复触发/数值陷阱一次性）。
- **文件**：`index.html`（`landOn` trap 分支）
- **改了什么**：龙符咒铺云从"把已有格子转成云"改为"**在任意空位新建可走的云雾格**"：①新增 `screenToGrid(e)`——把 3D 场景点击点逆变换反算成棋盘网格坐标（逆平移→逆缩放→逆 rotateX→逆 rotateZ→加 cur），支持在空位定位；②`sceneClick` 在 cloudMode 时用 `screenToGrid` 得 (c,r) 调 `placeCloudAt(c,r)`；③`placeCloudAt(c,r)` 改为坐标版——已有可铺格则转云，**空位则 `addCloudCell(c,r)` 新建**（push 进 LEVEL.path + 渲染 `.tile t-cloud` 到 #cam + `buildAdjacency`）；④新建云格须与"玩家当前可达的任一格"曼哈顿=1（保证铺上去能连上网络、可当桥）；`tryMoveTo` 的 2D 地图铺云同步改为坐标调用。
- **为什么**：用户要求龙符咒能在"没有格子路径的地方放置云雾格"，从而走过云雾到达目的地（桥接断开的路径），而非只能把已有格转云。
- **后果**：带龙符咒时点 3D 场景空位可新建云雾格，连上断开的路径后走过去（云雾格仍"离开才消失"）；2D 地图铺云兼容。验证：`node --check` OK；铺云新建/邻接连通/start→boss 连通/screenToGrid 逆变换自洽（4 朝向）12 项全过；walk/邻接/art 回归全过。
- **文件**：`index.html`（`screenToGrid`/`addCloudCell`/`placeCloudAt`/`sceneClick`/`tryMoveTo`）




### 【8.22·50关多层地图】编辑器支持3层编辑 + 跨层传送门；游戏端每次只渲染当前层（index.html + editor.html）
- **改了什么**：为第50关年兽Boss实现"同关3层地图"——①**编辑器**：工具栏新增「层1/层2/层3」选择器（`E.z` 当前层），`key/cellMap/getCellAt` 按 `(z,c,r)` 区分；每层格子坐标独立、同层内邻接才判定相邻（非当前层格子灰显 `other-layer` 不可点）；传送门属性面板新增「目标层」下拉 + 目标坐标，`link.z` 标记跨层目标；导出 `cellToExport/exp` 带 `z` 与 `link.z`，导入保留。②**游戏**：新增 `curZ` 当前层变量，`build3`/`buildMap` 只渲染当前层格子；进入传送门若目标层不同则 `build3()+buildMap()` 重建渲染该层并 toast「层X→层Y」；`buildAdjacency` 物理相邻仅同层（z 相同），传送门按 `link.z`+坐标跨层配对；`normalizeLevel` 检测多层（存在>1种 z）时禁用线性 next 首尾相连（层间只靠传送门）；`pathEdges` 只画当前层连线。
- **为什么**：用户希望第50关做成同一关3层地图、只有最后一层（层3）才有年兽Boss、层间用传送门连接——编辑器需支持分层编辑，游戏端需分层渲染与跨层传送。
- **后果**：编辑器可逐层画3个独立棋盘并跨层连传送门（层1起点→层2→层3年兽Boss）；游戏内逐层探索，进跨层传送门才切换显示下一层，视觉上"一层一层前进"。验证：`node --check` OK（index+editor）；分层测试31项全过（编辑器分层UI/导出带z、同层相邻、跨层传送门配对、start→boss 经传送门全可达、build3 只渲染当前层）；walk/adjacency/cloud/clone 回归通过。
- **文件**：`editor.html`（层选择器/`key`/`getCellAt`/`handlePath`/`renderGrid`/传送门目标层/`cellToExport`/`buildAdj`/`validateLevel`），`index.html`（`curZ`/`build3`/`buildMap`/`buildAdjacency`/`pathEdges`/`normalizeLevel`/传送门跨层）


### 【8.22·分身格显示规则】分身格未生效(无符咒且非猴Boss关)时还原为普通格,地图不显示分身（index.html）
- **改了什么**：`startLevel` 在分身格未生效（`cloneIdx<0` 且非猴Boss关，即编辑器放了 clone 格但玩家没带猴符咒）时，把 `LEVEL.path` 里的 clone 格还原为 `t="normal"`（删 `isClone`），地图上不再显示 💠。
- **为什么**：用户要求"设置分身格但没有携带符咒的关卡，不生成分身格"——此前 cloneIdx=-1 但格子仍渲染 💠 图标，造成"没符咒却出现分身"的错觉。
- **后果**：无符咒且非猴Boss关时，编辑器设置的 clone 格被还原成普通格（不显示、不生成、不积累）；39关和带符咒场景不受影响。验证：`node --check` OK；规则测试7项全过（含 clone 格被还原为 normal、39关保留）。
- **文件**：`index.html`（`startLevel`）


### 【8.22·分身格三规则】分身格生效规则:①39猴Boss关自带无符咒也生效 ②带符咒其它关自动生成 ③非猴Boss关无符咒不生成（index.html）
- **改了什么**：`startLevel` 分身格判定改为三条规则——①`manualIdx>=0 && (isMonkeyBoss||hasCharm("monkey"))` 时用关卡自带 clone 格（猴Boss关39无条件、带符咒时优先）；②`hasCharm("monkey")` 且无 clone 格时运行时自动挑一个（路径中段、可步行、非 start/boss/mob/elite/chest 的普通格）；③否则 `cloneIdx=-1`（不生成、不积累）。同时 `cloneSync` 去掉 `hasCharm("monkey")` 守卫，改为只依赖 `cloneIdx>=0`——关卡自带分身格（39关）无论带不带符咒都实时 +80% 同步、融合有收益。
- **为什么**：用户反馈"分身格收获0点数值"——根因是 `cloneSync` 有 `!hasCharm("monkey")` 守卫，而 39 关分身格是关卡自带机制，玩家不带符咒进入时 `cloneVal` 恒为0、融合收益0。且需明确"无分身格、无符咒的关不生成"。
- **后果**：39关自带分身格无符咒也同步+融合有收益；带符咒刷其它关自动生成；无分身格无符咒不生成。验证：`node --check` OK；三条规则测试5项 + 策略/同步回归全过（①39无符咒生效 ②带符咒自动生成 ③无clone无符咒-1）。
- **文件**：`index.html`（`startLevel`/`cloneSync`）


### 【8.22·分身同步完整性】补齐沉睡格/羊符咒/牛符咒获胜三处漏掉的 cloneSync（index.html）
- **改了什么**：给 3 处获得数值但漏调 `cloneSync` 的地方补上分身同步——①沉睡格数值收益（`else{val+=amt;...cloneSync(amt);}`）②羊符咒替罪羊吸收 20%（`cloneSync(g)`）③牛符咒掷骰获胜（`cloneSync(g)`）。陷阱扣数值**不**同步（分身只增不减）。
- **为什么**：用户反馈"分身格收获0点数值"——部分数值获得来源未同步给分身，导致 `cloneVal` 偏低/为0。
- **后果**：所有获得数值的来源（gain格/怪/精英/虎连杀/沉睡/牛Boss豪赌/宝箱/羊/牛符咒）都实时同步分身 80%。验证：`node --check` OK；10 项测试全过（各获得点有 cloneSync、陷阱不同步）。
- **文件**：`index.html`（`landOn` 沉睡/羊符咒/牛符咒）


### 【8.22·兔跃斜跳】兔符咒可跳"正前方一格往左一格"等斜前方1格落点（index.html）
- **改了什么**：`JUMPS` 从 8 个半径2落点（轴向2格 + 对角2格）扩展到 **12 个**，新增 4 个对角1格 `[-1,-1],[-1,1],[1,-1],[1,1]`——曼哈顿距离=2 的斜前方1格（如"正前方一格往左一格"），此前无法跳。
- **为什么**：用户反馈兔符咒不能跳到"正前方一格往左一格"的格子。
- **后果**：兔符咒可跳斜前方1格（±1,±1）；原有半径2落点保留；轴向1格/原地/3格仍不可跳。验证：`node --check` OK；17 项测试全过（斜前1格4方向可跳、原落点保留、非落点拒绝、总数12）；genLevel 兔Boss落点候选同步受益。
- **文件**：`index.html`（`JUMPS`）


### 【8.22·鸡符咒改版】沉睡格仍需集满3个才苏醒,但苏醒收益×2(200%)（index.html + editor.html + docx）
- **改了什么**：鸡符咒从「每经过一个沉睡格立即苏醒并获得120%收益」改为「**仍然需要经过 3 个沉睡格才能唤醒，但唤醒后获得 200%（×2）沉睡格的收益**」——`landOn` 沉睡分支移除"立即苏醒"，`sleepPending` 照常累积，集满3个苏醒时用 `roosterBonus=hasCharm("rooster")?2:1` 结算；`CHARMS.rooster`、`EDITOR_CHARMS.rooster`、编辑器 `simulatePath`（×2）、docx 同步更新。
- **为什么**：用户觉得原鸡符咒效果（免集齐立即苏醒）不太行，改为保留沉睡格盲盒节奏、用 200% 收益作为携带价值。
- **后果**：带鸡符咒仍需攒3个沉睡格才苏醒，但苏醒时每个收益×2（如3个+5=+30）；无符咒×1（+15）。验证：`node --check` OK；12 项测试全过（集满3结算/×2/×1/描述同步）。
- **文件**：`index.html`（`landOn` 沉睡分支/`CHARMS.rooster`），`editor.html`（`EDITOR_CHARMS.rooster`/`simulatePath`/装备提示），`docx`


### 【8.22·沉睡格盲盒】游戏内沉睡格不显示具体收益(数值/骰子/金币),苏醒时才揭晓（index.html）
- **改了什么**：沉睡格 3D 渲染从显示具体收益（`+N`/`🎲N`/`💰N`）改为**只显示 💤 + ?**；2D 地图 `mLab` 同样只显示 💤。`sleepReward` 结算逻辑保留——集满3个苏醒时才按类型揭晓（`+N`/`🎲+N骰`/`💰+N金`），鸡符咒×1.2（后改×2）。
- **为什么**：用户希望沉睡格营造"盲盒感"——不知道藏着数值还是骰子还是金币，苏醒时才有惊喜。
- **后果**：玩家看到沉睡格只知道是 💤 盲盒；编辑器属性面板仍可设收益类型/数量（仅游戏内隐藏）。验证：`node --check` OK；9 项测试全过（渲染只显示💤?/2D只显示💤/结算保留揭晓）。
- **文件**：`index.html`（`tileIconHTML` 沉睡分支/`mLab`）


### 【8.22·沉睡格多类型收益】沉睡格支持数值/骰子/金币三种收益（index.html + editor.html）
- **改了什么**：沉睡格从仅数值收益扩展为**数值/骰子/金币**三类——用 `p.rt`（value/dice/coin）+ `p.ra`（数量）结构（兼容旧 `p.v`）：渲染按类型显示图标、结算 `sleepReward` 按 `rt` 分发（骰子加 `diceLeft`、金币加 `coins`、数值加 `val`），默认数量骰=1/金=10。编辑器属性面板加「收益类型」下拉 + 数量输入（`setSleepRT`），导出含 `rt/ra`，`simulatePath` 按类型结算。
- **为什么**：用户希望沉睡格不仅数值收益，还能调整骰子和金币收益。
- **后果**：沉睡格可设为数值/骰子/金币任意类型；游戏内按类型发放。验证：`node --check` OK；20 项测试全过（三类型渲染/结算/模拟/鸡符咒/旧格式兼容/editor面板导出）。
- **文件**：`index.html`（`tileIconHTML`/`landOn` 沉睡/`mLab`），`editor.html`（属性面板/`setSleepRT`/`cellToExport`/`simulatePath`）


### 【8.22·编辑器断路放行】起点无法到达Boss改为软警告,断路/跨层设计可保存导入（editor.html）
- **改了什么**：`validateLevel` 把"起点无法到达Boss"从**硬错误**降为**软警告**——返回值改为 `{errs,warns}`，断路（如第33关：起点与Boss物理断开，靠跳跃/龙符咒铺云/跨层传送门通关）不再阻止保存/导入，只在难度面板显示黄色警告条；`saveToLib`/`writeCurrentToGame` 只拦 `errs`。`buildAdj` 同步：物理相邻仅同层、传送门按 `link.z` 跨层配对。
- **为什么**：用户第33关路径断裂、起点无路径连Boss，但设计本意是让玩家通过跳跃或搭路（龙符咒铺云）通关，要求断路也能直接导入游戏本体。
- **后果**：断路地图可正常保存/写回本体；缺起点/重复格等真错误仍拦截。验证：`node --check` OK；8 项测试全过（断路无致命错误/有跳跃铺云警告/导出保留全图/缺起点重复格仍报错/连通关无警告）。
- **文件**：`editor.html`（`validateLevel`/`saveToLib`/`writeCurrentToGame`/`renderDifficulty`/`buildAdj`）




### 【8.20·龙关卡手工制作】龙1-3 + 龙Boss 四关用编辑器手工铺设，云雾格大量使用（levels/level_20~23.json）
- **改了什么**：8.20 用地图编辑器手工制作 4 张龙系关卡并写入 `levels/`：**L20 龙1**（start6/5骰/云雾格×4/宝箱×2/Boss1200）、**L21 龙2**（start15/5骰/**云雾格×12** 高密度云区/宝箱×3/Boss1600）、**L22 龙3**（start20/5骰/云雾格×4/宝箱×4/Boss2026）、**L23 龙boss**（start20/5骰/宝箱×3/Boss2500，无云）。
- **为什么**：龙Boss关(23)胜利条件=先踏过云雾格再胜 Boss，用户用编辑器为龙系 4 关手工铺云与数值分布，替代生成器默认布局。
- **后果**：龙系关卡有专属手工棋盘与云雾密度设计（龙2 高达 12 朵云）；文件时间戳 8/20 14:03~15:26（UTC）。**注**：L23 龙boss 无内嵌云雾，胜利判定为"到达 Boss 且数值>龙Boss"（不强制踏云），与龙1-3 的云区地形配合。
- **文件**：`levels/level_20.json`、`level_21.json`、`level_22.json`、`level_23.json`



### 【8.20·兔Boss关手工制作】兔Boss(19) 关卡用编辑器手工铺设（levels/level_19.json）
- **改了什么**：8.20 用编辑器手工制作 L19 兔Boss 关（start10/5骰/宝箱×2/精英×1/Boss3500，文件名 `level_19 .json`），配兔跃机制（Boss 孤立于跳跃落点）。
- **为什么**：用户为兔Boss关(19)手工设定数值成长与宝箱布局。
- **后果**：兔系关卡数值曲线到 Boss(3500) 的跳跃与成长空间由用户手工定；文件时间戳 8/20 0:59（北京时间约 8.20 凌晨）。
- **文件**：`levels/level_19.json`



### 【8.19·兔系关卡手工制作】兔1-3 三关用编辑器手工铺设（levels/level_16~18.json）
- **改了什么**：8.19 用地图编辑器手工制作 3 张兔系关卡并写入 `levels/`：**L16 兔1**（start5/5骰/**传送门×2**/宝箱×1/Boss989）、**L17 兔2**（start6/5骰/宝箱×1/陷阱×1/Boss720）、**L18 兔3**（start8/5骰/宝箱×4/精英×2/Boss1000）。
- **为什么**：用户手工制作兔系 16-18 关（含 L16 首次使用传送门配对连通 Boss 区），为兔跃机制铺路。
- **后果**：L16 是首张"仅靠传送门连通 Boss 区"的手工地图（呼应 8.18 传送门地图修复）；兔系数值从 5→8 起步、Boss 989→1000 递增。文件时间戳 8/19 0:06~0:41（UTC，即北京时间 8.19 凌晨）。
- **文件**：`levels/level_16.json`、`level_17.json`、`level_18.json`



### 【8.18·editor写回修复 + 云雾离开才消失 + 兔Boss数值】写回前重读磁盘防覆盖；云雾再走一步才消失；兔Boss需数值>Boss（index.html + editor.html）
- **改了什么**：①**editor 写回修复**——`writeToGameFile` 每次写回前**重新从磁盘读取 index.html 最新文本**（`await gameFileHandle.getFile().text()`），不再用导入时的旧 `gameFileText` 缓存整份覆盖——这是此前"index.html 改动反复被回滚"的根因（editor 用旧缓存覆盖了后来新增的游戏代码）。②**云雾格改"离开才消失"**——`landOn` 踩上云雾只计数（`doneSet.add` 防重复），不消失；`moveTo`/`doJump` 在**离开旧格**时若旧格是云雾且未消耗 → `cloudGone=true; clearCloudTile(oi); buildAdjacency()`，即"再走一步才消失"，玩家可正常站在云上继续走。③**兔Boss 判定**——恢复为 `ok=cv>p.v`（跳跃抵达 Boss 格 + 数值>兔Boss 才通关），此前被回滚成 `ok=true` 登顶即胜。
- **为什么**：用户指出 editor 写回会回滚特殊格子代码（确认根因=旧缓存整份覆盖）；并要求云雾"需要再走一步才消失"（而非踩上当场散，否则玩家卡住动不了）；兔Boss 需数值比较。
- **后果**：editor 写回只替换 LEVELS 数组、保留其它游戏代码；云雾"踩上可站、再走一步才散"；兔Boss 需数值>Boss 通关。验证：`node --check` OK（index+editor）；云雾逻辑测试12项 + 兔Boss 判定 + editor 重读磁盘 全过；walk/adjacency/art 回归全过。
- **文件**：`index.html`（`landOn`/`moveTo`/`doJump`/兔Boss分支），`editor.html`（`writeToGameFile`）

### 【8.18·云雾格踩上即消失】云雾格经过一次即消失（玩家铺的与关卡内置统一）（index.html）
- **改了什么**：云雾格效果统一为「**踩上一次即消失**」——`landOn` 的 cloud 分支不再只 `doneSet.add`（仅不触发），而是踩上当场 `p.cloudGone=true; delete p.isCloud; clearCloudTile(idx); buildAdjacency()`：清掉 3D 格子上的 ☁ 外观与 2D 地图云雾格、标记不可再走、重建邻接；`cloudsCrossed++` 保留（龙Boss关"踏过云"判定继续有效）。同时移除 `moveTo`/`doJump` 里原先"玩家铺云踩上计数 + 离开后消散"的重复逻辑（现在统一由 landOn 管，避免重复计数）。
- **为什么**：用户要求云雾格"经过一次就会消失"，第 20 关（龙1）cells 含 4 个云雾格（3,6/2,6/3,4/2,4）。
- **后果**：任意云雾格（关卡内置或玩家用龙符咒铺的）踩一次就消失变普通格、不可再走；龙Boss关"踏云"胜利条件因 cloudsCrossed 保留而正常。验证：`node --check` OK；云雾逻辑测试 9 项全过（landOn 踩上即 cloudGone/clearCloudTile/buildAdjacency、moveTo 不再单独处理、消失前可走/消失后不可再走、普通格正常）；L1~50 walk/邻接/art 回归全过。
- **文件**：`index.html`（`landOn`/`moveTo`/`doJump`）

### 【8.18·起始格稳健修复】normalizeLevel 把起点格排到数组第0位，idx=0 即从起点格开始（index.html）
- **改了什么**：**改用稳健方案**——把"从起点格开始"的保证从 `startLevel`（其 `idx=0` 行反复被外部回滚）移到 `normalizeLevel`：在 path 末尾把 `t==="start"` 的格移到数组第 **0 位**，并**同步重映射 `next`/`branches` 的数组下标**（旧si→0、>si的-1、<si不变，`id` 同步重编），保证邻接/连通/路径分析不因重排错乱。这样 `startLevel` 里即使 `idx=0` 被回滚，从第0格开始=从起点格开始。另重新应用 19 关兔Boss自由跳跃（`freeJumps`/`toggleJump`/`doJump`/`btnJump`）。
- **为什么**：用户反馈编辑器地图导入游戏后仍从第0格而非起点格移动；此前在 `startLevel` 的修复反复被外部回滚失效，故把保证下沉到 `normalizeLevel` 使其不可被单独回滚破坏。
- **后果**：编辑器导入的任何关卡（含19关起点在中间者）开局必在起点格；`next/branches` 重映射保证移动/难度不破。验证：`node --check` OK；重排测试（起点到第0位/id唯一/next有效/start→boss连通/起点已第0位不重排）5项 + 19关（起点(1,8)到第0位/Boss存在/id唯一）+ 自由跳跃14项 全过；L1~50 walk/邻接/art 回归全过。
- **文件**：`index.html`（`normalizeLevel` 重排+重映射/`startLevel`/`toggleJump`/`doJump`/`btnJump`/`freeJumps`）

### 【8.18·19关起点与自由跳跃】起始格改为起点格；兔Boss关特殊效果=开局1次自由跳跃(两格内)（index.html）
- **改了什么**：①**起始格修复**——`startLevel` 从 `idx=0` 改为 `idx=LEVEL.path.findIndex(p=>p.t==="start")`（无起点格回退0），主角不再从地图数组第0格开始，而是从真正的起点格开始（19关起点在 cells 第28格(1,8)）。②**兔Boss关(19)特殊效果**——开局 `freeJumps=1` 并获得提示；新增"自由跳跃"能力：`toggleJump` 优先消耗自由跳跃（`freeJumpActive`），否则才用兔符咒(消耗1骰)；`doJump` 对自由跳跃按「以自身为中心、两格内(Chebyshev≤2,含对角)」判定，落地时 `freeJumps--` 并清除标记；`btnJump` 启用条件与文案支持自由跳跃（显示「🐇 自由跳跃×N」），无需兔符咒/不耗骰。
- **为什么**：用户反馈19关主角从编辑器第0格(1,4怪)而非起点格(1,8)开始；且19关为兔Boss关应有特殊效果——开局给1次可随时使用的自由跳跃（自身两格内），用于跃上孤立Boss。
- **后果**：19关从起点格(1,8)开始；玩家可在任意时刻点「自由跳跃」在自身两格内落地（Boss(4,6)距可达格(2,8)恰为(2,2)可跳达），无需携带兔符咒。验证：`node --check` OK；14 项测试全过（起始格/自由跳跃状态/优先级/范围/消耗/兔跃保持半径2）。
- **文件**：`index.html`（`startLevel`/`toggleJump`/`doJump`/`sync` btnJump/`freeJumps`/`freeJumpActive`）

### 【8.18·编辑器允许跳跃孤立格】validateLevel 放行"可达格的半径2跳跃落点"孤立格（editor.html）
- **改了什么**：`validateLevel` 连通性检查在「传送门豁免」之外，新增**跳跃落点豁免**——某个格若与任一可达格相差 `(±2,0)/(0,±2)/(±2,±2)`（与游戏兔跃 `JUMPS` 完全一致），即视为合法的跳跃落点，即使它不邻接主路也不报"孤立"；真正不可达且不在跳跃半径内的孤立格仍报错；传送门逻辑保持。
- **为什么**：用户反馈编辑器总报"格(1,1)孤立，不与起点连通"，但部分格为后续跳跃机制（兔跃/孤立Boss）故意孤立放置，应允许。
- **后果**：兔跃关式"孤立 Boss + 跳跃落点"地图可在编辑器正常保存/导入；真孤立(误放)仍被拦截。验证：`node --check` OK；场景测试——水平/对角跳跃落点通过、真孤立仍报错、传送门连通仍通过 全过。
- **文件**：`editor.html`（`validateLevel`）

### 【8.18·牛符咒文案与连杀联动】牛符咒弹窗明确"大于才算通过、等于不算"；牛符咒掷骰胜过的敌人同样开启虎符咒连杀（index.html）
- **改了什么**：①**判定文案**——牛符咒弹窗从「掷出 >2/>4 即击败敌人」改为更明确的「掷出 **大于 2/4** 才可通过（**等于不算**）」，`CHARMS.ox` 描述同步改为"点数>2(精英>4)才可通过(等于不算)"；判定逻辑本身一直是严格的 `r>need`（普通>2、精英>4，等于算败），未改。②**虎符咒联动**——`useOxCharm` 掷骰胜过敌人后调用 `grantTigerChain()`：若这是本局击败的第一个敌人，虎符咒连杀状态照常开启（`tigerUsed` 守卫保证每局一次，普通战斗已开启则不重复）。
- **为什么**：用户反馈弹窗文案造成"≥"误解（实际是严格大于）；并询问牛符咒胜过的是否也该算"击败"触发虎连杀——设计上"击败第一个敌人"应涵盖牛符咒掷骰胜利，二者均为每局一次且消耗资源，作为牛+虎组合的协同合理。
- **后果**：文案与判定一致（等于不算通过）；牛符咒首胜也能开启虎连杀，组合更灵活。验证：`node --check` OK；判定边界（普通2败/3胜、精英4败/5、6胜）+ 牛胜触发虎连杀、已开启不重复 全过。
- **文件**：`index.html`（`offerOxCharm` 文案/`useOxCharm`/`CHARMS.ox`）

### 【8.18·移动邻接回归修复】16关 (4,6)→(3,6) 无法移动——邻接恢复为物理相邻+传送门边（index.html）
- **改了什么**：`buildAdjacency` 由「图边(next/branches)+物理相邻」交集改回「**所有物理相邻(曼哈顿=1)的路径格 + 传送门配对边**」。此前交集规则误伤手工地图：16关 path 顺序不是线性走法，(4,6) 的 next 指向 (2,6) 而 (3,6) 排在更后（idx7），虽物理相邻却不是 next 前后项，导致走到 (4,6) 无法再走 (3,6)。恢复自由移动后相邻格均可走；传送门配对边保留，使"仅靠传送门连通的 Boss 区"仍不会被路径数组顺序误连。
- **为什么**：用户反馈 16 关走到 (4,6) 后无法继续到 (3,6)——两者曼哈顿距离=1 本应可走，是上一轮「图边+物理相邻」交集引入的回归。
- **后果**：16 关恢复正常自由移动；传送门 Boss 区隔离设计、L19 兔 Boss 需跳跃、各生成关连通性均保持。验证：`node --check` OK；16关(4,6)→(3,6)可走 + start→boss 可达、传送门Boss区仅靠传送门可达、主路径末格不能直连Boss区、L1/7/15/23/27/31/50 连通、L19 兔Boss不可步行直达——全部通过；walk/art 回归全过。
- **文件**：`index.html`（`buildAdjacency`）

### 【8.18·编辑器补齐符咒】editor 装备面板加入全部 12 个符咒，虎/马效果纳入难度模拟（editor.html）
- **改了什么**：`EDITOR_CHARMS` 从仅 3 个（狗/鼠/牛）扩展到**全部 12 生肖符咒**（补虎/兔/龙/蛇/马/羊/猴/鸡/猪，名称/图标/描述与 index.html 一致）；`simulatePath` 新增**虎符咒连杀**模拟（击败首个敌人后开启连杀，下一个敌人数值≤(当前数值+临时)×2 直接斩杀、一次性、不计门槛）与**马符咒**模拟（首骰×2 ≈ 有效骰子 +1）；兔/龙/蛇标注为策略性效果（跳跃/铺雾/陷阱免疫），数值上难量化；装备面板与难度提示文案同步更新。
- **为什么**：用户反馈 editor 里没有新增符咒可选（此前只有狗/鼠/牛），难度模拟无法反映新符咒。
- **后果**：编辑关卡时可选择任意符咒评估难度；虎符咒连杀能"救回"一个≤2倍的大敌，马符咒提升行动点，模拟结果更贴近实际游戏。验证：`node --check` OK；simulatePath 场景测试——虎连杀救回 would-block 敌人、无虎则被卡死、>2倍不触发连杀、马 effDice+1 全过。
- **文件**：`editor.html`（`EDITOR_CHARMS`/`simulatePath`/装备提示）

### 【8.18·传送门地图修复】编辑器支持"仅靠传送门连通"的合法地图；游戏邻接改为图边+物理相邻+传送门边（editor.html + index.html）
- **改了什么**：①**编辑器**——`buildAdj` 把传送门配对（`portal.link`）作为一条"传送边"计入连通性，`validateLevel` 的"孤立格"校验由此认可"经传送门可达 Boss"的地图（此前只豁免 portal 格本身，Boss 区普通格/Boss 仍被判孤立，导致存储/导入报"格(2,1)孤立，不与起点连通"）；真正的孤立非传送门格仍正确报错；未配对传送门豁免孤立且不崩溃。②**游戏本体**——`buildAdjacency` 从"仅图边(next/branches)"改为「**图边 + 物理相邻(曼哈顿=1)**」双重校验，并新增**传送门配对边**：既避免跨岔路/斜向捷径，又避免线性路径顺序把物理不相邻的分区连起来（否则 Boss 区会因 `cells` 线性顺序的 `next` 被玩家直接走进去，绕过传送门）；`pathEdges`（2D 地图连线）同步只画物理相邻边，避免把"仅传送门连通区"画成直接连线。
- **为什么**：用户编辑第16关要求玩家只能从传送门进入 Boss 路径（Boss 区与主路径无物理相连），但编辑器报"地图不合法，格(2,1)孤立"，无法存储/导入；且游戏旧邻接会允许绕开传送门步行进入 Boss 区。
- **后果**：仅靠传送门连通的关卡可在编辑器正常保存/导入、游戏内必须走传送门才到 Boss；兔跃关(19)的孤立 Boss 也更严格地只能跳跃抵达（顺带修复了此前可沿线性 next 步行走上去的潜在问题）。验证：`node --check` OK；编辑器校验（传送门连通通过/真孤立报错/未配对豁免）3 项全过；游戏邻接（Boss 经传送门可达、主路径末格不能直连 Boss 区、pathEdges 无误导连线）全过；L1~50 walk/邻接回归全过（L19 兔 Boss 正确不可步行直达）。
- **文件**：`editor.html`（`buildAdj`），`index.html`（`buildAdjacency`/`pathEdges`）

### 【8.17·虎符咒重做 + UI 美术资源层】虎符咒改为自动连杀状态；UI 拆分为 images/UI 可替换资源（index.html + images/UI）
- **改了什么**：①**虎符咒重做**：原「击败敌人后弹窗询问是否进入连杀」+「连杀触发后不清除」改为——击败本局**第一个敌人**（小怪/精英均可）后**自动**进入「连杀」状态（每局一次，不再弹窗）；连杀状态下遇到下一个**数值≤自身2倍**的敌人**直接判赢**，显示「连杀生效」横幅+Toast，并**移除连杀状态**（一次性）；>2倍敌人不消费状态（保留到遇到合格敌人为止）。删 `offerTigerChain/confirmTigerChain/declineTigerChain`，新增 `grantTigerChain`（自动授予），精英获胜分支也接入；`CHARMS.tiger` 描述同步。②**UI 美术资源层**：新增 `images/UI/` 目录（含 README 规范与子目录 topbar/stat/controls/modal/panel/level/shop/prep/icons），新增 `UI_ART` 注册表 + `uiBg/uiImg/applyUIArt/applyUICards` 资源层——黑色渐变 UI 组件全部打上 `data-ui` 标记，放同名图即覆盖默认外观（`images/UI/<子目录>/<名>.png/.svg`），缺图自动回退 CSS 渐变，换皮=放图不改代码；动态生成的武器/符咒卡（`.pw/.pc`）、关卡格（`.lv`）由 `applyUICards` 铺 `panel/card`、`level/cell`；金币/骰子/旗帜/步数小图标可经 `uiImg` 覆盖。
- **为什么**：用户要求虎符咒改为「自动获得连杀状态、触发即移除」的一次性连杀；并希望把以黑色为主的 UI 组件美术资源化，明天直接用自己美术替换。
- **后果**：虎符咒交互更直观（自动开启+一次性连杀，横幅提示清晰）；UI 全组件可逐张换皮，缺图保持现状。验证：`node --check` OK；虎符咒状态机测试（首敌自动开启/≤2倍判赢并移除/＞2倍保留/边界=2倍/无符咒不开启）11 项全过；data-ui→文件路径解析与目录结构 22 项全过；walk/adjacency/art 回归全过。
- **文件**：`index.html`（`grantTigerChain`/`landOn` 虎连杀分支/`CHARMS.tiger`/`UI_ART`/`uiBg`/`uiImg`/`applyUIArt`/`applyUICards`/各屏 `data-ui` 标记），`images/UI/`（新目录 + README）

### 【8.17·四联动】去岔路标识 + 落点居中 + 修跨岔路捷径 + 牛符咒弹窗掷骰（index.html）
- **改了什么**：①**去掉岔路标识格**——移除 3D 格子的「岔」徽标（`fork-badge`）与 2D 地图岔路格紫色 `◇`（`.mcell.branch`），岔路格并入普通格纹理（`FLAT_TEX` 加 `branch:'normal'`）。②**落点居中**——玩家棋子 `.tokin` 由 `align-items:flex-end` 改为 `center`，角色正好落在格子正中心，消除视觉误导。③**修跨岔路捷径 bug**——`buildAdjacency` 从「曼哈顿距离=1 全局判定」改为「**只走路径图边**（`next`/`branches` 双向，可原路折返）」，杜绝从岔路前一格直接跳到另一条分支（如（2,0）不经（3,0）直达（3,1））；start→boss、折返、岔路末格连通性全部保留。④**牛符咒改弹窗 + 掷骰动画**——首次比不过小怪/精英时先弹「🐂 牛符咒」询问（放弃 or 使用每局一次的能力，附当前数值 vs 敌人数值）；选使用则消耗 1 骰并播放与行动点同款掷骰动画（`animateOxRoll`），普通怪 `>2`、精英 `>4` 即通过，通过吸收敌人、失败判负；原 `tryOxCharm` 直接掷骰改为 `offerOxCharm/useOxCharm/declineOxCharm`。
- **为什么**：用户反馈四点——岔路标识多余、落点不在格中心误导、跨岔路捷径是明显 bug、牛符咒应「先弹窗询问 + 展示掷骰动画 + 再判定」。
- **后果**：棋盘只保留普通格外观；角色居中；移动严格沿路径图（无捷径）；牛符咒交互更清晰可感知。**注意**：本批改动曾因文件被外部覆盖回退而一度丢失，已重新应用并复核确认写入磁盘（`offerOxCharm` 3 处、`tryOxCharm` 0 处、`fork-badge` 渲染 0 处、`.tokin` center、`buildAdjacency` 图边均生效）。验证：`node --check` OK；生成关/教程关 start→boss 与折返用图边全连通、岔路图跨支捷径已消除且连通保留、变体/锁步回归全过。
- **文件**：`index.html`（`build3`/`FLAT_TEX`/`.tokin`/`buildAdjacency`/`landOn`/`offerOxCharm`/`useOxCharm`/`declineOxCharm`/`animateOxRoll`/`CHARMS.ox`/删除 `fork-badge`/`.mcell.branch`）

### 【8.17·数值徽章上贴图】普通怪/精英怪/Boss 数值叠在贴图顶上，黄/紫/红三色（index.html）
- **改了什么**：实体数值徽章从「图片下方」改为**叠在贴图顶上居中显示**——`.mob-val`/`.boss-val` 改为 `position:absolute; left:50%; top:50%; translate(-50%,-50%)`，`.tile-entity/.boss-entity/.mob-entity` 加 `position:relative` 作定位基准，`z-index:3` + `pointer-events:none`（不挡点击）；颜色：普通怪=黄 `#ffd257`、精英=紫 `#e0b3ff`（揭晓后仍保持紫，移除原先揭晓变黄）、Boss=红 `#ff6b6b`，均保留黑色描边发光保证可读。精英格「近距揭晓」机制保留（未靠近显示 ?，靠近后显示真值，仍紫色）。
- **为什么**：用户要求所有普通怪/精英/Boss 的数值放在对应贴图顶上显示，并按 黄(普通)/紫(精英)/红(Boss) 区分。
- **后果**：三色数值徽章直接叠在立绘上，颜色一眼区分敌人类别。验证：`node --check` OK；walk/art/变体/锁步回归全过。
- **文件**：`index.html`（`.mob-val`/`.mob-val.elite(.revealed)`/`.boss-val`/`.tile-entity`/`.boss-entity`/`.mob-entity`）

### 【8.17·天空对齐与实体变体】天空元素化并 100% 对齐 #scene；怪/精英多贴图随机分配，教程关 Boss 用另一张精英贴图（index.html + themes README）
- **改了什么**：①**天空**：由 `#scene` 的 CSS 背景改为专用 `#skyImg` 元素（`position:absolute;inset:0;width:100%;height:100%;object-fit:fill;z-index:0`），宽高强制与 #scene 完全对齐（可自由调整），`applyArt` 把套图 sky→background→基础 `images/sky.svg` 的候选链结果设进 `#skyImg`。②**实体立绘变体**：怪 3 种 `mob1/2/3`、精英 2 种 `elite1/2`，按「关卡id×格序号」稳定随机分配（`mobVariant/eliteVariant`，重开不变），每格只出现一种；实体立绘候选链扩展为 `entities/<名>.svg → tiles/<名>.svg → 基础图`（支持你把贴图放 `set1/tiles/`）；宝箱 `chest.svg` 同链。③**教程关 Boss**（1、2关，无生肖）：用本关精英格没用到的另一张精英贴图（`levelEliteVariant()` 取本关第一个精英格的变体，Boss 取另一张；如精英=elite2 → Boss=elite1）；生肖 Boss 链也补 `tiles/boss_<z>.svg`。④顺带修复 Boss 图片 onerror 的嵌套引号隐患（改为同 DOM 隐藏柴犬兜底，失败时 `querySelector('.shiba')` 显示）。
- **为什么**：用户要求天空宽高与 #scene 完全一致；已把宝箱/怪物(3种)/精英怪(2种)贴图放进 `set1/tiles/` 但未显示，要求随机分配、每格一种；教程关(1/2)Boss 换成与精英格互补的另一张精英贴图。
- **后果**：天空与棋盘视口严丝合缝；set1 的 mob1-3/elite1-2/chest/boss_dog 全部按变体随机生效（命中 `set1/tiles/*.svg`）；1、2关 Boss 显示另一张精英贴图。验证：`node --check` OK；变体逻辑（3 种 mob/2 种 elite 全覆盖、每格唯一且稳定、Boss 取互补精英）全过；实体候选链实际落盘命中 6/6；walk/art/锁步回归全过；README 已补变体命名。
- **文件**：`index.html`（`#skyImg`/`applyArt`/`MOB_VARIANTS`/`ELITE_VARIANTS`/`mobVariant`/`eliteVariant`/`levelEliteVariant`/`entityCands`/`tileIconHTML`/`build3`），`images/themes/README.md`

### 【8.17·天空/背景/格尺寸】天空拉伸至与 #scene 完全对齐；整页背景固定 background.png；格子加大（index.html + themes README）
- **改了什么**：①**天空覆盖**：`#scene` 的天空由 `cover` 改为 `100%×100%` 拉伸，与 #scene 宽高完全对齐（不再按比例裁切）。②**整体背景固定**：整页 `body` 背景固定为 `images/background.png`（CSS 默认），所有页面（主菜单/选关/商店/对局）一致不随页面改动——删除 `applyMenuArt` 的 body 覆盖（含 `menu/background.png` 槽）与 `applyArt` 对 body 的复位/覆盖，天空只铺 `#scene`；顺带删除无用 `bodyBgCSS`。③**格子加大**：`TW` 60→64、`.tile` 60→64px，格子填满整个 64px 网格（无空隙，边框成网格线），中心定位仍精确（`p.c*T-TW/2`）。
- **为什么**：用户要求天空宽高与 #scene 完全对齐、整页背景任何页面都固定为 background.png、格子尺寸加大。
- **后果**：页面背景全站统一、天空贴合棋盘视口、棋盘格子更大更满。验证：`node --check` OK；walk/art/锁步回归全过；README 已更新（天空 100%×100% 对齐、页面背景固定说明）。
- **文件**：`index.html`（`applyArt`/`applyMenuArt`/删 `bodyBgCSS`/`TW`/`.tile`），`images/themes/README.md`

### 【8.17·天空与平面格】天空改用 sky.svg 且只铺满 #scene；所有格子取消顶面/侧面只留纹理+立绘（index.html + themes README）
- **改了什么**：①**天空**：美术槽位由 `background` 改名为 `sky`（`ART.sky='images/sky.svg'`），套图候选链 `sky.svg/png → background.svg/png → images/sky.svg`（兼容你已放的 `set1/background.svg`）；**覆盖范围**从「整页 body」收窄为「仅 `#scene` 棋盘视口」——`applyArt` 把天空铺到 `#scene`（darken + cover），并把游戏页 body 复位为纯深色（整页不再被背景贴图铺满）。②**平面格**：所有格子取消顶面（`.tile-top`）与侧面（`.tile-side`）立体面，格子即一张平铺纹理（`applyTopTextures` 直接把纹理铺满 `.tile`），上方只保留内容层（立绘实体 + 信息徽章）；`tileTextureType()` 让增益复用 normal、怪/宝箱/精英/Boss 用 normal 纹理打底再叠立绘；删除全部 `.t-xxx .tile-top/.tile-side` 类型色块 CSS；状态效果（done/flash/reachable/pop）改作用于整格；云朵格保留浅蓝 CSS 兜底。
- **为什么**：用户反馈背景覆盖范围过大，希望与 `images/sky.svg`（512×512 天空）的覆盖一致，并明确把背景资源改为 `sky.svg`、重定覆盖范围；格子要求去掉 3D 顶面/侧面，只保留纹理 + 立体贴图。
- **后果**：天空只出现在棋盘视口、整页不再被大面积贴图铺满；格子呈平面纹理风格（实体仍竖立正对镜头）。验证：`node --check` OK；sky 候选链（set1 无 sky 时命中 background.svg、基础命中 images/sky.svg）全对；walk/art/锁步回归全过；README 已同步 sky/平面格规范。
- **文件**：`index.html`（`ART.sky`/`applyArt`/`build3`/`.tile`/`.tile-content`/删 `.tile-top`/`.tile-side` 及类型色块/`applyTopTextures`/`tileTextureType`/状态效果规则/`.t-cloud`），`images/themes/README.md`

### 【8.17·套图生效修复】background.svg/board.svg 生效 + 删除旧绿色 low-poly 地面/路径贴图（index.html）
- **改了什么**：①`background` 候选链补 `.svg`（原只查 `background.png`，用户放入 `set1/background.svg` 不生效）：`artChain(['background.png','background.svg'], …)`；②修复 `board.svg` 不生效的根因——`board-img` 标签带 `src=""` + `onerror="this.remove()"`，空 src 触发 onerror 在 `applyArt` 设图前就把 `<img>` 删掉了：改为无 src 属性、加载成功才设 `src`、失败才 `onerror` 移除；③**删除旧的深绿色 low-poly 地面/路径占位贴图**：`build3` 不再生成 `terrainHTML`（草地棋盘格 + 树木/岩石等 3D 装饰）与 3D 路径连接线 `path-layer`（并删除 `terrainHTML`/`getDecorTypes` 死代码），`#scene` 地面改为纯色贴图（`board.svg`）承载；`.ground` 底色由亮绿改为中性深色；增益格 `.t-gain` 顶面/侧面取消旧深绿立体配色，改为与普通格同款（纹理即 normal.svg，仅以 +N 徽章区分）。
- **为什么**：用户反馈 `set1/background.svg`、`board.svg` 未生效（背景候选缺 svg 后缀 + board-img 空 src 自毁），并要求删掉旧的深绿色 low-poly 立体贴图（草地/装饰/增益格立体配色）与路径贴图——它们已被 `themes` 套图（board.svg/normal.svg）取代。
- **后果**：set1 背景/棋盘底面/格面纹理全部生效（缺图自动回退基础图）；3D 场景不再渲染绿色草地格、装饰物与路径线（2D 俯视地图的路径线保留，`connectorSVG` 仅用于地图）。验证：`node --check` OK；候选链实际落盘文件解析（background.png→svg→基础、board.png→svg→基础、tiles/normal.svg 直中）全对；walk/art/锁步回归全过。
- **文件**：`index.html`（`applyArt`/`build3`/`.ground`/`.t-gain`/删除 `terrainHTML`/`getDecorTypes`/3D `path-layer`）

### 【8.17·还原单层方案】四层视角→单平面旋转(性能/贴图减负) + 保留丝滑旋转与美术统一（index.html）
- **改了什么**：用户反馈旋转后每走一步仍卡顿，怀疑是 4 层同时渲染的贴图/DOM 负担而非镜头角度问题，决定**还原为单层方案**：①`build3` 恢复单块平面渲染（一格一份 DOM/贴图，`id=t{i}` 直取），删掉 4 个 `.pview` 层、`camIdx/spinTo/tileCache/tileEls/tileVis/ANG/angleSet/angleIndex` 等全部四层基础设施；②镜头恢复「单平面 + 动态 `rotateZ(R)` 随主角朝向旋转」——`R=headingR()` 恒为 90° 倍数，**静止帧天然就是四角度单平面视角之一**，无透视变形；③**丝滑旋转保留**：`#cam` 的 `transition` 同时声明 `transform` 与 `--nrz`（同缓动 `.3s cubic-bezier(.25,.8,.3,1)`）锁步过渡，棋盘转、实体/主角反向等速抵消，全程正对镜头零倾斜；`--nrz` 仅在朝向变化时更新（直线移动跳过，免每步触发实体样式重算）；④美术统一方案（`artChain`/`applyTopTextures` 去重/增益复用 normal/`board-img` 全层）与性能优化（`--ntx` 构建时只设一次）保留并适配单层。
- **为什么**：4 层方案让每格贴图/DOM 变 4 份，旋转后每步卡顿疑与贴图/DOM 负担直接相关；单层方案用一块平面即可满足「镜头跟随 + 90° 量化 + 静止帧四角度无变形」，且贴图只加载/绘制一份。
- **后果**：每步 DOM 从 4 份降到 1 份（`getElementById` 直查），贴图从 4× 降到 1×；旋转过渡仍平滑（transform+--nrz 锁步）。验证：`node --check` OK；8 关全程 `headingR` 朝向采样恒为 90° 倍数、四轴向朝向规范、实体锁步抵消（-270~180 全角）全过；artChain 候选链 + 锁步数学全过。
- **文件**：`index.html`（`build3` 单层/`camUpdate`/`headingR`/`#cam` 过渡/`sync`/`sceneClick`/`clearCloudTile`/`placeCloudAt`/`popTile`/`showBattle`/`flash3`/`float3`/`moveTo`/`doJump`/`cameraMove`，删除 `.pview`/四层状态与函数）

### 【8.17·丝滑与美术统一】旋转/每步卡顿修复 + 美术资源层按 `images/themes` 统一（index.html）
- **改了什么**：①**每步卡顿修复**（旋转后每走一步都卡）：a. 4 层格子建索引缓存 `tileCache`（build3 一次性收集，`sync` 每步直接复用，删掉每格 `querySelectorAll`）；b. 常量 `--ntx` 改为 build3 只设一次（原先每步 `camUpdate` 重复 `setProperty` 会触发全场景含 4 层所有 `.tokin/.tile-content/.f3` 的样式重算）；c. 旋转轴 `transform-origin` 只在换格时更新（原先每步重设 4 次触发布局）。②**旋转动画更丝滑**：`#cam` 的 `rotateZ(spinAnim)` 与当前可见层的 `--nrz` 用同一缓动 `cubic-bezier(.25,.8,.3,1)`、同一时长 420ms **锁步过渡**——棋盘转、实体/主角反向等速抵消，全程正对镜头零倾斜、交接零跳变（原先旋转中实体/主角会歪、交接有 snap）；收尾用 `getComputedStyle` 强制刷新后再瞬时归零，杜绝过渡残留；`#cam` 基础过渡也换成同款缓动 .3s。③**美术资源层统一**：新增 `artChain(subPaths, basePaths)` 统一解析「套图优先→基础兜底」候选链（`images/themes/<set>/… → ART 基础表 → CSS/emoji 回退`），背景/棋盘底面/平躺纹理/立绘/Boss 全部走它；`applyTopTextures` 按类型**去重加载一次**铺到所有同型格面（含 4 层，省 4× 重复加载），增益格按 README 规范复用 normal 纹理；棋盘底面 `board-img` 原来只有第 1 层有图（id 重复），现统一平铺全部 4 层。
- **为什么**：用户反馈旋转后每走一步卡顿、旋转不够丝滑，并把美术资源化方式定为 `images/themes`（README：五套分段换皮 1~11/12~23/24~39/40~47/48~50，目录 background/board/tiles/entities/menu），要求全游戏美术部分统一遵守。
- **后果**：每步 DOM 写入从「46 次扫描 ×4 层 + 常量重设」降到「1 次缓存查表 + 换格才更轴」；旋转全程实体/主角不歪、交接无跳变。验证：`node --check` OK；artChain 无套图/套图两态候选链、Boss/实体/平躺/Boss 缺 z 全过；锁步数学（-90→0、0→90、180→-90 全程净旋转=0）全过；角度/步行/居中回归全过。
- **文件**：`index.html`（`tileCache`/`build3`/`camUpdate`/`spinTo`/`SPIN_MS`/`artChain`/`applyArt`/`applyTopTextures`/`FLAT_TEX`/`tileIconHTML`/`#cam` 过渡），`images/themes/README.md`（规范，已对齐）

### 【8.17·四角度单平面视角】棋盘随转向变形修复——每关同时渲染 4 个固定 90° 静态平面层，镜头在层间旋转切换（index.html）
- **改了什么**：2.5D 伪 3D 视角此前把整块棋盘平面 `rotateX(52°)` 透视倾斜后，再叠加随主角朝向动态变化的 `rotateZ(R)`（`headingR()`），倾斜平面在转向动画中经过中间角度时投影被拉伸变形。现改为**四角度单平面视角**：①**同时渲染 4 层**——`build3` 把整关（地面/地形/连线/格子/实体）克隆进 4 个 `.pview` 层，每层把 `rotateZ(ANG[k])` 与 `--nrz:-ANG[k]` 烘焙为静态变换（`ANG=angleSet(headingR())`，对齐关卡「起步朝向」，四角恰为 0/90/180/270 四方向）；**任一时刻只显示当前朝向那一层**，静止时各层零动态变换→永不出现透视变形；②**镜头随主角转向**——`sync` 每次用 `angleIndex(headingR())` 求当前朝向层，变了就 `spinTo()`：给 `#cam` 加 `rotateZ(spinAnim)`（±90°/180° 最短路径）做 0.38s 旋转动画，动画结束把可见层与主角 token（`#tok`）切换到目标层并归零 `#cam` 旋转——由于目标层恰好静止在该角度，交接像素级无缝；③`--nrz/--ntx` 反向旋转随层继承，实体/主角始终正对镜头；`WASD`/点地图仍为屏幕相对方向（`cameraMove` 用 `lastR`=当前朝向），W 始终=视角远方；④格子的状态更新（done/dim/reachable/云雾/弹出/胜负光晕/精英揭晓）改为 `tileEls(i)` 全层同步，点击取坐标只用 `tileVis(i)`（当前可见层）；`float3` 飘字挂到可见层。同时**回退上一版镜头锁定方案**（删 `camR`/`applyHeroFace`，主角恢复固定 `hero_front.png`）。
- **为什么**：用户明确不要「镜头锁定」，要求每关**同时渲染四个角度的单平面视角**、随主角动态旋转整体不变形，并选择「单画面跟随切换 + 90°旋转动画」的过渡方式（接受旋转过程中的短暂透视拉伸）。
- **后果**：镜头不锁定但转向时棋盘各静止帧均为 4 个固定 90° 单平面视角之一（永远干净）；转向为量化 90° 旋转动画，中途有短暂拉伸（用户认可）。**旋转轴修复**：4 层 `rotateZ` 的 `transform-origin` 在每次 `sync` 跟随主角所在格（= `#cam` 的 `translate(-cur)` 同点），否则各层绕棋盘左上角旋转、主角会被甩出屏外（曾导致「转向后地图丢失/错位」，修复前 (2,3) 格 90° 偏移 286px）。验证：`node --check` OK；`angleSet` 任意 base 生成 4 个等距 90° 角、四方向→四层唯一映射、任意层间转向增量 ∈ {±90,±180} 全过；8×8 格×4 角度 主角居中 256 断言全过；8 关步行模拟（L1/7/15/23/27/31/50 到达 Boss）全程转向增量均规范（兔跳关 19 Boss 孤立仅跳跃可达，步行模拟自然到不了，属设计行为）。
- **文件**：`index.html`（`.pview` 样式/`build3` 四层/`camTransform`/`spinTo`/`angleSet`/`angleIndex`/`tileEls`/`tileVis`/`sync` 触发/`sceneClick`/`popTile`/`showBattle`/`flash3`/`float3`/`moveTo`/`doJump`/`cameraMove`）

### 【8.16·新Boss机制】虎/兔/龙/蛇/马五关 + 对应符咒 + 云雾格地形（index.html + editor.html + md）
- **改了什么**：按设计稿新增 5 个特殊 Boss 关与符咒，并让 md 与游戏对齐——①**虎杀(15)**：胜=数值>Boss 且 ≤2×Boss（杀过头反败），虎符咒=每局一次战胜敌人后可进入连杀（遇敌≤自身2倍直接斩杀，`offerTigerChain`/`chainKill`）；②**兔跃(19)**：Boss 孤立在半径2跳跃落点（`JUMPS` 8 方位，genLevel 选界内且四周无相邻格的落点），只能跳跃抵达登顶即胜，兔符咒=骰子转跳跃（可多次）；③**腾云驾雾(23)**：Boss 前 4 格为云雾格，须先踏云雾再胜 Boss，龙符咒=每局铺 4 格云雾（云雾格踩过一次后消失，离开时标记 `cloudGone` 并重建邻接）；④**潜伏(27)**：全陷阱地图+Boss(8)，蛇符咒=每回合前 3 陷阱减益失效（`snakeShield`，掷骰时重置）；⑤**马跃(31)**：总行动≥30格且数值≥Boss，马符咒=每关首骰 ×2 行动点（`horseFirst`）；⑥HUD 加「已行动」统计 + 兔跃/龙雾按钮（`btnJump`/`btnCloud`），`sceneClick` 支持跳跃/铺云模式；⑦`endGame` 解锁全 12 生肖符咒；⑧**md 对齐**：鼠Boss=数值≥Boss(含成长,每杀敌+20%，无开2宝箱要求)、牛Boss=数值≥Boss(需求=自身数值)+Boss前一格豪赌+500，均以游戏实际为准修正；⑨编辑器加「云雾格☁」按钮/`TYPES`/`.ct-cloud`。
- **为什么**：用户要求 md 与游戏保持一致（鼠/牛特殊效果以游戏为准），并把这 5 个 Boss、符咒与云雾格/陷阱格等新内容落到游戏本体与编辑器。
- **后果**：新 Boss 关生成即玩（genLevel 特殊化：31 P≥30、27 P≥20、23 P≥18、19 P≥16；兔 Boss 与任何格不相邻已验证），云雾格双端可用；14 项逻辑断言 + 全文件语法校验全部通过，`node --check` OK。
- **文件**：`index.html`（CHARMS/ART.charms/`cloud` 类型/`JUMPS`/`moveTo`/`doJump`/`placeCloudAt`/`landOn`/`genLevel`/`endGame`/HUD），`editor.html`（云雾格），`docx/宝箱boss武器和符咒系统_初版_0812.md`（Boss/符咒/地形章节重写）

### 【8.16·美术实体直立】棋格平躺 + 怪/宝箱/增益等实体立绘竖立（index.html）
- **改了什么**：修复美术资源化后实体全部消失的回归——此前 `tileIconHTML` 以「配置了图片→内容层只显示数值徽章/空」实现（`ART.tiles` 12 种全配路径，导致 chest/coin/dice/trap/portal/branch/start 全空、mob/elite/gain 只剩数字），且图片从未被应用。现在按「平躺 vs 立体」双角色分工：①**平躺类型**（normal/start/gain/trap/portal/coin/dice/branch，均为 64×64 平面纹理）——图片由新增 `applyTopTextures()` 预加载成功后平铺到 `.tile-top` 顶面（`background-image` + `background-size:100% 100%`），失败/缺图保留 CSS 渐变，内容层只留信息徽章（gain 的 ±N、start 的「起」）；②**立体类型**（mob/elite/chest/boss，均为大尺寸 PNG 立绘）——图片渲染为 `.tile-entity` 直立立绘（`tile-content` 已被 `--nrz/--ntx` 反向旋转，图片始终正对镜头），数值徽章保留 `mob-val`（精英保留 `mob-val elite` 供临格揭晓逻辑复用）；③拆出 `entityBadgeHTML`（数值徽章）与 `cssEntityFallback`（缺图/删图回退整套 CSS 实体）；④新增 `FLAT_TYPES` 常量 + `.tile-entity/.tile-entity-img` 样式（56px、投影、上移 14px）。
- **为什么**：用户要求「棋盘平躺、怪/宝箱要立体站上来，两者美术都要资源化」；此前替换的 chest/enemy/elite 新 SVG 完全不显示。
- **后果**：换皮=换文件全面成立——替换 `images/tiles/` 里 64×64 的文件即换顶面平铺纹理，替换大尺寸立绘文件即换站立实体；缺文件自动回退（纹理→渐变、立绘→CSS实体）。14 项渲染断言 + 全文件语法校验全部通过。
- **文件**：`index.html`（`FLAT_TYPES`/`tileIconHTML`/`entityBadgeHTML`/`cssEntityFallback`/`applyTopTextures`/`.tile-entity` 样式）

### 【8.15·UI美化】主菜单改为 UI 原图组件抠图 + 热区按钮（index.html + images/UI_cut.png）
- **改了什么**：主菜单改为「UI 设计图组件直出」方案——①`scripts/cut_ui.py` 用 PIL+numpy 把 `images/UI.png` 的近白底（RGB≥235）抠为透明、羽化边缘、按内容 bbox 裁剪，产出 `images/UI_cut.png`（1051×975，仅标题+五按钮组件）；②`index.html` 主菜单背景换成游戏自身的深色渐变，`.menu-image-wrap` 以 `width:98vw; aspect-ratio:1051/975` 大幅放大铺满视口；③脚本自动测量 5 个按钮在裁剪图中的精确行列范围，热区改为实测百分比（left:19.7%/width:63.5%；b1 top:43.8% 起，各按钮间隔约 10%），点击热区与图中按钮像素级对齐；④按钮功能：开始游戏→`goLevels()` 跳关卡选择，读取存档/成就/符咒图鉴/设置→`toast('即将上线')`。
- **为什么**：用户先要求按 UI.png 1:1 还原主菜单；CSS 复刻版被反馈「不够还原」，改为原图打底+热区方案；后又反馈原图含白底不干净、组件偏小，故抠底只留组件并放大。
- **后果**：抠图后按钮内部完整（各带透明率仅 6-10%，为元素间隙）；脚本语法校验通过；热区位置直接源自源图逐像素测量，天然对齐。
- **文件**：`index.html`、`images/UI_cut.png`（新增）、`scripts/cut_ui.py`（新增）

### 【8.15·关卡选择UI】关卡选择界面替换为 levels.png 背景 + 动态网格（index.html）
- **改了什么**：①参考主菜单 UI 方法，用 `images/levels.png` 作为关卡选择界面背景图，在其上叠加热区（返回主页、商店按钮）和动态覆盖文字（`lvProgress` 显示已解锁数/50，`lvCoins` 显示实际金币数）；②关卡网格由 5 列改为 **10 列 × 5 行**（与图片一致），CSS 按钮样式全面改为金/褐色系——解锁=金框褐底金色字（`background: linear-gradient(180deg, #5c4a3a, #3d2a1a)`，`border-color: #c9a96e`），未解锁=深褐框深褐底灰色字+🔒锁图标，Boss 关（3/7/11/15/19/23/27/31/35/39/43/47/50）额外加金色边框光晕+外层伪元素金框（`box-shadow: 0 0 14px rgba(255,215,0,0.45)`）；③`goLevels()` 额外更新 `lvProgress.textContent` 和 `lvCoins.textContent`；网格覆盖层百分比位置基于源图目测估算（left:13%/top:37%/width:74%/height:59%）。
- **为什么**：用户要求关卡选择界面按 `levels.png` 还原，且进度/金币需动态、关卡状态需按实际解锁情况渲染。
- **后果**：脚本语法校验通过；截图工具因 file:// 协议限制无法有效预览，需浏览器实测对齐。
- **文件**：`index.html`

### 【8.15·牛Boss接入】第11关「牛boss」贴图替换为生肖牛（index.html）
- **改了什么**：第 11 关「牛boss」的 boss 格（path + cells 两份，c:4/r:6，v:2500）加 `"z": "ox"`，渲染为 `images/bosses_3d/boss_ox_preview.png`（Low Poly 3D 牛立绘，80px）。
- **为什么**：用户要求把第十一关 Boss 贴图换成牛（生肖牛与关卡名「牛boss」吻合）。
- **后果**：Node 解析验证 z 字段正确、脚本语法 OK；牛的 3D 预览图为首批已生成资产，无需等待自动化。
- **文件**：`index.html`

### 【8.15·续】Boss 立绘放大 + 主角/怪物美术资源接入（index.html + images/chars/）
- **改了什么**：①**Boss 渲染纯分支化**——`tileIconHTML` 重构为严格分支：有 `z` 时只返回 `<img>`（彻底删除该格 CSS 柴犬 HTML），无 `z` 时保留完整柴犬；原实现是柴犬始终渲染再条件追加 img，存在 DOM 冗余。②**Boss 图片放大**——`.boss-img` 由 70px → **80px**，translate 微调居中。③**主角 CSS → 图片**——`heroTokenHTML` 由 `hero-hat/head/body/belt/legs` 等 5 个 CSS 几何块改为一行 `<img class="hero-img" src="images/chars/hero_front.png">`，`onerror` 自动回退原 CSS 几何块；新增 `.hero-img` 样式（`inset:0; object-fit:contain`）。④**角色图拆分**——从 `character_enemy.png`（1536×1024，5列×4行）裁剪：第1行 5 个主角（front/left/back/right/action）存 `images/chars/hero_*.png`，第2行 5 个怪物（minotaur/goblin/skeleton/ghost/plague）存 `images/chars/mob_*.png`（待后续接入怪物格）。
- **为什么**：用户反馈 Boss 图片不够大、要求彻底移除这两关的 CSS 柴犬；同时提供主角怪物合图，要求拆分后替换主角。
- **后果**：14 项冒烟测试全过；主角图由 307×256 原始 PNG 经 `object-fit:contain` 缩放到 48×60 容器，清晰度高、保留投影；回退逻辑完备（Boss 图失败回退柴犬，主角图失败回退 CSS 几何块）。
- **文件**：`index.html`、`images/chars/hero_*.png`、`images/chars/mob_*.png`

### 【8.15】生肖Boss 3D 低多边形模型接入：第3关狗 / 第7关鼠（index.html + images/bosses_3d/）
- **背景**：为 12 个生肖 Boss 建立 3D Low Poly 模型流水线（云端 hy-3d 图生 3D，三角面风格，每日限额 5 个）。当日完成 5/12（鼠/牛/虎/兔/龙，预览图 + GLB/OBJ 模型双交付），剩余 7 个（蛇/马/羊/猴/鸡/狗/猪）由自动化「生肖Boss 3D模型续生成」每日 00:10 续跑。
- **改了什么**：①**Boss 格数据升级**——path 数组的 boss 格新增 `z`（生肖标识）字段：第3关「守门犬」（v=276）→ `z:"dog"`、第7关「鼠boss」（v=6000）→ `z:"rat"`，path 与 cells 双份数据同步写入。②**渲染层升级**——`tileIconHTML` 检测到 `p.z` 时渲染 `images/bosses_3d/boss_<z>_preview.png` 作为 Boss 立绘（新增 `.boss-img` 样式：70px 等距立绘 + 投影），图片缺失/加载失败时 `onerror` 移除自身、**自动回退原 CSS 柴犬**，旧关卡（无 z 字段）零影响。③**狗 Boss 占位**——今日额度已尽、狗 3D 图未生成，暂用原设计图 `boss_dog_preview.png` 占位，明日 3D 图生成后同名覆盖自动升级（自动化下载逻辑为直接覆盖写，已验证不跳过）。
- **为什么（用户驱动）**：用户确认 Boss 用 PNG 立绘嵌入（游戏为 CSS 等距 2.5D，非真 3D 引擎，直接嵌 GLB 体积/成本不划算），指定第3关狗、第7关鼠先行替换验证效果。
- **后果（已验证）**：Node DOM 桩 14 项全过；LEVELS 解析确认两关 boss 的 z 字段正确、其余关卡无 z 自动回退。
- **文件**：`index.html`、`images/bosses_3d/boss_dog_preview.png`

### 【8.13·晚₃·小修】修「下一关/返回」不跳转的 bug（index.html）
- **改了什么**：结算/选择弹窗 `#modal` 是全局固定层（z-index 99），原 `show()` 切屏函数只切换各 `.screen` 面板、从不收弹窗 → 通关后点「下一关/返回选关」，界面已切到出战准备/选关但被结弹窗盖住，看起来像没反应。修复：`show()` 里切屏前先 `$("modal").classList.remove("show")`，一举覆盖 `goMenu`/`goLevels`/`tryLevel` 所有导航。
- **后果**：Node DOM 桩 14 项全过（新增「切屏收起弹窗」用例）；语法通过。用户补立契约：**小修小补也单列记录**。

### 【8.13·晚₂】操作反馈强化：新增通告横幅动画 · 精英数值临格揭晓 · 战斗比对可视化（index.html）
- **改了什么**：针对"反馈不明显"做了四类表现层强化。①新增**中央通告横幅 `announce()`**（顶中央大字卡 + 弹入/淡出动画 + 按类型配色）：增益格显示「▲ 增益 +N / 当前数值」绿色横幅、宝箱显示「📦/💰/🎲 宝箱 +N」金色横幅、诅咒宝箱显示红「☠ 诅咒」横幅；同时落地格整体弹跳（`popTile`）+ 玩家数值徽章弹跳（`bumpHeroVal`）。②**宝箱开箱**在通告横幅基础上加金色强调。③**精英怪数值临格揭晓**：原全程显示「?」，现改为距玩家 ≤1 格时揭晓真实数值（3D 视角 `.mob-val` 与 2D 地图 `.mcell` 同步），金色光晕提示，远离后恢复「?」。④**战斗比对可视化 `showBattle()`**：遭遇小怪/精英/Boss 时弹出「你的 N vs 敌方 M → 胜!/败」横幅（胜绿/败红），敌方格子同时套上绿/红光晕；鼠 Boss 宝箱不足时单列原因。
- **为什么（用户驱动）**：增益、宝箱获得提示不明显；精英全程「?」缺信息；与所有敌人比大小的过程看不清。用户建议用"简单动画"类思路解决。
- **后果（已验证）**：Node DOM 桩整机测试 13 项全过——新增「精英 ≤1 格揭晓 / 远距恢复 ?」用例；语法检查通过。注：战斗横幅为**非阻塞**式（不打断移动主流程），避免重构 landOn 异步化带来的游戏状态风险。
- **文件**：`index.html`

### 【8.14】编辑器：难度引擎改风险制 + 格子路径分析 + 第2关权威源（editor.html）
- **改了什么**：①**难度引擎重设计**——从「加权得分」（路径/数值/骰子/敌人/结构/Boss 六项系数）改为**风险制**：难度=输掉的风险。两项风险：`骰子/行动点风险`（权重0.7，最短可行路径到达概率越低越难，数值余量紧时按 `slackAmp` 放大）+ `卡死风险`（权重0.3，被数值卡死的岔路比例 + 死胡同折算）。关键点：**骰子充裕 + 数值必过 → 难度 0**（满足"第1/2关设计成 0 难度"的意图）；无解（无可行路径 / 骰子最大行动点仍不够）→ 强制 95+。`simulatePath` 新增 `minFree`（途中各门槛最紧余量）支撑"最紧可行余量"展示。②**格子路径分析**——新增「到达本格 · 路径分析」面板：点任意格子，枚举从起点到该格的所有简单路径，逐条累计数值（增益+/击败敌人数值全额+宝箱数值），输出**最小/最大到达数值、期望数值、期望行动点**（及按均值3.5折算的期望耗骰），被卡死的路径单独标红说明原因。③**第2关权威源**——`levels/level_2.json`（新手教程，含分叉）内嵌为 `LEVEL_SOURCE[2]`，关卡库缺第2关时自动补齐；面板加「↺ 恢复第2关 = level_2.json」一键覆盖库内第2关并载入。
- **为什么（用户驱动）**：①原引擎把"路径长、有怪、有 Boss"都算难度分，导致**故意把骰子做得充裕、数值必过 Boss 的第1/2关仍算出高难度**——难度应只衡量真实失败风险；②画好图后想预判"走到某个格子玩家数值大概多少、耗多少行动点"，以便调数值；③编辑器库里的第2关与 `levels/level_2.json` 不一致，后者才是权威版本。
- **后果（已验证）**：Node DOM 桩测试——示例线性关（dice4/4步/数值必过）**难度=0**、hard 关（dice3/8步）难度15；路径分析：到 Boss 格 min=max=期望=14 数值 / 4步，到小怪格 12 / 2步；内嵌 `LEVEL_SOURCE[2]` 与 `levels/level_2.json` 逐字节一致；语法检查通过。**补骰修复**：初始版本只按初始骰子算到达概率，把 level_2 算成 11；用户指出第2关沿路宝箱会补骰（`(7,5)`骰子宝箱+2、`(1,2)`骰子宝箱+1、`(6,7)`数值宝箱+20），改为**每条路径有效骰子=初始骰子+途中骰子宝箱/骰子格之和**，到达概率取所有可行路径的最大值。实测 level_2 = **3**（最优路线14步/有效骰6，到达概率96%，两条路径数值 148/166 均>Boss 132）——接近 0，属"极简"；仍有 4% 骰子失败率与最紧门槛余量+4（舒适线+5）的小警告，若需严格 0 可加骰至6或放松 comfortSlack。
- **文件**：`editor.html`

### 【8.14·修】编辑器写回丢失岔路 · 游戏分岔引擎重写（editor.html + index.html）
- **改了什么**：①**编辑器写回 bug**——`editorToGameLevel` 写回游戏本体时只取 `ed.path`（BFS 主路径），把存在 `ed.cells` 里的岔路数据丢光，导致含岔路的关写回后变纯线性。改为 `const cells=ed.cells||ed.path`（全图优先，线性关退化 path），有岔路时正常写出 `cells`。②**游戏 `buildForkGraph` 重写**——原实现只支持**单个**分岔（主路+一条绕行链，`op=M[0..j]+D+M[j+1..]`），多岔路/嵌套分岔的格子被整体丢弃。改为**结构化绕行遍历**：主路径沿 BFS 最短链；主格旁的非主路邻格=绕行入口→分岔 `branches=[主路,...绕行]`；绕行链按邻接结构递归走到邻回主路（汇合）为止，汇合尾回连最近主路格（按距Boss距离排序），支持多岔/子分岔/汇合/死胡同。③`pathEdges` 回退分支加 Boss 保护（Boss 可能不在 path 末尾，避免画出一条指向下一放置格的假连线）。
- **为什么（用户驱动）**：用户在编辑器画的第3关（含岔路）"写回游戏本体后不一致，所有岔路都被取消了"。
- **后果（已验证）**：Node DOM 桩——level_2 图（2 岔路）25/25 格可达、主路与绕行链均到 Boss、无死链；合成"主路+双绕行"图 16/16 可达、2 个分岔、两条绕行各自走到汇合回主路→Boss；编辑器写回模拟：分岔关写出 `cells`（14 格 > 主路 9 格）✓、线性关不写 ✓；index.html / editor.html 语法检查通过。修复后用户需在编辑器中对该关点「写回当前关」重新导出一次。
- **文件**：`editor.html`、`index.html`

### 【8.14·修2】一次性格子防重复 · 相机按移动方向朝向 · 编辑器文件导入（index.html + editor.html）
- **改了什么**：①**一次性格子**——`landOn()` 顶部加守卫：非 Boss 格若已在 `doneSet`（增益/宝箱/普通怪/精英怪/金币/骰子被消耗过）则直接返回，重复经过不再重复给奖励。②**相机朝向修复**——`headingR()` 原本恒按"沿路径指向 Boss 的方向"（`peekNextIndex`）朝向，走进死路后往回退格时镜头会反向翻转；改为**优先按实际移动方向**朝向：`moveTo` 记录 `lastMove={dc,dr}`，未移动/初始时退回路径方向。③**编辑器导入改文件**——删除「导入单关JSON」的 `prompt()` 粘贴导入，改为隐藏 `<input type="file">` + `importJSONFile()`（FileReader 读关卡 JSON，自动解析地图 `cells||path` 与 id/名称/起始数值/骰子/增益并载入编辑器）。
- **为什么（用户驱动）**：①宝箱/增益/怪可反复经过重复领取，属严重漏洞；②死路后退视角反向，不便于操作；③粘贴 JSON 不便，希望直接上传关卡文件自动读取。
- **后果（已验证）**：语法检查通过；headingR 实测前进/后退方向角相差 180°（相机跟随移动方向）；`landOn` 一次性守卫、`lastMove` 写入、编辑器 `importJSONFile`/文件上传均在位。
- **文件**：`index.html`、`editor.html`

### 【8.14·修3】装备栏可视化 + 遭遇战计算过程展示（index.html）
- **改了什么**：①**关卡内装备栏**——`screenGame` 统计栏下方新增 `loadout-bar`：1 个武器槽（金色描边）+ 3 个符咒槽，`startLevel` 时 `renderLoadoutBar()` 按 `loadout.w`/`charms[]` 填充图标+名称，空槽置灰显示"无武器/符咒·空"，悬停 title 显示效果描述。②**遭遇战数值重算 + 过程展示**——新增 `battleCalc()`：每次遇敌时以 `effVal()` 为基础**实时重算**（狗符咒不预置在常驻数值里，只在遇敌那一刻生效），并生成计算文案 `["基础 N","狗符咒 ×1.4 → M"]`；`showBattle` 新增 `calc` 参数，横幅副行展示"基础 10 → 狗符咒 ×1.4 → 14 · 胜!"，Boss 的怯战 debuff 也追加步骤。③小怪/精英/Boss 三处战斗全部改用 `battleCalc()`。
- **为什么（用户驱动）**：①装配了狗符咒但关卡内无任何装备展示，战斗中也没有计算过程，玩家看不出符咒是否生效；②要求狗符咒效果只在遇敌时重新计算并展示给玩家看。
- **后果（已验证）**：Node 测试——带狗（val=10）→ cv=14、calc=[基础 10,狗符咒 ×1.4 → 14]；不带狗 → cv=10、calc=[基础 10]；`renderLoadoutBar` 调用、`showBattle(cv,ev,label,win,calc)`、装备栏 HTML 均在位；语法检查通过。
- **文件**：`index.html`

### 【8.14·修4】全游戏背景图 + 编辑器难度引擎接入武器/符咒（index.html + editor.html）
- **改了什么**：①**背景图**——`body` 底层加 `images/background.png`（`cover` 居中不重复），上方叠原径向渐变作照明 + 半透明暗色覆盖保证 UI 可读；所有界面（主菜单/准备/关卡/结算）共用。②**编辑器出战装备模拟**——新增「出战装备模拟」面板（1 武器下拉 + 3 符咒槽下拉，localStorage 持久化），难度/数值/行动点全部按此计算：骰面分布泛化 `dieWeights`/`calcReach`（诅咒骰每骰+2、双生骰取双掷大者、幸运骰前2骰1~2重掷、掠夺者匕首补1骰）；`simulatePath` 按装备累计数值（腐朽之刃临时+50逐格-5、贪婪镰刀每杀+5、鼠符咒击败+30%、狗符咒战斗值×1.4、牛符咒首次失败救回并照常击败吸收且耗1骰）。难度面板显示「按装备模拟」，路径分析同步按装备判定并提示。
- **为什么（用户驱动）**：①希望用现有背景图统一整个游戏视觉；②编辑器算难度/数值/行动点是否通过时，必须考虑玩家实际携带的武器和符咒（否则误判关卡难度）。
- **后果（已验证）**：Node 桩——编辑器无装备 level_2=3（基线不变）、狗符咒余量+4→+8、诅咒骰+狗→难度0/到达100%、双生骰→100%、腐朽之刃+鼠余量+19；牛符咒：数值不足路径 无牛=卡死100 / 有牛=可过0（救回后正确吸收敌人数值、有效骰-1）；背景图文件存在、`body` 引用到位；两文件语法通过。
- **文件**：`index.html`、`editor.html`

### 【8.14·修5】第5关补回缺失格 (2,6)（index.html）
- **改了什么**：用户反馈第5关"从(3,6)走不到(2,6)"。核查：编辑器导出 `level_5.json` 与游戏 LEVELS 的 cells 均为 32 格，**(2,6) 根本不存在**（(3,6) 仅连 (3,7)/(3,5)，移动逻辑正常），属地图漏画。在游戏第5关 `cells` 补入 `{c:2,r:6,t:"gain",v:8}`（连通 (3,6)↔(2,6)↔(2,5)，使 (3,6) 成为分岔：主路 (3,5) / 岔路 (2,6)→(2,5)），并同步写出 `levels/level_5.json` 供导入编辑器。
- **为什么（用户驱动）**：游戏里从 (3,6) 无法走到 (2,6) 格。
- **后果（已验证）**：(3,6) 与 (2,6) 连通、33/33 格可达、Boss 可达、语法通过；(3,6) 变为分岔点（branches=[(3,5),(2,6)]）。
- **文件**：`index.html`、`levels/level_5.json`

### 【8.14·修6】移动邻接改全网格 · 岔路/反向可互走（index.html）
- **改了什么**：①**移动邻接改为全网格邻接**——`buildAdjacency` 不再用 `pathEdges`（遍历链的 `next/branches`），改为遍历 `LEVEL.path` 两两检查曼哈顿距离=1 建边（双向）。**只要两格相邻就走得了**，不受 `buildForkGraph` 遍历链是否覆盖此边影响，支持岔路间互走、反向退格。②`shortestNodeDistance` 也改用 `adj` BFS，保证"距Boss"与实际可达一致。遍历链（`next/branches`）仍用于镜头朝向、连线渲染。
- **为什么（用户驱动）**：第5关 `(1,3)` 走不到 `(2,3)`（实测相邻却不连通）。根因：`walkDetour` 全局 `done` 集合使 `(2,3)` 被 `(4,2)` 岔路"抢先消耗"，`(1,3)` 的岔路判定 `off=[]` 故无边。移动不该被遍历链约束。
- **后果（已验证）**：`(1,3)↔(2,3)` 连通 ✓；33/33 格可达、Boss 可达 ✓；语法 OK。
- **文件**：`index.html`

### 【8.14·修7】生肖Boss过关符咒弹窗 · 失败弹窗显示基础与狗加成双数值（index.html）
- **改了什么**：①**Boss关符咒获取弹窗**——新增 `newCharm` 状态，`endGame` 胜利且首次解锁狗/鼠/牛符咒时记录（`newCharm=cid`），通关弹窗（`sync` 的 win modal）插入「获得符咒」高亮卡片（icon+名称+效果描述+「已解锁，可在出战准备中装入符咒槽」提示），紫色渐变边框醒目。`startLevel` 重置。②**失败弹窗双数值**——`battleCalc` 返回值补 `base`；小怪/精英/Boss 失败原因改为显示 **基础数值 + 狗符咒加成后数值**（如"被 45 小怪挡住——你的数值 20（狗符咒加成后 28）"），给玩家判断是否该攒数值/换路。
- **为什么（用户驱动）**：①过了金色边框的生肖 Boss 关（如第3关）应明确弹出"获得符咒+效果"，方便后续使用；②带狗符咒失败时，弹窗要同时给当前值和狗加成值，作为判断依据。
- **后果（已验证）**：语法通过；`newCharm` 声明/重置/赋值、win modal 的 `charm-gain` 卡片与 CSS、失败文案 `bc.base + 狗加成` 均在位；`battleCalc` 实测 dog val=10 → base=10/cv=14、无狗 → base=10/cv=10。
- **文件**：`index.html`

### 【8.14·修8】编辑器关卡库岔路丢失修复（editor.html）
- **改了什么**：①**`normalizeGameLevel` 保留全图**——原来只用 `lv.path`（BFS 主路径）并丢弃 `lv.cells`，导致「📂 导入游戏本体」时库内所有关被覆盖成"仅主路径"的线性版，手存带岔路关卡再点开就只剩主路。改为 `cells=(lv.cells||lv.path||[])`，有全图时同时写出 `cells`。②**`importGameFile` 改合并**——不再无条件覆盖库内已有 id 的关（跳过已存在者，避免覆盖手存编辑），toast 显示"新增 N / 跳过 M"。
- **为什么（用户驱动）**：编辑器关卡库里之前手动存入的关，过一会儿（导入本体后）再点开，岔路全被取消。
- **后果（已验证）**：语法通过；`normalizeGameLevel(level_5)` 导入后 `cells` 保留全图（32格 与 path 相同 → 有岔路时 33>主路）且不再丢 `(2,6)`；二次 add 同一关跳过、库计数不变。注：`levels/level_5.json` 现为用户最新导出（32格、无 2,6），若需恢复 2,6 请重新写入。
- **文件**：`editor.html`

### 【8.14·新】鼠Boss特殊效果：击杀敌人→Boss成长20%（index.html + level_7.json）
- **改了什么**：①**机制**——新增 `bossGrowth` 状态（进关重置）；新增 `bossGrow(v)`：仅当本关 Boss 生肖为「鼠」时，每击败一个敌人（含牛符咒救回），`bossGrowth += round(敌人值×0.2)`，并以 tile 浮字+toast 提示"鼠Boss成长 +X（累计+Y）"。Boss 判定改为 `need = p.v + bossGrowth`，取代原"需开2个宝箱"占位机制；胜负横幅/文案显示「初始+成长」。②**Boss数值同步显示**——`bossGrow` 内调用 `updateBossDisplay()`：每击败一个敌人，3D 格 `.boss-val` 与 2D 地图 `#m{idx}` 上的 Boss 数值立即更新为「初始+成长」（如 6000→6912）。③**弹窗**——点开第7关（鼠Boss）进入出战准备时，`showBossSpecial()` 弹窗说明："每战胜一个敌人，其20%数值加到鼠Boss上；每杀净赚80%"，附初始Boss值，点"知道了"关闭。④**数值**——鼠Boss 初始值定为 **6000**（最终≈6000+1048=7048，狗符咒玩家战斗值≈7426，裕度≈380）。
- **为什么（用户驱动）**：为第7关鼠Boss增加"敌人20%数值加到Boss"的特殊效果，并需在打开该关时弹窗告知玩家。
- **后果（已验证）**：语法通过；`bossGrowth` 声明/重置、`bossGrow` 在 mob/elite/牛符咒三处调用、boss 判定 `need=p.v+bossGrowth`、`showBossSpecial` 弹窗及 `tryLevel` 触发均在位；模拟击杀 30/782/1450/2300 → 成长 6+156+290+460=912，最终需要 6000+912=6912；`updateBossDisplay` 桩测试：成长 912 后 3D `.boss-val` 与 2D `#m` 均显示 6912。
- **文件**：`index.html`、`levels/level_7.json`

### 【8.14·修9】Boss数值移出贴图遮挡 · 鼠符咒额外加成提示（index.html）
- **改了什么**：①**Boss 数值放贴图下方**——`.boss-img` 原是 `position:absolute` 悬浮在 boss-entity 上（z-index 4），把 `.boss-val` 数值盖住；改为文档流（`display:block`，无 absolute），flex 列布局下数值自然排在图片下方。②**鼠符咒额外加成提示**——新增 `ratBonusTip(v)`：击败敌人（小怪/精英/牛符咒）时，若有鼠符咒，显示浮字"鼠 +X" + toast"🐀 鼠符咒：额外吸收 +X"，让玩家感知到击败敌人后的额外数值（原只有合并后的总吸收 +g，无法区分鼠符咒贡献）。
- **为什么（用户驱动）**：①Boss 3D 贴图把数值挡住了，看不清；②鼠符咒效果生效但玩家感知不到额外数值加成。
- **后果（已验证）**：语法通过；`.boss-img` 已去 absolute、数值随 flex 排在下方；`ratBonusTip` 定义并接入小怪/精英/牛符咒三处；桩测试：带鼠符咒杀 45 → 浮字"鼠 +14" + toast"鼠符咒：额外吸收 +14"；不带鼠符咒无提示。
- **文件**：`index.html`

### 【8.14·修10】主角数值消失修复 · 精英临格揭晓改坐标判定（index.html）
- **改了什么**：①**主角数值消失**——根因：`images/chars/hero_front.png` 不存在，主角 `<img>` 的 `onerror` 原用 `parentElement.innerHTML='<div class="hero-hat">…'` 把**整个 hero-char 内容替换**（连 `hero-val` 数值徽章一起抹掉），导致主角头顶数值消失。改为 `this.outerHTML='…'`（只替换 `<img>` 自身），`hero-val` 徽章作为兄弟节点保留；图片缺失时显示 CSS 低多边形小人 + 头顶数值。②**精英临格揭晓**——揭晓判定从 `shortestNodeDistance(idx,i)<=1`（依赖 `adj`）改为**直接用坐标** `Math.abs(cur.c-p.c)+Math.abs(cur.r-p.r)<=1`，不依赖邻接表/遍历链，更稳；3D `.mob-val` 与 2D `#m` 同步显示数值并切换 `revealed` 高亮（参数已修正为 `near`）。
- **为什么（用户驱动）**：主角没有数值显示了；精英怪靠近一格也不显示数值。
- **后果（已验证）**：语法通过；`heroTokenHTML` 的 onerror 为 `outerHTML`、`hero-val` 徽章保留；精英揭晓用坐标判定，桩测试同格/相邻=真、相隔3格=假；3D/2D 揭晓均更新 textContent。
- **文件**：`index.html`

### 【8.14·修11】取消通关金币奖励（index.html）
- **改了什么**：移除通关弹窗里的金币奖励——原 `sync()` 胜场分支 `coins += 10 + LEVEL.id*2` 及弹窗文案"获得金币 +N"一并删除。现在金币**仅来自宝箱**（金币宝箱 `rt="coin"`），通关本身不再发金币。
- **为什么（用户驱动）**：用户不知道有通关奖励，要求取消（金币应只作为关卡内宝箱/探索奖励，而非通关白给）。
- **后果（已验证）**：语法通过；`const reward=10+LEVEL.id*2` 与"获得金币 +"均无残留；无悬空 `reward` 引用。经济影响：第1~8关全清通关不再得 152 金币，仅剩宝箱 145 金币（武器售价 100~150），武器购买需靠重复刷宝箱/权衡。
- **文件**：`index.html`

### 【8.14·修12】武器购买分阶段解锁（index.html）
- **改了什么**：每个武器新增 `unlockLv`（需通过第 N 关后才可购买）。出战准备面板：未达解锁条件的武器显示"🔒 通过第N关解锁"并置灰（`.pw.locked`，灰度+禁点）；`prepWeapon` 加守卫，未解锁购买弹 toast 提示。解锁顺序：腐朽之刃(9) → 幸运骰(12) → 贪婪镰刀(15) → 双生骰(18) → 掠夺者匕首(25) → 诅咒骰(30)。
- **为什么（用户驱动）**：限制武器购买时机——如"通过第九关才能购买腐朽之刃"，其余为待解锁状态，把武器变成进度奖励。
- **后果（已验证）**：语法通过；`unlockLv` 入列、renderPrep 锁定态、prepWeapon 守卫、`.pw.locked` CSS 均在位；桩测：unlocked=9 无武器可买、=10 开放 blade、=31 全部开放。
- **文件**：`index.html`

### 【8.14·修13】死路掉头不耗行动点（index.html + editor.html）
- **改了什么**：①**游戏移动**——`moveTo` 中当**当前格是死胡同（`adj[idx].size===1`，唯一相邻格）**时，退回该唯一相邻格**不扣行动点**（原路返回、非新探索）；其余一切移动（含岔路返程、回访、进入新格）照常耗 1 点。曾误改为"所有已访问格免费"，已回撤（`visitedSet` 移除）。②**编辑器**——`findBranches` 的岔路往返行动点按**单程 `格数`**计（死路掉头免费，去返不再双倍），补给路线分析到达概率随之修正。
- **为什么（用户驱动）**：从死路掉头没有实质性地移动到新格，不应耗行动点；但"从一个格子移动到另一格子"（岔路返程/回访/探索）都该耗点。
- **后果（已验证）**：两文件语法通过；`visitedSet` 无残留、`deadEndRetreat=adj[idx].size===1` 规则在位；死胡同格退格免费、普通移动 -1AP；编辑器死胡同岔路 AP=单程格数。
- **文件**：`index.html`、`editor.html`

### 【8.14·修14】牛Boss(第11关)特殊效果：Boss前一格豪赌+500（index.html）
- **改了什么**：①**移除通用"牛Boss×1.5"**——原 `if(zz==="牛")need=Math.ceil(p.v*1.5)`（v=2500→3750，用户误以为套用第7关鼠Boss规则）。现牛Boss**需求=自身数值**（2500）。②**实现第11关专属特殊**——新增 `gambleUsed` 状态；`landOn` 中当玩家停在**紧邻Boss格**（`adj[idx].has(lastIdx)`）且牛Boss关、未赌过、有骰时，弹出"🐂 牛Boss特殊效果"弹窗：**消耗1枚骰子，掷出>3 额外+500数值**（≤3 损失1骰）。③**进关说明**——`tryLevel` 选第11关时 `showBullSpecial()` 弹窗先说明牛Boss特殊效果（Boss数值+豪赌规则）；与鼠Boss(第7关)说明弹窗并列。④**流程顺序**——玩家到达前一格 (4,7) 先开骰子宝箱获得骰子，随后 `offerBullGamble()` 弹出询问是否消耗1骰检定；3D 格子底部保留金色 `🎲 赌+500` 徽标作预提示（2D 地图角标已按用户要求移除）。`bullGamble()` 结算后在弹窗内展示骰子判定结果（掷出X · >3/≤3 · 得失），点"继续"关闭。
- **为什么（用户驱动）**：第11关Boss显示2500但比对值偏高（×1.5所致），质疑是否误用第7关规则；明确"每个Boss关特殊效果不同"，第11关特殊=Boss前一格消耗1骰、>3得+500；要求关卡前有说明、前一格有提示并加载骰子判定。
- **后果（已验证）**：语法通过；`need=Math.ceil(p.v*1.5)` 已移除、牛Boss需求=2500；`showBullSpecial`/`offerBullGamble`/`bullGamble`/`gambleUsed` 在位；`bullPre` 桩测邻接Boss格=true、非邻接=false；3D 前一格标记与 CSS 在位；桩测 roll5→val+500/骰-1、roll2→无加成/骰-1。**弹窗不显示的修复**：根因是 `sync()` 在 `status==="playing"` 时无条件移除弹窗，导致豪赌/双生骰/幸运骰弹窗一闪而过；新增 `keepModal` 标志——这些刻意弹窗显示时置 true，`sync()` 仅当 `!keepModal` 才移除，`closeBull()`/选择后置 false。桩测：offer+sync 后弹窗保留 ✓、正常移动仍清理残留弹窗 ✓。注意：第11关前一格 (4,7) 现为骰子宝箱(+1骰)，打开后即可用该骰豪赌；如需改成普通格请在编辑器调整。
- **文件**：`index.html`

### 【8.14·新2】商店系统：选关页右上角 · 武器购买移入商店（index.html）
- **改了什么**：①**选关页顶栏**——右上角加「🛒 商店」按钮（金色胶囊样式 `.shopbtn`），其左侧显示金币（`#lvCoins`）。②**新增商店屏 `screenShop`**——`goShop()`/`renderShop()`/`shopBuy()`：武器购买全部移入（含价格/解锁进度 `🔒 通过第N关解锁`/已拥有态/金币不足提示），购买后金币实时刷新。③**出战准备改纯装备**——`renderPrep` 武器列表**只显示已购买武器**、符咒列表**只显示已获得符咒**（未购/未获得不显示，空时给提示）；`prepWeapon` 移除购买逻辑，仅装备/卸下。
- **为什么（用户驱动）**：商店放选关页右上角、金币在商店左侧；开局前只选已购武器/已获符咒，购买统一到商店。
- **后果（已验证）**：语法通过；选关页 `lvCoins`+`goShop()`、`screenShop`/`shopWeapons`、`goShop`/`renderShop`/`shopBuy` 在位；renderPrep 只渲染 ownedWeapons/ownedCharms；prepWeapon 无购买；桩测 shopBuy：解锁+金币足→扣款并拥有、未解锁→locked 提示。
- **文件**：`index.html`

### 【8.14·新3】传送门 + 陷阱格 + 孤立格放置（editor.html + index.html）
- **改了什么**：**编辑器**：①新增格子类型「传送门 portal」（属性面板填配对坐标 c/r，格上显示 ⛩ + →坐标）与「陷阱格 trap」（效果下拉：-数值/-骰子/回起点）。②**增益格允许负数**（填负数即"陷阱数值"，格上显示负值）——-数值类陷阱可用增益负值实现。③**允许放置孤立格**——`handlePath` 不再强制邻接（新格可不邻接任意格，提示但不阻止），`validateLevel` 对不连通的 portal 格不再报错，为后续跳跃/传送端点准备。导出/写回同步 portal.link 与 trap.eff/ra。**游戏**：`landOn` 新增 portal 分支（`teleportDepth` 防连环，免费传到配对门并递归触发目标格效果）与 trap 分支（-骰子/-数值/回起点，一次性）；增益负数正确扣数值并显示 "-N"；3D/2D 图标（⛩/☠）与负数增益显示。
- **为什么（用户驱动）**：为第2幕(11-20关)引入传送门与陷阱格玩法；-数值陷阱复用增益负数；孤立格为后续跳跃功能做准备。
- **后果（已验证）**：两文件语法通过；桩测：传送门落地→idx 跳到配对格并触发其增益、trap dice -2、trap value -20、负数增益 -8；编辑器孤立格放置/portal link/trap eff 导出在位。注意：编辑器难度引擎暂把 portal 当普通格（未建模传送边），trap 无数值影响，后续可增强。
- **文件**：`editor.html`、`index.html`

### 【8.14·新4】难度引擎重构：连续胜率制 + 可视化（editor.html）
- **改了什么**：①**告别一刀切100**——旧引擎有硬性"过关/失败"边界（数值差一点、符咒没选、死路卡住 → 直接判 100），极不灵敏。改为**连续模型**：`难度 = 0.6×数值风险 + 0.4×行动点风险`。数值风险=数值达成度平滑曲线（缺口5%→10、25%→50、55%+→100，数值够=0）；行动点风险=到达概率连续值×余量放大。只有"全最大行动点都到不了"或"数值缺口>50%"才逼近100。②**宝箱/死路/符咒响应**——死路往返不计难度（免费掉头后仅耗进点）；宝箱/岔路补给计入数值达成（`valRatio=最高可达数值/Boss需求`）；换装备（狗符咒等）实时反映（无装备卡死=100、带狗=0）。③**可视化**——难度面板新增**数值达成条**（可达/需求 + 百分比 + 红黄绿）与**行动点达成条**（到达概率），风险构成改为「数值缺口/行动点」两项进度条，诊断文案更具体（"数值缺口 X%"、"主路全被中途怪卡死——可尝试切换装备"）。
- **为什么（用户驱动）**：引擎考虑因素太多、小改动(宝箱/符咒/死路)就总跳100，不够灵敏；要求重构且更可视化。
- **后果（已验证）**：语法通过；桩测——L2 基线=2、boss需求+10%→17、+20%→26、+75%→57（平滑不跳100）；死路需去拿宝箱→0（补给计入）；无装备卡死=100 / 带狗=0（符咒敏感）；陷阱/传送门格不破坏；岔路宝箱把+10%难度从17降到0。
- **文件**：`editor.html`

### 【8.14·新5】美术资源化：换皮=换文件（index.html + images/）
- **改了什么**：新增 `ART` 美术配置（`sky` 背景 / `board` 棋盘底面 / `tiles` 各格子 / `weapons` / `charms`，均为图片路径），配合 `artImg`/`iconHTML`/`applyArt` 渲染：**配置了图片就用图片，缺图/删图自动回退 CSS 或 emoji**。接入点：①**格子顶面背景**——`build3` 把 `images/tiles/*.svg` 作为 `.tile-top`（45° iso 顶面）的背景图（`background:url() center/cover no-repeat, 原渐变`，缺图自动回退渐变），图片**平躺在棋盘上、贴合相机视角**；有图时内容层只显示数值徽章（mob/精英/增益）。②**武器/符咒图标**——商店、出战准备、局内装备栏统一 `iconHTML(ART.weapons/charms, emoji)`（图片加载则显示、失败回退 emoji）。③**棋盘**——`build3` 加 `#boardImg` 覆盖棋盘底面。④**天空**——`applyArt()` 用 `ART.sky` 设置 body 背景（默认 `images/background.png` 不变）。生成了全部占位 SVG（`images/tiles/*.svg` 12、`weapons/*.svg` 6、`charms/*.svg` 3、`board.svg`、`sky.svg`）。
- **为什么（用户驱动）**：纯前端换美术麻烦；占位图初版是"竖立"方块不符合 45° 视角，改为顶面背景后平躺贴合。希望"换皮=换文件"（格子/武器/符咒/棋盘/天空均可单独替换）。
- **后果（已验证）**：语法通过；ART 配置/helper/applyArt/build3 boardImg/`tile-top` 顶面背景（boss 除外）/各面板图标资源化 全部在位；18 个 SVG 占位文件已生成；缺图自动回退 CSS/emoji。换皮：覆盖 `images/` 对应文件或改 `ART` 路径。注：占位 SVG 仅演示管线，可删 `images/tiles/*.svg` 恢复 CSS 低多边形。
- **文件**：`index.html`、`images/`

### 【8.13·晚】取消回合制 · 掷骰自由化 · WASD 相机朝向适配（index.html）
- **改了什么**：①**撤销回合制**——删掉「每回合只掷 1 骰」限制与「结束行动」按钮/回合计数/回合卡片，掷骰完全自由：玩家可一口气掷完 5 骰攒行动点，也可走几步行动点不够了再掷；行动点全局累积、不再论回合。状态卡由四卡（回合/行动点/骰子/距Boss）减为三卡。②**失败判定迁移**：原「结束行动时骰子+行动点耗尽判负」改为「每次落子后若行动点与骰子皆耗尽且未到 Boss 则判负」（无回合可"结束"了）。③**WASD/方向键按相机朝向适配**——第二关相机首次旋转后，视角里应是 W 前进，但原按键逻辑仍按旋转前的网格轴（要按 D 才前进），反操作；现把屏幕方向按相机 Z 轴旋转角 R 逆旋转回网格方向再移动，转角后 W 恒为视角前方、D 为视角右方。
- **为什么（用户驱动）**：①回合制限制了运营节奏，玩家想要更大的自由度——"既可以一下子把五个骰子都丢了攒行动点数，又可以每次丢完行动点数不够用了再丢骰子"；②旋转后按键与视角不一致，属于明显的反直觉操作。
- **后果（已验证）**：Node DOM 桩整机测试 12 项全过——新增"第二掷立刻放行（自由掷骰）"与"相机旋转后 W 前移 / R=0 保持朴素映射"两项；语法检查通过。此 pivot 把玩法从"回合资源统筹"改回"单局自由资源管理"，节奏更顺、更贴近大富翁手感。
- **文件**：`index.html`

### 【8.13】移动操控改点击地图 · 界面放大 · UI 美化 · 音效系统接入（index.html）
- **改了什么**：①**移除方向键（D-pad）实体按钮 UI**，改为**直接点击地图移动**——点击点相对主角的方位决定走向：点主角正前方=向前、点正右方=向右、点左/后方=左移/后退；实现上取「可达相邻格」中离点击点屏幕位置最近的一格移动（基于 `getBoundingClientRect` 真实渲染位置，天然适配 3D 旋转视角，无需逆变换）。2D 俯视地图格同样可点击移动。②**主界面与格子放大**：格距 T 52→64、格子显示 TW 48→60、俯视图格 M 42→54、镜头 ZOOM 1.08→1.16、场景高 556→640、容器宽 470→540，各格内容与棋子/怪/数值清晰可读。③**UI 美化**：状态卡片加左侧渐变强调条 + 行动点专属发光边框，回合/行动点数值变化时弹跳动画（`bump`/`bumpAP`）。④**音效系统**：Web Audio 合成（零外部文件）——掷骰/移动/增益/金币/宝箱/胜利/失败/按钮点击 8 种音，顶栏加 🔊 静音开关（localStorage 持久化）。
- **为什么（用户驱动）**：①点击方向本身就能替代实体按键，D-pad 冗余且占空间；②格子太小看不清内容；③行动点是核心资源，需要更强的视觉反馈；④「加一些基本音效」提升操作反馈。
- **后果（已验证）**：Node DOM 桩整机测试 11 项全过——sfx 在无 AudioContext 环境安全空转、掷骰加行动点、步耗 1 行动点、0 点移动被拦、点击地图选中正确相邻格、回合推进/未掷拦、BUMP 动画同步不抛错。语法检查通过。
- **文件**：`index.html`

### 【8.13】核心玩法 Pivot：大富翁强制走 → 回合制行动点自由移动（index.html）
- **改了什么**：①推倒「掷骰强制走满 N 格」的自动移动，改**回合制**——每回合掷 1 次骰子获得**行动点数**（AP），用**鼠标点击方向 / WASD / 点击相邻格子**沿路径逐格自由移动，每走 1 格消耗 1 点行动点；②**行动点跨回合累积**（结束回合不清空）；③新增绿色「结束行动」按钮进入下一回合，并**计回合数**（顶栏新增「回合」卡片 + 每回合 toast「第 X 回合」）；④**每回合限掷 1 次骰子**（掷过即禁用掷骰按钮，需结束行动才能再掷）；⑤新增方向键（▲◀▶▼）D-pad + WASD/方向键/空格(掷骰)键盘监听；⑥可移动的相邻格金色高亮（`.tile.reachable`）；⑦失败条件调整：结束行动时骰子与行动点均耗尽且未到 Boss → 失败（不再一次走完立即判负）。
- **为什么（用户驱动）**：用户明确要求改成回合制行动点体系——把「强制走满」的被动体验改为「每回合精心分配行动点」的主动资源规划，接近战术棋类；行动点可跨回合存，玩家可攒点做长程进击。
- **后果（已验证）**：Node DOM 桩整机测试全过——掷骰加行动点、本回合双击被拦、每步恰好 -1 行动点、非法/0 点移动被拦、分叉可自由选任一支、结束回合推进并重置掷骰标记、行动点跨回合累积、骰子耗尽后仍可用积攒行动点移动、到 Boss 判胜、耗尽判负。武器/符咒（腐朽之刃每步-5、双生骰、幸运骰、诅咒骰、牛符咒）在行动点语境下仍生效；分叉选择不再弹窗（自由移动天然解决）。
- **文件**：`index.html`

### 【8.10】敌人吸收机制回归全额 · 修复编辑器写回后数值不同步
- **改了什么**：①游戏 `enemyReward()` 普通怪吸收从 50% 改为 100%（与设计稿"击败敌人后自动获得敌人的数值"一致，精英怪本就全额）；②游戏 `step()` 补 elite 分支（原本完全没处理精英格→走过啥也不发生）；③增益/金币/骰子格改读每格 `p.v`（原本 gain 用全局 `LEVEL.gain`、coin 用随机、dice 固定+1）；④精英怪游戏中显示"?"隐藏；⑤编辑器导出 coin/dice 的 `v`、属性面板加金币/骰子数量输入。
- **为什么**：编辑器写回第1关后被精英怪挡住，排查发现：不是+6增益没生效（+6 生效了），而是普通怪只吸收50%导致数值链 4→9→怪8(只+4=13)→+5=18→+6=24→精英26 被挡（差2）。根因是游戏 `enemyReward` 的50%规则与设计稿"全额吸收"+难度引擎(按全额算)矛盾。
- **后果**：第1关 4→9→怪8(+8=17)→+5=22→+6=28→精英26(28≥26通过)→Boss42(54≥42通关)。游戏与难度引擎数值模型统一。全局变易一些（需复核已有关卡数值）。
- **文件**：`index.html`、`editor.html`

### 【8.10】地图编辑器 + 难度引擎独立工具（editor.html 重做）
- **改了什么**：废弃旧 editor.html 的节点-连线图模型（用户反馈"无法添加删除节点、棋盘乱七八糟"），按新规格重做为**画路径**模型独立单文件工具：①9×9 空白棋盘 + 画路径模式（正交邻接、禁止斜向/跳格、START/1/2/3 编号）；②格子类型：普通/增益/宝箱/普通怪/精英怪/Boss/起点（起点 Boss 唯一）；③参数面板：mob/elite/boss/gain 数值、宝箱奖励类型(数值/骰子/金币/Debuff)+数量手动填、精英怪游戏中隐藏但编辑器与引擎知道实值；④画分叉模式（fork→支线→rejoin 收束）；⑤实时统计（路径/各类型计数/分叉/起始数值/骰子）；⑥难度引擎——独立函数 `calculateDifficulty`/`calculateReachProbability`(D6 和分布)/`calculateValuePotential`(沿主路径模拟、**击败敌人吸收其数值**)/`validateLevel`，权重集中在 `DIFFICULTY_CONFIG`（路径20%/数值30%/骰子20%/敌人15%/结构10%/Boss5%），输出 0-100 分 + 1-5 星 + 子项压力条 + 诊断(到达概率/数值裕度/后半段敌人密集/分叉) + 目标难度对比 + 不可通关检测(骰子最大点数不足/数值无法击败 Boss)；⑦导出/导入 JSON 兼容游戏 `{id,name,startVal,dice,gain,path:[{c,r,t,v,rt,ra}],branches}`。
- **为什么**：手写 LEVELS 数据不可持续，需要可视化制作前 11 关乃至 50 关并直接生成游戏可读数据；难度梯度需要客观分数而非拍脑袋。
- **关键约束落实**：击败吸收敌人数值（核心规则）、精英隐藏但引擎知实值、武器/符咒本阶段不计入、权重集中可调、模块化(状态/渲染/校验/难度/IO 解耦)。
- **后果（待目检）**：单文件无依赖，桌面浏览器 UI。待真机验证画路径/设类型/设参/分叉/难度/导出全链路。
- **文件**：`editor.html`（重做，覆盖旧节点-连线版）

### 【8.10】低多边形色块风换肤（index.html）
- **改了什么**：①废弃 8.9 像素风（ASCII 精灵图 + 12 色调色板），改低多边形色块风——`.face` 从像素贴图改为纯色块 + 切面渐变高光（lit-from-top-left，`linear-gradient(135deg,rgba(255,255,255,.10),rgba(0,0,0,.20))` 叠底色）；②棋子/怪从 ASCII 像素改为纯 CSS 几何体（菱形 `transform:rotate(45deg)` + 2px 描边 + 切面渐变）；③数值文字加 `text-shadow:0 0 8px currentColor` 发光，敌我不同色（怪红/Boss 金/玩家蓝）——强化"数值对撞"核心视觉记忆点；④骰子从 3×3 像素点阵改为切面色块 + 42px 大数字；⑤调色板换为深蓝紫底 + 翠绿/珊瑚红/金/青蓝功能色。
- **为什么**：8.9 像素风是为正交 2D 设计的，放入 3D 透视场景产生"贴纸悬浮"违和感（8.10 稿第九节确认）。低多边形 + 切面高光天然适配伪 3D 透视，且纯 CSS 无图、AI 改 class/size 即改造型，修改成本低（呼应"AI 驾驭力"）。
- **后果（待目检）**：只动视觉层（CSS + spriteHTML/dieHTML/labV/build3/goMenu 五处），**难度引擎 PLAN/diceDist/genPath/genLevel/roll/step/sync 一行未碰**。DOM 桩未跑，待真机/浏览器目检观感。
- **局限/待补**：低多边形色板与 tile/角色几何形态细化（8.10 稿待确认 #8）尚未系统化；十二生肖颜色编码方案待落地（现仅狗符咒接入）。
- **文件**：`index.html`

### 【8.10】完整设计讨论稿引入 · 系统蓝图大幅扩充
- **改了什么**：引入 `docx/十二生肖闯关_完整设计讨论稿_0810.md` 作为新核心参考。在 8.2 框架 + 8.9 demo 引擎之上，定稿/扩出六大新系统蓝图：①DAG 分叉路径（四独立旋钮：密度/宽度/步数张力/嵌套，附 50 关节奏表）；②宝箱系统（数值/骰子双池，玩家主动开启，前期 70%+ 好结果→后期 debuff 反超）；③金币+武器系统（金币仅关外消费、单武器槽、跨维度效果、永久收集+局内限量）；④符咒设计维度收敛（早期数值修饰→后期规则破格，跳跃消耗等量步数）；⑤挑战模式（独立存档肉鸽，一命爬 50 关，全符咒可选）；⑥失败机制定案（不引入安全垫，失败即重试）。
- **为什么**：8.9 demo 验证了"骰子张力 + 数值门"骨架可玩，但诚实清单暴露"无岔路→门怪是氛围不是威胁、胜负纯由骰子决定"。8.10 稿系统性回应：用分叉把"资源取舍"显性化、用宝箱补决策密度、用武器/符咒做 build 纵深、用挑战模式拉重玩价值。
- **后果**：①设计蓝图从"骰子+数值两资源"扩为"骰子/数值/宝箱/武器/符咒/金币"多系统；②难度引擎需从单段改分段（分叉后多段拼接胜率，不推翻）；③开发优先级重排（见待办）；④待确认事项 8 条。
- **关键约束（勿破坏）**：分叉步数不等时"步数差≈内容价值差"；规则破格型符咒（跳跃）必须消耗等量步数；骰子与数值两资源紧张感必须同时存在。
- **文件**：`docx/十二生肖闯关_完整设计讨论稿_0810.md`（新增）

### 【8.9】AI 关卡生成流水线 v1 上线 · L3~L50 全部可玩（骨架版）
- **改了什么**：①genLevel(id) 生成器接入 index.html——种子随机（mulberry32，seed=id×7919+13，同一关永远生成同一张图，可复盘可调参）；②难度平面 PLAN：目标胜率 0.90→0.52 平滑下行 + 三条硬约束（胜率不升 / 步数不缩 / 骰子不减），逐关贪心搜索 (骰子数 D6, 步数 P)——难度来源从「拍脑袋参数」变成「骰子和分布 DP 精确反解」；③路径造型：自避随机走，直线/蛇形/回字混合随年代变化；④数值填配：绿格占比 78%、门怪=到达值的 50~95%（无岔路期=氛围压迫）、Boss=预算×(0.80→1.00)；⑤生肖 Boss 命名落地（L3 狗 → L46 猪，L50 年兽·终局）；⑥选关格提示改「AI草稿」，通关生成关同样解锁下一关。
- **为什么**：比赛要求 50 关 + AI 驾驭力。先让 50 关「全部可玩、数值可证」，再逐层加机制（岔路/符咒）。
- **后果（已验证）**：48 关全量审计通过——邻接/出界/重复格零问题、每道门到达即过、Boss 必可达可胜、胜率单调不升 / 步数单调不减 / 骰子单调不减（含 L2→L3 接缝）；L3/L25/L50 真机确定性输赢路径 + fuzz 全过。难度签名：77.9%(L3-21 平台)→72.1%→66.8%→62.0%→57.6%→50.0%(L48-50)，步数 15→32，骰子 5→9。
- **已知局限（v1 诚实清单）**：①无岔路 → 胜负纯由骰子和决定，门怪是氛围不是威胁（L2 同款问题放大）；②胜率量化粒度粗（同骰子数下 ±7pt/步），L3-21 出现 19 连平台——靠地形/门怪密度/Boss 系数做体验差异，数学难度相同；③符咒未接入，生肖 Boss 目前只是命名。→ 下一步按优先级：岔路生成（门怪变真威胁 + 决策回归）→ 符咒系统（RNG 对冲 + build）→ 平台期细分旋钮。
- **文件**：`index.html`

### 【8.9】美术基调定调 · 像素风全面换肤（index.html）
- **改了什么**：①定调文档 `docx/像素风美术基调.md`——限定 12 色调色板、禁圆角/渐变/柔光、3px 深描边 + 硬投影、精灵 8×8~16×16；②index.html 全量换肤：全局 border-radius:0!important、组件色全换调色板色；③引入 ASCII 精灵图渲染器 spriteHTML(map,px)（box-shadow 像素画），HERO/MOB/BOSS/FLAG 四张精灵入库，替换全部 emoji 图标（小怪/Boss/旗帜/骰子/按钮/锁关 → 精灵或纯文字）；④骰子改 72px 白方块 + 3×3 像素点数阵（dieHTML），掷骰=逐帧闪变定格；⑤主菜单加像素勇者大图。
- **为什么**：用户定调像素风。评审不看美术——像素风的意义是「最低成本换完成度感」；ASCII 精灵图存代码内，版本可管、diff 可读、AI 改字符=改像素（AI 驾驭力考点）。
- **后果（已验证）**：DOM 桩整机冒烟全过——精灵像素数与字符图一一对应、骰子点数阵 1~6 全对、主菜单/50 关格渲染正常、L1/L2 确定性输赢路径（恒 6 赢 / 恒 1 输）+ 随机 fuzz 均正常结算、localStorage 解锁持久化正确、emoji 清零。
- **待目检**：真机/浏览器看精灵观感，按反馈微调配色造型。
- **文件**：`docx/像素风美术基调.md`（新增）、`index.html`

### 【8.9】关卡 UI 重设计 · 信息降噪 + 掷骰动画（index.html）
- **改了什么（全部用户驱动）**：①小怪/Boss 标签改单行 emoji 图标（👹n / 👑n / 🚩起点），删掉格子底部厚度块与双行小字——修复「文字图标显示异常、格子看起来没对齐」；②删底部日志条与教学提示条；③统计栏只保留 剩余骰子 + 距 Boss（玩家数值由棋子 token 承担）；④操作栏改为 [地图][掷骰子][重新开始]，掷骰居中放大；⑤掷骰加滚动动画（75ms×6 帧闪变后定格弹显）。
- **为什么**：原 UI 信息过载、3D 标签双行小字渲染粗糙；掷骰是核心动作，需要仪式感与反馈。
- **后果（已验证）**：DOM 桩整机执行通过；删除项零残留、新增项全部在位。**待目检**：emoji 图标在不同机型上的渲染差异。
- **文件**：`index.html`

### 【8.9】游戏外壳 · 主界面 + 50 关选关 + 新手教程关（index.html）
- **改了什么**：新建游戏主入口单文件，三屏流转——主界面（标题 / 进度 / 玩法说明）→ 50 关选关格（锁定 🔒 / 可玩 / 已通关 ✓ 三态，金框 = 生肖 Boss 关）→ 关卡内 3D+2D 双视角引擎（沿用 level1-3d 原型）。进度 localStorage 持久化（通 N 解锁 N+1）。新增第 1 关新手教程：直线 9 步 (5,0)→(5,9)，教学 tips 随事件弹出（掷骰 → +2 → 数值门 → Boss）；原蛇形走廊关顺移为第 2 关。3~50 关占位「待生成」。
- **为什么（用户驱动）**：需要正式游戏骨架承接 50 关；第 1 关只教两件事——丢骰子前进 + 比大小。
- **后果（真实数据已验证）**：L1 门怪 6（6=6 平局教学）→ 门 8（12≥8）→ Boss16（16=16 恰好平局收尾，戏剧化）；9 步 / 4 骰通关率 94.6%（教学友好）。L2 77.9%。**难度第一坡度成立：94.6% → 77.9%**。注意：L1→L2 目前只有数值递进，机制递进（岔路 / 精英 / 符咒）从 L3 开始规划。
- **文件**：`index.html`（新增，主入口）

### 【8.9】视角原型 · 3D 跟随主视角 + 2D 俯视地图（level1-3d.html）
- **改了什么**：新增表现层原型——主界面 CSS 3D 跟随视角（视线始终朝前进方向，拐弯镜头随转，无引擎零依赖）；「地图」按钮切换 2D 俯视平面图看全棋盘。3D 视图自带视野压制：只看得清前方 6 格，更远显示 ?（0802 视野机制的首次落地）；2D 地图全信息。顺带修复 level1.html 棋盘容器 0 宽不渲染的 bug（已验证 382×382 正常显示）。
- **为什么（用户驱动）**：等轴 45° 静态视角「从左上往右下」看着奇怪；主视角往前走更有前进感，2D 留给玩家做全局规划。
- **后果（已验证）**：语法通过；镜头朝向序列全程 ≤90° 平滑转角、无大回旋。**待试玩验证**：billboard 标签可读性 / 拐弯镜头是否晃 / 手机上 40+ 个 preserve-3d 元素的性能。
- **设计含义**：①3D 有限视野 vs 2D 全信息的信息分层——未来可把 2D 地图做成消耗资源（侦察）；②3D 镜头反约束关卡形状：急弯多的关卡镜头频繁旋转，后续 AI 生成关卡需控制急弯频率。
- **文件**：`demo/level1-3d.html`（新增）

### 【8.3】第1关验证 Demo · 用户指定走廊关（level1.html）
- **改了什么**：按用户精确参数新建单关 demo：起点 (0,5)，15 步蛇形走廊至 Boss (9,5)；5 颗骰子、每回合 1 掷强制走满；普通格 +2 途经即收；小怪为数值门（6/16/16，不吸收，平局胜）；Boss 24；起始数值 4（策划补的缺省值，可调）。
- **为什么**：用户需要能玩的 demo 验证「纯骰子走廊 + 线性成长」手感，与 v2 棋盘岔路版形成对照实验。
- **后果（数值已验证）**：成长链确定性可解——门怪 6=6 平、16=16 平、22≥16，Boss 26≥24 余 2；唯一失败源 = 骰子总和 <15，蒙特卡洛 10 万局实测通关 77.9% / 纯运气败北 22.1%。**22% 运气败率对第 1 关是否过狠，待试玩拍板**。
- **文件**：`demo/level1.html`（新增）

### 【8.3】Demo v2 · iso 9×9 棋盘重做
- **改了什么**：推翻塔式 demo，重写为 45° iso 9×9 棋盘。外圈环 + 中心十字，46 格可走。起点 (0,0)，Boss 中心 (4,4) val18，狗符咒 (8,8)，3 个补给格，1 个精英 (4,7) val8 奖励 +2 骰，约 21 个小怪。起始数值 5，骰子预算 6。
- **为什么**：用户 pivot 到大富翁式棋盘形态（见下条），demo 必须同步重做以验证新规则手感能成立。
- **后果**：核心循环跑通——掷骰→自动走→岔路金色高亮选方向→落点触发→吸收滚雪球→上臂决战 Boss。验证了「臂=承诺」的自洽设计涌现成立。
- **新风险**：岔路密度不够（仅 4 个岔路口），N 多数对应单路时会退化成纯运气。需在 AI 生成约束里写「可达落点≥2 的岔路比例」。
- **文件**：`demo/index.html`（覆盖 v1）

### 【8.3】重大 Pivot · 塔式 → 9×9 iso 大富翁式棋盘
- **改了什么**：从「垂直塔、每层 2~3 位置选 1」改为「平面 45° iso 9×9 棋盘、大富翁式掷骰强制走 N 格、岔路口选方向」。
- **为什么（用户驱动）**：
  - 9×9 iso 色块棋盘即使无美术也好读，H5 友好
  - "掷骰走步"本就是大富翁母题，与骰子步数经济天然契合
  - 2D 路径决策比"每层 3 选 1"丰富
  - 12 生肖 = 棋盘上 12 命名格，主题落地更顺
  - 生成 50 张棋盘比生成塔层更能秀「AI 驾驭力」
- **搞坏的 3 样（策划指出）**：①步数张力曲线（线性→模糊）②主动权（强制落点剥夺选敌）③"50 关"无着落。→ 用「强制走 N + 岔路选择」全部解决：马里奥派对式，骰子定距离、玩家定方向、落点定触发。
- **后果**：规则整体重写（见「规则更新」条），demo 重做。

### 【8.3】规则更新 · Pivot 后定稿
- 骰子点数 N = 强制移动格数（不再有步数池）
- 每回合掷一次骰，必须走 N 格；到岔路口玩家选方向（决策核心）
- 只有**落点**触发，途经不触发（=大富翁规则）
- Boss 格**触碰即开战**（无论步数走没走完）
- 骰子预算 D（每关可掷次数）= 步数张力杠杆
- 平局玩家胜；吸收=全额；狗符咒=战斗有效值×1.2
- 跨关 = 50 张不同 9×9 棋盘，AI 生成

### 【8.3】Tower Demo v1 · 塔式实现（已被覆盖）
- 垂直塔，2 关。骰子预算制（掷骰得步数池，爬层耗步，步数结转）。每层 3 选 1，滚雪球，顶层 Boss。
- **P0 骰子规则第一版**（塔式）：骰子预算 D / 一次掷一颗 d6 / 步数池同关结转 / 爬层耗 C / 平局玩家胜 / 吸收全额 / 每层选 1 位置。
- **价值**：验证了「骰子管理 + 滚雪球 + Boss」核心循环可玩，为 pivot 后的规则重写提供了对照基线。pivot 后此版本被 v2 覆盖。

### 【8.3】设计评审 · 风险识别 + 新想法提案
- **策划解读**：三系统咬合——数值对撞=决策核心 / 骰子=张力来源 / 视野=信息设计 / 符咒=局间成长+build 差异化+难度兜底。框架扎实，核心循环 30 秒能懂、深度藏资源规划。
- **4 个风险**：
  1. 骰子 RNG vs 解谜确定性的根本矛盾——完美规划输给坏运气=致命挫败
  2. 横向免费=单层决策薄，深度全靠跨层规划
  3. 双败线（步数尽+数值败）且无兜底，偏硬核
  4. 金币标注局外养成但又说不做复杂系统→局内是死格
- **7 条新想法**：①骰子决策化 ②步数成本与位置挂钩 ③Boss=符咒机制预告片 ④AI 关卡流水线（参数表→生成→求解验证→精修）⑤死亡归因 ⑥心跳节奏 ⑦11 符咒草案表

### 【8.2】起点 · 核心玩法框架定稿
- **源文件**：`docx/十二生肖爬塔_核心玩法框架_0802.md`
- **形态**：Hero Tower Wars 式数值对撞爬塔（塔纯向上，N 层，顶层 Boss，每层 2~3 横向位置免费选）
- **核心系统**：数值对撞+吸收滚雪球 / 骰子步数经济（步数张力=难度主杠杆）/ 视野限制（只看清当前及邻近层）/ 十二生肖符咒收集（携带 1→2→3 随关卡解锁）
- **50 关五幕**：认知期 1~10 / 资源博弈期 11~20 / 符咒搭配期 21~30 / 高压期 31~40 / 极限与收尾 41~50
- **Boss 节奏**：第 3 关送狗符咒（教学），6/10/14/18/22/26/30/34/38/42/46 关各一生肖 Boss，第 50 关年兽终极
- **当时待定**：11 符咒效果、道具清单、Boss 出场顺序、数值公式、道具衰减曲线

---

## 四、待办与下一步

按价值排序（8.10 稿重排）：
- **P0** 分叉路径接入生成器：数据结构 线性数组→图结构 + genPath 改造 + 难度引擎分段改造（前段+各支线+汇合段胜率拼接）——门怪变真威胁、决策回归，是 8.9 诚实清单头号问题（分叉的运行时选择已由 8.13 自由移动天然解决，剩生成端）
- **P0** 低多边形色板/几何形态系统化（8.10 待确认 #8）+ 十二生肖颜色编码方案
- **P1** 宝箱系统接入（数值/骰子双池，玩家主动开启，结果池随 50 关衰减曲线）
- **P1** 武器系统（单槽、跨维度效果；前期示例已定：+6 数值 / 30% 击杀掉骰）
- **P1** 11 符咒效果定稿 + 接入（早期数值修饰→后期规则破格，生肖 Boss 关发放）
- **P2** 挑战模式（独立存档肉鸽，复用 genLevel 不同种子）+ 评审模式（全解锁）
- **P2** 死亡预警/归因 + 视野规则复评（现 3D 自带 6 格压制；8.10 稿定分叉点可见每分支第一格）
- **P2** 金币产出曲线 + 关外商店定价/强化曲线

---

## 五、文件索引

- `docx/十二生肖爬塔_核心玩法框架_0802.md` — 8.2 原始框架（塔式，已部分被 pivot 覆盖，仍作历史参考）
- `docx/十二生肖闯关_完整设计讨论稿_0810.md` — **当前核心参考**（8.10，六大新系统蓝图 + 50 关节奏表 + 待确认 8 条）
- `docx/像素风美术基调.md` — 像素风验收标准（8.9 定调，8.10 已废弃改低多边形，留作历史）
- `demo/index.html` — 当前可玩 demo（iso 9×9 v2）
- `demo/level1.html` — 第 1 关走廊验证版（8.3）
- `demo/level1-3d.html` — 第 1 关 3D+2D 双视角原型（8.9）
- `生肖英雄_游戏本体.html` — **游戏主入口**：主界面 + 存档/选关/商店/图鉴/成就/玩法说明/设置 + 50关游戏；主菜单可直接进入地图编辑器
- `地图编辑器.html` — **独立关卡编辑器 + 难度引擎**（画路径模型 + 风险制难度 + JSON/游戏文件导入导出 + 多层地图；由游戏主菜单直接跳转）
- `迭代优化过程.md` — 本文档
- `images/chars/` — **角色与怪物美术资源**：`character_enemy.png` 拆分产物——5 张主角（hero_front/left/back/right/action.png）+ 5 张怪物（mob_minotaur/goblin/skeleton/ghost/plague.png），8.15 主角接入游戏本体，怪物待后续接入 mob 格
- `images/bosses_3d/` — **生肖 Boss 3D 预览图 + 模型**：已交付 5/12（鼠/牛/虎/兔/龙 PNG 预览图 + GLB/OBJ 模型），狗占位图待明日 3D 图覆盖升级；剩余 7 个自动化续跑
- `images/bosses/` — **生肖 Boss 原设计图**：`boss.png` 12 生肖合图拆分后的 12 张独立 2D 原图（boss_rat/ox/tiger/rabbit/dragon/snake/horse/goat/monkey/rooster/dog/pig.png），用于 3D 生成时的参考图输入
